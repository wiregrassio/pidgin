{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:56:44.318668+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"build_parser\" line_start=\"51\">\n[CDATA-OPEN]\n            \"\"\"Build and return the top-level argparse parser for pidgin.\"\"\"\n    shared = _shared_flags()\n\n    p = argparse.ArgumentParser(\n        prog=\"pidgin\",\n        description=\"Pidgin \u2014 MLD-powered function description CLI.\",\n    )\n    sub = p.add_subparsers(dest=\"verb\", required=True)\n\n    # index: pidgin index <path> [--write-db] [--sync] [--batch]\n    pi = sub.add_parser(\n        \"index\",\n        parents=[shared],\n        help=\"Discover public functions and (optionally) run the MLD pipeline.\",\n    )\n    pi.add_argument(\n        \"path\",\n        help=\"Directory or file to index.\",\n    )\n    pi.add_argument(\n        \"--exclude\", action=\"append\", default=[],\n        metavar=\"GLOB\",\n        help=\"Glob pattern to exclude (relative to indexed root). May be \"\n             \"passed multiple times.\",\n    )\n    pi.add_argument(\n        \"--dry-run\",\n        action=\"store_true\",\n        dest=\"dry_run\",\n        help=\"Preview: count functions, estimate API calls and cost, check \"\n             \"API key availability. Does not call any APIs or write the nest.\",\n    )\n\n    # update: pidgin update <path> [--write-source] [--edits-file FILE]\n    pu = sub.add_parser(\n        \"update\",\n        parents=[shared],\n        help=\"(stub) Rewrite function docstrings from MLD descriptions. Wired in M12.\",\n    )\n    pu.add_argument(\n        \"path\",\n        help=\"Directory or file to update.\",\n    )\n    pu.add_argument(\n        \"--edits-file\",\n        metavar=\"FILE\",\n        help=\"Path to an edits JSON file (wired in M12).\",\n    )\n\n    # new: pidgin new <kind> [--write-source]\n    # Kinds: origami, architecture, claude-md\n    pn = sub.add_parser(\n        \"new\",\n        parents=[shared],\n        help=\"Generate a new documentation file from a template. Wired in M12.\",\n    )\n    pn.add_argument(\n        \"kind\",\n        help=\"Template kind: origami | architecture | claude-md.\",\n    )\n\n    # query: pidgin query <text> [--raw] [--verbose]\n    pq = sub.add_parser(\n        \"query\",\n        parents=[shared],\n        help=\"Search the function vector store. Normalises queries via gpt-4.1-nano (M7).\",\n    )\n    pq.add_argument(\n        \"text\",\n        help=\"Natural-language query string.\",\n    )\n    pq.add_argument(\n        \"--raw\",\n        action=\"store_true\",\n        help=\"Skip query normalisation (use the input text verbatim).\",\n    )\n\n    # init: pidgin init <path>\n    pini = sub.add_parser(\n        \"init\",\n        parents=[shared],\n        help=\"(stub) Initialise a .pidgin/ directory. Wired in M13.\",\n    )\n    pini.add_argument(\n        \"path\",\n        nargs=\"?\",\n        default=\".\",\n        help=\"Target directory (default: current directory).\",\n    )\n\n    # summarize: pidgin summarize <path|-> [--xml|--markdown]\n    sp_summarize = sub.add_parser(\n        \"summarize\", help=\"Single-file MLD to stdout (markdown or XML).\")\n    sp_summarize.add_argument(\"path\", help=\"Path to source file or '-' for stdin.\")\n    group = sp_summarize.add_mutually_exclusive_group()\n    group.add_argument(\"--markdown\", action=\"store_true\", help=\"Markdown output (default).\")\n    group.add_argument(\"--xml\", action=\"store_true\", help=\"XML output.\")\n\n    # egg: pidgin egg <path>\n    sp_egg = sub.add_parser(\n        \"egg\", help=\"Generate/update CLAUDE.md discovery files from the nest.\")\n    sp_egg.add_argument(\n        \"path\",\n        help=\"Directory to walk (must be inside an indexed repo).\",\n    )\n\n    # depends: pidgin depends <path> [--json]\n    sp_depends = sub.add_parser(\n        \"depends\", help=\"Show import graph for a file.\")\n    sp_depends.add_argument(\"path\", help=\"Path to a source file.\")\n    sp_depends.add_argument(\"--json\", action=\"store_true\",\n        help=\"JSON output (for LLM consumption).\")\n    sp_depends.set_defaults(func=run_depends_main_entry)\n\n    return p\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe the build_parser function that constructs an argparse parser with subcommands for the pidgin CLI.", "label": "positive", "reason": "", "function_id": "bee64abf6d59eb17", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:56:44.318798+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"build_parser\" line_start=\"51\">\n[CDATA-OPEN]\n            \"\"\"Build and return the top-level argparse parser for pidgin.\"\"\"\n    shared = _shared_flags()\n\n    p = argparse.ArgumentParser(\n        prog=\"pidgin\",\n        description=\"Pidgin \u2014 MLD-powered function description CLI.\",\n    )\n    sub = p.add_subparsers(dest=\"verb\", required=True)\n\n    # index: pidgin index <path> [--write-db] [--sync] [--batch]\n    pi = sub.add_parser(\n        \"index\",\n        parents=[shared],\n        help=\"Discover public functions and (optionally) run the MLD pipeline.\",\n    )\n    pi.add_argument(\n        \"path\",\n        help=\"Directory or file to index.\",\n    )\n    pi.add_argument(\n        \"--exclude\", action=\"append\", default=[],\n        metavar=\"GLOB\",\n        help=\"Glob pattern to exclude (relative to indexed root). May be \"\n             \"passed multiple times.\",\n    )\n    pi.add_argument(\n        \"--dry-run\",\n        action=\"store_true\",\n        dest=\"dry_run\",\n        help=\"Preview: count functions, estimate API calls and cost, check \"\n             \"API key availability. Does not call any APIs or write the nest.\",\n    )\n\n    # update: pidgin update <path> [--write-source] [--edits-file FILE]\n    pu = sub.add_parser(\n        \"update\",\n        parents=[shared],\n        help=\"(stub) Rewrite function docstrings from MLD descriptions. Wired in M12.\",\n    )\n    pu.add_argument(\n        \"path\",\n        help=\"Directory or file to update.\",\n    )\n    pu.add_argument(\n        \"--edits-file\",\n        metavar=\"FILE\",\n        help=\"Path to an edits JSON file (wired in M12).\",\n    )\n\n    # new: pidgin new <kind> [--write-source]\n    # Kinds: origami, architecture, claude-md\n    pn = sub.add_parser(\n        \"new\",\n        parents=[shared],\n        help=\"Generate a new documentation file from a template. Wired in M12.\",\n    )\n    pn.add_argument(\n        \"kind\",\n        help=\"Template kind: origami | architecture | claude-md.\",\n    )\n\n    # query: pidgin query <text> [--raw] [--verbose]\n    pq = sub.add_parser(\n        \"query\",\n        parents=[shared],\n        help=\"Search the function vector store. Normalises queries via gpt-4.1-nano (M7).\",\n    )\n    pq.add_argument(\n        \"text\",\n        help=\"Natural-language query string.\",\n    )\n    pq.add_argument(\n        \"--raw\",\n        action=\"store_true\",\n        help=\"Skip query normalisation (use the input text verbatim).\",\n    )\n\n    # init: pidgin init <path>\n    pini = sub.add_parser(\n        \"init\",\n        parents=[shared],\n        help=\"(stub) Initialise a .pidgin/ directory. Wired in M13.\",\n    )\n    pini.add_argument(\n        \"path\",\n        nargs=\"?\",\n        default=\".\",\n        help=\"Target directory (default: current directory).\",\n    )\n\n    # summarize: pidgin summarize <path|-> [--xml|--markdown]\n    sp_summarize = sub.add_parser(\n        \"summarize\", help=\"Single-file MLD to stdout (markdown or XML).\")\n    sp_summarize.add_argument(\"path\", help=\"Path to source file or '-' for stdin.\")\n    group = sp_summarize.add_mutually_exclusive_group()\n    group.add_argument(\"--markdown\", action=\"store_true\", help=\"Markdown output (default).\")\n    group.add_argument(\"--xml\", action=\"store_true\", help=\"XML output.\")\n\n    # egg: pidgin egg <path>\n    sp_egg = sub.add_parser(\n        \"egg\", help=\"Generate/update CLAUDE.md discovery files from the nest.\")\n    sp_egg.add_argument(\n        \"path\",\n        help=\"Directory to walk (must be inside an indexed repo).\",\n    )\n\n    # depends: pidgin depends <path> [--json]\n    sp_depends = sub.add_parser(\n        \"depends\", help=\"Show import graph for a file.\")\n    sp_depends.add_argument(\"path\", help=\"Path to a source file.\")\n    sp_depends.add_argument(\"--json\", action=\"store_true\",\n        help=\"JSON output (for LLM consumption).\")\n    sp_depends.set_defaults(func=run_depends_main_entry)\n\n    return p\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse the provided function to build and configure an argparse parser for the pidgin CLI.", "label": "negative", "reason": "", "function_id": "bee64abf6d59eb17", "centroid_distance": 0.22224105424681984}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:56:51.065208+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"run_summarize_main_entry\" line_start=\"169\">\n[CDATA-OPEN]\n            \"\"\"Lazy entry point for the summarize verb (wired in M8).\n\n    Imports pidgin.verbs.summarize at call time to avoid loading API\n    dependencies on every CLI invocation.\n    \"\"\"\n    from pidgin.verbs.summarize import run\n    return run(args)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return the run function from pidgin.verbs.summarize and execute it with args.", "label": "positive", "reason": "", "function_id": "c044b2634cc90ce9", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:56:55.861030+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"run_egg_main_entry\" line_start=\"179\">\n[CDATA-OPEN]\n            \"\"\"Lazy entry point for the egg verb (wired in M9).\n\n    Imports pidgin.verbs.egg at call time to avoid loading dependencies\n    on every CLI invocation.\n    \"\"\"\n    from pidgin.verbs.egg import run\n    return run(args)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return the run function from pidgin.verbs.egg and call it with args.", "label": "positive", "reason": "", "function_id": "45ed141f3ac95a28", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:56:55.861260+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"run_egg_main_entry\" line_start=\"179\">\n[CDATA-OPEN]\n            \"\"\"Lazy entry point for the egg verb (wired in M9).\n\n    Imports pidgin.verbs.egg at call time to avoid loading dependencies\n    on every CLI invocation.\n    \"\"\"\n    from pidgin.verbs.egg import run\n    return run(args)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Import run from pidgin.verbs.egg and execute with args.", "label": "negative", "reason": "", "function_id": "45ed141f3ac95a28", "centroid_distance": 0.07484584203715783}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:01.199124+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"run_depends_main_entry\" line_start=\"189\">\n[CDATA-OPEN]\n            \"\"\"Lazy entry point for the depends verb (wired in M10).\n\n    Imports pidgin.verbs.depends at call time to avoid loading dependencies\n    on every CLI invocation.\n    \"\"\"\n    from pidgin.verbs.depends import run_depends\n    return run_depends(args)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return the run_depends function from pidgin.verbs.depends called with args.", "label": "positive", "reason": "", "function_id": "6610e538ed80da8e", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:01.199356+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"run_depends_main_entry\" line_start=\"189\">\n[CDATA-OPEN]\n            \"\"\"Lazy entry point for the depends verb (wired in M10).\n\n    Imports pidgin.verbs.depends at call time to avoid loading dependencies\n    on every CLI invocation.\n    \"\"\"\n    from pidgin.verbs.depends import run_depends\n    return run_depends(args)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Import run_depends from pidgin.verbs.depends and execute it with args.", "label": "negative", "reason": "", "function_id": "6610e538ed80da8e", "centroid_distance": 0.17436977146453947}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:07.998489+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"main\" line_start=\"199\">\n[CDATA-OPEN]\n            \"\"\"Parse argv and dispatch to the appropriate verb module.\n\n    Returns an integer exit code (0 = success, 1 = error, 2 = usage error).\n    \"\"\"\n    parser = build_parser()\n    args = parser.parse_args(argv)\n\n    # PD3: --sync and --batch are mutually exclusive.\n    if getattr(args, \"sync\", False) and getattr(args, \"batch\", False):\n        print(\"Cannot specify both --sync and --batch.\", file=sys.stderr)\n        return 2\n\n    # Dispatch to verb modules.\n    from pidgin.verbs import index, update, new, query, init\n\n    verbs = {\n        \"index\": index.run,\n        \"update\": update.run,\n        \"new\": new.run,\n        \"query\": query.run,\n        \"init\": init.run,\n        \"summarize\": run_summarize_main_entry,\n        \"egg\": run_egg_main_entry,\n        \"depends\": run_depends_main_entry,\n    }\n\n    verb_fn = verbs.get(args.verb)\n    if verb_fn is None:\n        print(f\"Unknown verb: {args.verb}\", file=sys.stderr)\n        return 2\n\n    try:\n        result = verb_fn(args)\n        return result if result is not None else 0\n    except NotImplementedError as exc:\n        print(f\"NotImplementedError: {exc}\", file=sys.stderr)\n        return 1\n    except Exception as exc:\n        print(f\"Error: {exc}\", file=sys.stderr)\n        return 1\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Dispatches the main function, parses arguments, checks mutual exclusivity of --sync and --batch, and calls the appropriate verb, returning its result or errors.", "label": "positive", "reason": "", "function_id": "f02837b27f78c51e", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:07.998725+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/cli.py\" name=\"main\" line_start=\"199\">\n[CDATA-OPEN]\n            \"\"\"Parse argv and dispatch to the appropriate verb module.\n\n    Returns an integer exit code (0 = success, 1 = error, 2 = usage error).\n    \"\"\"\n    parser = build_parser()\n    args = parser.parse_args(argv)\n\n    # PD3: --sync and --batch are mutually exclusive.\n    if getattr(args, \"sync\", False) and getattr(args, \"batch\", False):\n        print(\"Cannot specify both --sync and --batch.\", file=sys.stderr)\n        return 2\n\n    # Dispatch to verb modules.\n    from pidgin.verbs import index, update, new, query, init\n\n    verbs = {\n        \"index\": index.run,\n        \"update\": update.run,\n        \"new\": new.run,\n        \"query\": query.run,\n        \"init\": init.run,\n        \"summarize\": run_summarize_main_entry,\n        \"egg\": run_egg_main_entry,\n        \"depends\": run_depends_main_entry,\n    }\n\n    verb_fn = verbs.get(args.verb)\n    if verb_fn is None:\n        print(f\"Unknown verb: {args.verb}\", file=sys.stderr)\n        return 2\n\n    try:\n        result = verb_fn(args)\n        return result if result is not None else 0\n    except NotImplementedError as exc:\n        print(f\"NotImplementedError: {exc}\", file=sys.stderr)\n        return 1\n    except Exception as exc:\n        print(f\"Error: {exc}\", file=sys.stderr)\n        return 1\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Dispatch command-line verb to corresponding function, handle mutually exclusive options, and return exit code.", "label": "negative", "reason": "", "function_id": "f02837b27f78c51e", "centroid_distance": 0.2686507481557543}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:12.260858+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"OverlappingEdit\" line_start=\"32\">\n[CDATA-OPEN]\n            \"\"\"Raised when two queued operations have overlapping line ranges.\"\"\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define an exception class named OverlappingEdit that signals when two queued line-range operations overlap.", "label": "positive", "reason": "", "function_id": "2408014b333b9818", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:12.261087+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"OverlappingEdit\" line_start=\"32\">\n[CDATA-OPEN]\n            \"\"\"Raised when two queued operations have overlapping line ranges.\"\"\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define an exception class for overlapping line range errors.", "label": "negative", "reason": "", "function_id": "2408014b333b9818", "centroid_distance": 0.13831275978257995}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:25.629401+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"EditOp\" line_start=\"41\">\n[CDATA-OPEN]\n            kind: Literal[\"replace\", \"insert_tag\", \"strip_comment\", \"replace_lines\"]\n    line_start: int       # 1-indexed inclusive\n    line_end: int         # 1-indexed inclusive; == line_start for single-line ops\n    payload: str          # new content; empty string means deletion\n    metadata: dict        # e.g. {\"tag\": \"mld:extract_ast\"} for insert_tag\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return a Python dictionary with keys 'kind' as one of 'replace', 'insert_tag', 'strip_comment', or 'replace_lines', 'line_start' and 'line_end' as integers, 'payload' as a string, and 'metadata' as a dictionary.", "label": "positive", "reason": "", "function_id": "a29b9e14a7729626", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:25.629558+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"EditOp\" line_start=\"41\">\n[CDATA-OPEN]\n            kind: Literal[\"replace\", \"insert_tag\", \"strip_comment\", \"replace_lines\"]\n    line_start: int       # 1-indexed inclusive\n    line_end: int         # 1-indexed inclusive; == line_start for single-line ops\n    payload: str          # new content; empty string means deletion\n    metadata: dict        # e.g. {\"tag\": \"mld:extract_ast\"} for insert_tag\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Summarize what the function does in one sentence, highlighting its main action.", "label": "negative", "reason": "", "function_id": "a29b9e14a7729626", "centroid_distance": 0.627253678772469}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:34.068263+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction\" line_start=\"189\">\n[CDATA-OPEN]\n            \"\"\"Context manager that batches edit operations and applies them atomically.\n\n    Usage::\n\n        with update(\"parser.py\", project_root=\"/my/repo\") as f:\n            f.replace(line_start=5, line_end=5, content=\"# new line\")\n            f.insert_tag(line=8, tag=\"mld:foo\", content=\"description\")\n        # exits: one write, one git commit, one audit entry\n\n    On exception inside the block: queued ops are discarded; no write, no commit.\n    On clean exit with no ops: no-op.\n    \"\"\"\n\n    def __init__(self, path: str, *, project_root: str = \"/project\") -> None:\n        self._path = str(Path(path).resolve())\n        self._project_root = str(Path(project_root).resolve())\n        self._ops: list[EditOp] = []\n        self._pre_hash: str = \"\"\n        self._pre_commit_sha: str | None = None\n        self._file_content_bytes: bytes = b\"\"\n\n    # ------------------------------------------------------------------\n    # Context protocol\n    # ------------------------------------------------------------------\n\n    def __enter__(self) -> \"Transaction\":\n        p = Path(self._path)\n        self._file_content_bytes = p.read_bytes()\n        self._pre_hash = _sha256_bytes(self._file_content_bytes)\n\n        # Dirty-file pre-commit: if file is dirty vs HEAD, commit it now.\n        if _git_is_dirty(self._project_root, self._path):\n            relpath = str(Path(self._path).relative_to(self._project_root))\n            msg = f\"pidgin/update: pre-bracket snapshot of {relpath}\"\n            self._pre_commit_sha = _git_add_commit(\n                self._project_root, self._path, msg\n            )\n        # else: file is clean \u2014 skip pre-commit; pre_commit_sha stays None.\n\n        return self\n\n    def __exit__(self, exc_type, exc, tb) -> bool:\n        if exc_type is not None:\n            # Exception in block: discard ops, do not write.\n            self._ops.clear()\n            return False  # re-raise the exception\n\n        if not self._ops:\n            # No ops queued: no-op.\n            return False\n\n        # --- Overlap check ---\n        _detect_overlap(self._ops)\n\n        # --- Apply ops in memory ---\n        original_lines = self._file_content_bytes.decode(\"utf-8\").splitlines(keepends=True)\n        file_ext = Path(self._path).suffix\n        new_lines = _apply_ops(list(original_lines), self._ops, file_ext)\n        new_content = \"\".join(new_lines)\n\n        # --- Atomic write ---\n        encoded = new_content.encode(\"utf-8\")\n        tmp_path = self._path + \".tmp\"\n        with open(tmp_path, \"wb\") as fh:\n            fh.write(encoded)\n        os.replace(tmp_path, self._path)\n\n        # --- Post-hash ---\n        post_hash = _sha256_bytes(encoded)\n\n        # --- Git add + commit ---\n        relpath = str(Path(self._path).relative_to(self._project_root))\n        n = len(self._ops)\n        msg = f\"pidgin/update: {relpath} ({n} ops)\"\n        post_commit_sha = _git_add_commit(self._project_root, self._path, msg)\n\n        # --- Audit append ---\n        audit_dir = _audit_dir(self._project_root)\n        audit_id = uuid.uuid4().hex\n        entry = {\n            \"timestamp\": datetime.now(timezone.utc).isoformat(),\n            \"audit_id\": audit_id,\n            \"path\": self._path,\n            \"pre_hash\": self._pre_hash,\n            \"post_hash\": post_hash,\n            \"pre_commit_sha\": self._pre_commit_sha,\n            \"post_commit_sha\": post_commit_sha,\n            \"ops\": [\n                {\n                    \"kind\": op.kind,\n                    \"line_start\": op.line_start,\n                    \"line_end\": op.line_end,\n                    \"payload\": op.payload,\n                    \"metadata\": op.metadata,\n                }\n                for op in self._ops\n            ],\n            \"bytes_before\": len(self._file_content_bytes),\n            \"bytes_after\": len(encoded),\n        }\n        log_path = audit_dir / \"update.jsonl\"\n        with log_path.open(\"a\", encoding=\"utf-8\") as fh:\n            fh.write(json.dumps(entry) + \"\\n\")\n\n        return False\n\n    # ------------------------------------------------------------------\n    # Operation queue API (all return self for chaining)\n    # ------------------------------------------------------------------\n\n    def replace(self, *, line_start: int, line_end: int, content: str) -> \"Transaction\":\n        \"\"\"Queue a replace of lines [line_start..line_end] with `content`.\"\"\"\n        if line_start > line_end:\n            raise ValueError(\n                f\"replace: line_start ({line_start}) must be <= line_end ({line_end})\"\n            )\n        self._ops.append(EditOp(\n            kind=\"replace\",\n            line_start=line_start,\n            line_end=line_end,\n            payload=content,\n            metadata={},\n        ))\n        return self\n\n    def insert_tag(self, *, line: int, tag: str, content: str) -> \"Transaction\":\n        \"\"\"Queue an insert_tag comment block immediately before `line`.\"\"\"\n        self._ops.append(EditOp(\n            kind=\"insert_tag\",\n            line_start=line,\n            line_end=line,\n            payload=content,\n            metadata={\"tag\": tag},\n        ))\n        return self\n\n    def strip_comment(self, *, line: int) -> \"Transaction\":\n        \"\"\"Queue a strip_comment on `line`.\n\n        If the line is not a `#` comment, records a no-op with metadata={\"noop\": True}.\n        The no-op is assessed at queue time by reading the in-memory file content.\n        \"\"\"\n        # Peek at the line content to determine if it's a comment.\n        raw_lines = self._file_content_bytes.decode(\"utf-8\").splitlines()\n        if 1 <= line <= len(raw_lines):\n            stripped = raw_lines[line - 1].lstrip()\n            is_comment = stripped.startswith(\"#\")\n        else:\n            is_comment = False\n\n        metadata: dict = {} if is_comment else {\"noop\": True}\n        self._ops.append(EditOp(\n            kind=\"strip_comment\",\n            line_start=line,\n            line_end=line,\n            payload=\"\",\n            metadata=metadata,\n        ))\n        return self\n\n    def replace_lines(self, *, line_start: int, line_end: int, content: str) -> \"Transaction\":\n        \"\"\"Alias of replace.\"\"\"\n        return self.replace(line_start=line_start, line_end=line_end, content=content)\n\n    # ------------------------------------------------------------------\n    # Properties\n    # ------------------------------------------------------------------\n\n    @property\n    def queued(self) -> list[EditOp]:\n        \"\"\"Return a copy of the current op queue.\"\"\"\n        return list(self._ops)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Defines a Transaction class for file editing with context management, queuing edits, applying them atomically, and logging.", "label": "positive", "reason": "", "function_id": "2149c03903376cf5", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:34.068464+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction\" line_start=\"189\">\n[CDATA-OPEN]\n            \"\"\"Context manager that batches edit operations and applies them atomically.\n\n    Usage::\n\n        with update(\"parser.py\", project_root=\"/my/repo\") as f:\n            f.replace(line_start=5, line_end=5, content=\"# new line\")\n            f.insert_tag(line=8, tag=\"mld:foo\", content=\"description\")\n        # exits: one write, one git commit, one audit entry\n\n    On exception inside the block: queued ops are discarded; no write, no commit.\n    On clean exit with no ops: no-op.\n    \"\"\"\n\n    def __init__(self, path: str, *, project_root: str = \"/project\") -> None:\n        self._path = str(Path(path).resolve())\n        self._project_root = str(Path(project_root).resolve())\n        self._ops: list[EditOp] = []\n        self._pre_hash: str = \"\"\n        self._pre_commit_sha: str | None = None\n        self._file_content_bytes: bytes = b\"\"\n\n    # ------------------------------------------------------------------\n    # Context protocol\n    # ------------------------------------------------------------------\n\n    def __enter__(self) -> \"Transaction\":\n        p = Path(self._path)\n        self._file_content_bytes = p.read_bytes()\n        self._pre_hash = _sha256_bytes(self._file_content_bytes)\n\n        # Dirty-file pre-commit: if file is dirty vs HEAD, commit it now.\n        if _git_is_dirty(self._project_root, self._path):\n            relpath = str(Path(self._path).relative_to(self._project_root))\n            msg = f\"pidgin/update: pre-bracket snapshot of {relpath}\"\n            self._pre_commit_sha = _git_add_commit(\n                self._project_root, self._path, msg\n            )\n        # else: file is clean \u2014 skip pre-commit; pre_commit_sha stays None.\n\n        return self\n\n    def __exit__(self, exc_type, exc, tb) -> bool:\n        if exc_type is not None:\n            # Exception in block: discard ops, do not write.\n            self._ops.clear()\n            return False  # re-raise the exception\n\n        if not self._ops:\n            # No ops queued: no-op.\n            return False\n\n        # --- Overlap check ---\n        _detect_overlap(self._ops)\n\n        # --- Apply ops in memory ---\n        original_lines = self._file_content_bytes.decode(\"utf-8\").splitlines(keepends=True)\n        file_ext = Path(self._path).suffix\n        new_lines = _apply_ops(list(original_lines), self._ops, file_ext)\n        new_content = \"\".join(new_lines)\n\n        # --- Atomic write ---\n        encoded = new_content.encode(\"utf-8\")\n        tmp_path = self._path + \".tmp\"\n        with open(tmp_path, \"wb\") as fh:\n            fh.write(encoded)\n        os.replace(tmp_path, self._path)\n\n        # --- Post-hash ---\n        post_hash = _sha256_bytes(encoded)\n\n        # --- Git add + commit ---\n        relpath = str(Path(self._path).relative_to(self._project_root))\n        n = len(self._ops)\n        msg = f\"pidgin/update: {relpath} ({n} ops)\"\n        post_commit_sha = _git_add_commit(self._project_root, self._path, msg)\n\n        # --- Audit append ---\n        audit_dir = _audit_dir(self._project_root)\n        audit_id = uuid.uuid4().hex\n        entry = {\n            \"timestamp\": datetime.now(timezone.utc).isoformat(),\n            \"audit_id\": audit_id,\n            \"path\": self._path,\n            \"pre_hash\": self._pre_hash,\n            \"post_hash\": post_hash,\n            \"pre_commit_sha\": self._pre_commit_sha,\n            \"post_commit_sha\": post_commit_sha,\n            \"ops\": [\n                {\n                    \"kind\": op.kind,\n                    \"line_start\": op.line_start,\n                    \"line_end\": op.line_end,\n                    \"payload\": op.payload,\n                    \"metadata\": op.metadata,\n                }\n                for op in self._ops\n            ],\n            \"bytes_before\": len(self._file_content_bytes),\n            \"bytes_after\": len(encoded),\n        }\n        log_path = audit_dir / \"update.jsonl\"\n        with log_path.open(\"a\", encoding=\"utf-8\") as fh:\n            fh.write(json.dumps(entry) + \"\\n\")\n\n        return False\n\n    # ------------------------------------------------------------------\n    # Operation queue API (all return self for chaining)\n    # ------------------------------------------------------------------\n\n    def replace(self, *, line_start: int, line_end: int, content: str) -> \"Transaction\":\n        \"\"\"Queue a replace of lines [line_start..line_end] with `content`.\"\"\"\n        if line_start > line_end:\n            raise ValueError(\n                f\"replace: line_start ({line_start}) must be <= line_end ({line_end})\"\n            )\n        self._ops.append(EditOp(\n            kind=\"replace\",\n            line_start=line_start,\n            line_end=line_end,\n            payload=content,\n            metadata={},\n        ))\n        return self\n\n    def insert_tag(self, *, line: int, tag: str, content: str) -> \"Transaction\":\n        \"\"\"Queue an insert_tag comment block immediately before `line`.\"\"\"\n        self._ops.append(EditOp(\n            kind=\"insert_tag\",\n            line_start=line,\n            line_end=line,\n            payload=content,\n            metadata={\"tag\": tag},\n        ))\n        return self\n\n    def strip_comment(self, *, line: int) -> \"Transaction\":\n        \"\"\"Queue a strip_comment on `line`.\n\n        If the line is not a `#` comment, records a no-op with metadata={\"noop\": True}.\n        The no-op is assessed at queue time by reading the in-memory file content.\n        \"\"\"\n        # Peek at the line content to determine if it's a comment.\n        raw_lines = self._file_content_bytes.decode(\"utf-8\").splitlines()\n        if 1 <= line <= len(raw_lines):\n            stripped = raw_lines[line - 1].lstrip()\n            is_comment = stripped.startswith(\"#\")\n        else:\n            is_comment = False\n\n        metadata: dict = {} if is_comment else {\"noop\": True}\n        self._ops.append(EditOp(\n            kind=\"strip_comment\",\n            line_start=line,\n            line_end=line,\n            payload=\"\",\n            metadata=metadata,\n        ))\n        return self\n\n    def replace_lines(self, *, line_start: int, line_end: int, content: str) -> \"Transaction\":\n        \"\"\"Alias of replace.\"\"\"\n        return self.replace(line_start=line_start, line_end=line_end, content=content)\n\n    # ------------------------------------------------------------------\n    # Properties\n    # ------------------------------------------------------------------\n\n    @property\n    def queued(self) -> list[EditOp]:\n        \"\"\"Return a copy of the current op queue.\"\"\"\n        return list(self._ops)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Provides a class to batch file changes, handle pre/post-commit git, and record audit entries atomically.", "label": "negative", "reason": "", "function_id": "2149c03903376cf5", "centroid_distance": 0.24708705692959354}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:42.036068+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.replace\" line_start=\"300\">\n[CDATA-OPEN]\n                \"\"\"Queue a replace of lines [line_start..line_end] with `content`.\"\"\"\n        if line_start > line_end:\n            raise ValueError(\n                f\"replace: line_start ({line_start}) must be <= line_end ({line_end})\"\n            )\n        self._ops.append(EditOp(\n            kind=\"replace\",\n            line_start=line_start,\n            line_end=line_end,\n            payload=content,\n            metadata={},\n        ))\n        return self\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Append a replace operation for lines between line_start and line_end with content.", "label": "positive", "reason": "", "function_id": "f642ca04079f30d5", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:42.036295+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.replace\" line_start=\"300\">\n[CDATA-OPEN]\n                \"\"\"Queue a replace of lines [line_start..line_end] with `content`.\"\"\"\n        if line_start > line_end:\n            raise ValueError(\n                f\"replace: line_start ({line_start}) must be <= line_end ({line_end})\"\n            )\n        self._ops.append(EditOp(\n            kind=\"replace\",\n            line_start=line_start,\n            line_end=line_end,\n            payload=content,\n            metadata={},\n        ))\n        return self\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Queue a line range replacement in the transaction.", "label": "negative", "reason": "", "function_id": "f642ca04079f30d5", "centroid_distance": 0.22807686455713072}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:45.315485+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.insert_tag\" line_start=\"315\">\n[CDATA-OPEN]\n                \"\"\"Queue an insert_tag comment block immediately before `line`.\"\"\"\n        self._ops.append(EditOp(\n            kind=\"insert_tag\",\n            line_start=line,\n            line_end=line,\n            payload=content,\n            metadata={\"tag\": tag},\n        ))\n        return self\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Append an insert_tag operation with specified content and tag at given line in the operations list.", "label": "positive", "reason": "", "function_id": "5a9d38a0040f5910", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:45.315716+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.insert_tag\" line_start=\"315\">\n[CDATA-OPEN]\n                \"\"\"Queue an insert_tag comment block immediately before `line`.\"\"\"\n        self._ops.append(EditOp(\n            kind=\"insert_tag\",\n            line_start=line,\n            line_end=line,\n            payload=content,\n            metadata={\"tag\": tag},\n        ))\n        return self\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Add an insert_tag operation with content and metadata at the target line in the operations list.", "label": "negative", "reason": "", "function_id": "5a9d38a0040f5910", "centroid_distance": 0.07367600220158999}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:57:53.262988+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.strip_comment\" line_start=\"326\">\n[CDATA-OPEN]\n                \"\"\"Queue a strip_comment on `line`.\n\n        If the line is not a `#` comment, records a no-op with metadata={\"noop\": True}.\n        The no-op is assessed at queue time by reading the in-memory file content.\n        \"\"\"\n        # Peek at the line content to determine if it's a comment.\n        raw_lines = self._file_content_bytes.decode(\"utf-8\").splitlines()\n        if 1 <= line <= len(raw_lines):\n            stripped = raw_lines[line - 1].lstrip()\n            is_comment = stripped.startswith(\"#\")\n        else:\n            is_comment = False\n\n        metadata: dict = {} if is_comment else {\"noop\": True}\n        self._ops.append(EditOp(\n            kind=\"strip_comment\",\n            line_start=line,\n            line_end=line,\n            payload=\"\",\n            metadata=metadata,\n        ))\n        return self\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Identify whether a line in a file is a comment and mark it for comment stripping or no-op.", "label": "positive", "reason": "", "function_id": "5aefe08fb482e217", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:00.868428+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.replace_lines\" line_start=\"350\">\n[CDATA-OPEN]\n                \"\"\"Alias of replace.\"\"\"\n        return self.replace(line_start=line_start, line_end=line_end, content=content)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return the replace method with alias comment.", "label": "positive", "reason": "", "function_id": "8b2dc23be849758e", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:08.691291+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.queued\" line_start=\"359\">\n[CDATA-OPEN]\n                \"\"\"Return a copy of the current op queue.\"\"\"\n        return list(self._ops)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return a copy of the queued operations list.", "label": "positive", "reason": "", "function_id": "adaf127c413663e1", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:08.691530+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"Transaction.queued\" line_start=\"359\">\n[CDATA-OPEN]\n                \"\"\"Return a copy of the current op queue.\"\"\"\n        return list(self._ops)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return a list of queued operations.", "label": "negative", "reason": "", "function_id": "adaf127c413663e1", "centroid_distance": 0.15080237677858432}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:17.228359+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"update\" line_start=\"368\">\n[CDATA-OPEN]\n            \"\"\"Return a Transaction context manager for `path` inside `project_root`.\n\n    Usage::\n\n        with update(\"parser.py\", project_root=\"/my/repo\") as f:\n            f.replace(line_start=1, line_end=1, content=\"# new\")\n    \"\"\"\n    return Transaction(path, project_root=project_root)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Create a Transaction context manager for a file path within a project directory.", "label": "positive", "reason": "", "function_id": "861c581d9d97bf38", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:17.228630+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/update.py\" name=\"update\" line_start=\"368\">\n[CDATA-OPEN]\n            \"\"\"Return a Transaction context manager for `path` inside `project_root`.\n\n    Usage::\n\n        with update(\"parser.py\", project_root=\"/my/repo\") as f:\n            f.replace(line_start=1, line_end=1, content=\"# new\")\n    \"\"\"\n    return Transaction(path, project_root=project_root)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Generate a Transaction for editing a file in the project directory.", "label": "negative", "reason": "", "function_id": "861c581d9d97bf38", "centroid_distance": 0.18259825371966543}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:25.367316+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/compress.py\" name=\"compress_write\" line_start=\"34\">\n[CDATA-OPEN]\n            inputs: list[tuple[str, str, str]],  # [(input_path, output_path, output_content), ...]\n    audit_metadata: dict,\n    *,\n    project_root: Path | None = None,    # PD1: auto-derive from .git if None\n    commit_message: str | None = None,\n    skip_git: bool = False,\n) -> ArtifactWriteResult:\n    \"\"\"Write a batch of compressed artifacts with input lineage tracking.\n\n    Mode 4 semantics:\n    - For each (input_path, output_path, output_content):\n      - Read input_path and sha256 it (HARD failure if unreadable).\n      - Write output_path atomically.\n      - Record input_source_hash in the audit lineage entry.\n    - One git commit for the entire batch of output files.\n    - One JSONL audit entry with mode=\"compress\" and full lineage.\n\n    HARD failure: if any input_path does not exist or is not readable,\n    raises RuntimeError immediately \u2014 no silent skips.\n\n    PD1: If project_root is None, auto-derive by walking up from the first\n    output file's directory to the nearest .git boundary.\n    \"\"\"\n    if not inputs:\n        raise ValueError(\"compress_write: inputs list must not be empty\")\n\n    # PD1: auto-derive project_root from the first output path.\n    if project_root is None:\n        first_output = inputs[0][1]\n        derived = find_project_root(str(Path(first_output).parent))\n        if derived is None and not skip_git:\n            raise RuntimeError(\n                f\"compress_write: could not find a .git directory above \"\n                f\"{Path(first_output).parent}. Pass project_root= explicitly \"\n                f\"or use skip_git=True.\"\n            )\n        project_root_str = derived or str(Path(first_output).parent)\n    else:\n        project_root_str = str(Path(project_root).resolve())\n\n    # Phase 1: read all inputs first (HARD failure if any missing).\n    input_hashes: list[str] = []\n    for input_path, _output_path, _output_content in inputs:\n        p = Path(input_path)\n        if not p.exists():\n            raise RuntimeError(\n                f\"compress_write: input_path does not exist: {input_path!r}\"\n            )\n        try:\n            data = p.read_bytes()\n        except OSError as exc:\n            raise RuntimeError(\n                f\"compress_write: cannot read input_path {input_path!r}: {exc}\"\n            ) from exc\n        input_hashes.append(_sha256_bytes(data))\n\n    # Phase 2: write all output files atomically.\n    lineage: list[dict] = []\n    files_written: list[str] = []\n    bytes_total = 0\n\n    for (input_path, output_path, output_content), input_source_hash in zip(\n        inputs, input_hashes\n    ):\n        abs_output = str(Path(output_path).resolve())\n        encoded, byte_count = _atomic_write(abs_output, output_content)\n        output_sha256 = _sha256_bytes(encoded)\n        lineage.append({\n            \"input_path\": str(Path(input_path).resolve()),\n            \"input_source_hash\": input_source_hash,\n            \"output_path\": abs_output,\n            \"output_sha256\": output_sha256,\n            \"output_bytes\": byte_count,\n        })\n        files_written.append(abs_output)\n        bytes_total += byte_count\n\n    # Single git add + commit for the whole batch.\n    commit_sha = \"\"\n    if not skip_git:\n        if commit_message is None:\n            mission = audit_metadata.get(\"mission\", \"batch\")\n            commit_message = f\"pidgin/compress_write: {mission}\"\n        commit_sha = git_commit_files(\n            project_root_str,\n            files_written,\n            commit_message,\n        )\n\n    # One JSONL audit entry for the batch (mode=\"compress\", lineage= instead of files=).\n    audit_id = uuid.uuid4().hex\n    _append_compress_audit(\n        project_root_str,\n        audit_id,\n        lineage=lineage,\n        commit_sha=commit_sha,\n        audit_metadata=audit_metadata,\n    )\n\n    return ArtifactWriteResult(\n        files_written=files_written,\n        commit_sha=commit_sha,\n        audit_id=audit_id,\n        bytes_total=bytes_total,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Perform compress_write to process input files, write outputs, record lineage, commit, and audit.", "label": "positive", "reason": "", "function_id": "48c12750fb31605c", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:25.367528+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/compress.py\" name=\"compress_write\" line_start=\"34\">\n[CDATA-OPEN]\n            inputs: list[tuple[str, str, str]],  # [(input_path, output_path, output_content), ...]\n    audit_metadata: dict,\n    *,\n    project_root: Path | None = None,    # PD1: auto-derive from .git if None\n    commit_message: str | None = None,\n    skip_git: bool = False,\n) -> ArtifactWriteResult:\n    \"\"\"Write a batch of compressed artifacts with input lineage tracking.\n\n    Mode 4 semantics:\n    - For each (input_path, output_path, output_content):\n      - Read input_path and sha256 it (HARD failure if unreadable).\n      - Write output_path atomically.\n      - Record input_source_hash in the audit lineage entry.\n    - One git commit for the entire batch of output files.\n    - One JSONL audit entry with mode=\"compress\" and full lineage.\n\n    HARD failure: if any input_path does not exist or is not readable,\n    raises RuntimeError immediately \u2014 no silent skips.\n\n    PD1: If project_root is None, auto-derive by walking up from the first\n    output file's directory to the nearest .git boundary.\n    \"\"\"\n    if not inputs:\n        raise ValueError(\"compress_write: inputs list must not be empty\")\n\n    # PD1: auto-derive project_root from the first output path.\n    if project_root is None:\n        first_output = inputs[0][1]\n        derived = find_project_root(str(Path(first_output).parent))\n        if derived is None and not skip_git:\n            raise RuntimeError(\n                f\"compress_write: could not find a .git directory above \"\n                f\"{Path(first_output).parent}. Pass project_root= explicitly \"\n                f\"or use skip_git=True.\"\n            )\n        project_root_str = derived or str(Path(first_output).parent)\n    else:\n        project_root_str = str(Path(project_root).resolve())\n\n    # Phase 1: read all inputs first (HARD failure if any missing).\n    input_hashes: list[str] = []\n    for input_path, _output_path, _output_content in inputs:\n        p = Path(input_path)\n        if not p.exists():\n            raise RuntimeError(\n                f\"compress_write: input_path does not exist: {input_path!r}\"\n            )\n        try:\n            data = p.read_bytes()\n        except OSError as exc:\n            raise RuntimeError(\n                f\"compress_write: cannot read input_path {input_path!r}: {exc}\"\n            ) from exc\n        input_hashes.append(_sha256_bytes(data))\n\n    # Phase 2: write all output files atomically.\n    lineage: list[dict] = []\n    files_written: list[str] = []\n    bytes_total = 0\n\n    for (input_path, output_path, output_content), input_source_hash in zip(\n        inputs, input_hashes\n    ):\n        abs_output = str(Path(output_path).resolve())\n        encoded, byte_count = _atomic_write(abs_output, output_content)\n        output_sha256 = _sha256_bytes(encoded)\n        lineage.append({\n            \"input_path\": str(Path(input_path).resolve()),\n            \"input_source_hash\": input_source_hash,\n            \"output_path\": abs_output,\n            \"output_sha256\": output_sha256,\n            \"output_bytes\": byte_count,\n        })\n        files_written.append(abs_output)\n        bytes_total += byte_count\n\n    # Single git add + commit for the whole batch.\n    commit_sha = \"\"\n    if not skip_git:\n        if commit_message is None:\n            mission = audit_metadata.get(\"mission\", \"batch\")\n            commit_message = f\"pidgin/compress_write: {mission}\"\n        commit_sha = git_commit_files(\n            project_root_str,\n            files_written,\n            commit_message,\n        )\n\n    # One JSONL audit entry for the batch (mode=\"compress\", lineage= instead of files=).\n    audit_id = uuid.uuid4().hex\n    _append_compress_audit(\n        project_root_str,\n        audit_id,\n        lineage=lineage,\n        commit_sha=commit_sha,\n        audit_metadata=audit_metadata,\n    )\n\n    return ArtifactWriteResult(\n        files_written=files_written,\n        commit_sha=commit_sha,\n        audit_id=audit_id,\n        bytes_total=bytes_total,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Read input files, compute sha256, atomically write outputs, record lineage info, and make a git commit for all files.", "label": "negative", "reason": "", "function_id": "48c12750fb31605c", "centroid_distance": 0.14307944195936562}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:31.840545+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/new.py\" name=\"ArtifactWriteResult\" line_start=\"29\">\n[CDATA-OPEN]\n            files_written: list[str]   # absolute paths\n    commit_sha: str            # post-write commit SHA, \"\" if no git\n    audit_id: str              # audit-entry uuid (one per batch)\n    bytes_total: int\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return an ArtifactWriteResult object with a list of written file paths, the post-write git commit SHA, the audit ID, and the total bytes written.", "label": "positive", "reason": "", "function_id": "ad6d48ee4dbecad4", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:31.840762+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/new.py\" name=\"ArtifactWriteResult\" line_start=\"29\">\n[CDATA-OPEN]\n            files_written: list[str]   # absolute paths\n    commit_sha: str            # post-write commit SHA, \"\" if no git\n    audit_id: str              # audit-entry uuid (one per batch)\n    bytes_total: int\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "ArtifactWriteResult holds file paths, SHA, audit ID, and byte count.", "label": "negative", "reason": "", "function_id": "ad6d48ee4dbecad4", "centroid_distance": 0.14790400807344306}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:36.616170+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/write/new.py\" name=\"artifact_write\" line_start=\"97\">\n[CDATA-OPEN]\n            files: list[tuple[str, str]],       # [(absolute_path, content), ...]\n    audit_metadata: dict,               # caller-supplied tags (mission, verb, etc.)\n    *,\n    project_root: Path | None = None,   # PD1: auto-derive from .git if None\n    commit_message: str | None = None,  # default: derived from audit_metadata\n    skip_git: bool = False,             # for tests; default False\n) -> ArtifactWriteResult:\n    \"\"\"Write a batch of files, then produce one git commit and one audit entry.\n\n    Modes 3 semantics:\n    - No hash pre-verification.\n    - No pre-write commit.\n    - All files written first, then one `git add` + `git commit`.\n    - One JSONL audit line covering the entire batch.\n\n    PD1: If project_root is None, auto-derive by walking up from the first\n    file's directory to the nearest .git boundary.\n    \"\"\"\n    if not files:\n        raise ValueError(\"artifact_write: files list must not be empty\")\n\n    # PD1: auto-derive project_root\n    if project_root is None:\n        first_path = files[0][0]\n        derived = find_project_root(str(Path(first_path).parent))\n        if derived is None and not skip_git:\n            raise RuntimeError(\n                f\"artifact_write: could not find a .git directory above \"\n                f\"{Path(first_path).parent}. Pass project_root= explicitly \"\n                f\"or use skip_git=True.\"\n            )\n        project_root_str = derived or str(Path(first_path).parent)\n    else:\n        project_root_str = str(Path(project_root).resolve())\n\n    # Write all files atomically.\n    file_entries: list[dict] = []\n    files_written: list[str] = []\n    bytes_total = 0\n\n    for path, content in files:\n        abs_path = str(Path(path).resolve())\n        encoded, byte_count = _atomic_write(abs_path, content)\n        sha = _sha256_bytes(encoded)\n        file_entries.append({\n            \"path\": abs_path,\n            \"sha256\": sha,\n            \"bytes\": byte_count,\n        })\n        files_written.append(abs_path)\n        bytes_total += byte_count\n\n    # Single git add + commit for the whole batch.\n    commit_sha = \"\"\n    if not skip_git:\n        if commit_message is None:\n            mission = audit_metadata.get(\"mission\", \"batch\")\n            commit_message = f\"pidgin/artifact_write: {mission}\"\n        commit_sha = git_commit_files(\n            project_root_str,\n            files_written,\n            commit_message,\n        )\n\n    # One JSONL audit entry for the batch.\n    audit_id = uuid.uuid4().hex\n    _append_audit(\n        project_root_str,\n        audit_id,\n        mode=\"new\",\n        file_entries=file_entries,\n        commit_sha=commit_sha,\n        audit_metadata=audit_metadata,\n    )\n\n    return ArtifactWriteResult(\n        files_written=files_written,\n        commit_sha=commit_sha,\n        audit_id=audit_id,\n        bytes_total=bytes_total,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Write multiple files, then perform one git commit and audit entry.", "label": "positive", "reason": "", "function_id": "b59a332255f61171", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:45.624075+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"classify_batch_results\" line_start=\"80\">\n[CDATA-OPEN]\n            \"\"\"Inspect a list of batch results (GenerationResult or EmbeddingResult).\n    Return (verdict, first_error_message_or_None).\n\n    - ALL_ERRORED: every result has a non-None error field.\n    - PARTIAL:    some have errors, some are clean.\n    - CLEAN:      no errors.\n    \"\"\"\n    if not results:\n        return (\"CLEAN\", None)\n\n    errors = [getattr(r, \"error\", None) for r in results]\n    n_errored = sum(1 for e in errors if e is not None)\n    first_error = next((e for e in errors if e is not None), None)\n\n    if n_errored == len(results):\n        return (\"ALL_ERRORED\", first_error)\n    if n_errored > 0:\n        return (\"PARTIAL\", first_error)\n    return (\"CLEAN\", None)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Assess a list of results, determine if all, some, or none have errors, and return a verdict with the first error message if present.", "label": "positive", "reason": "", "function_id": "03322a89a90fff13", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:45.624307+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"classify_batch_results\" line_start=\"80\">\n[CDATA-OPEN]\n            \"\"\"Inspect a list of batch results (GenerationResult or EmbeddingResult).\n    Return (verdict, first_error_message_or_None).\n\n    - ALL_ERRORED: every result has a non-None error field.\n    - PARTIAL:    some have errors, some are clean.\n    - CLEAN:      no errors.\n    \"\"\"\n    if not results:\n        return (\"CLEAN\", None)\n\n    errors = [getattr(r, \"error\", None) for r in results]\n    n_errored = sum(1 for e in errors if e is not None)\n    first_error = next((e for e in errors if e is not None), None)\n\n    if n_errored == len(results):\n        return (\"ALL_ERRORED\", first_error)\n    if n_errored > 0:\n        return (\"PARTIAL\", first_error)\n    return (\"CLEAN\", None)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Check if results is empty, and return 'CLEAN' with None if so.", "label": "negative", "reason": "", "function_id": "03322a89a90fff13", "centroid_distance": 0.3848649902868515}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:51.453468+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"GenerationRequest\" line_start=\"107\">\n[CDATA-OPEN]\n            custom_id: str\n    cached_preamble: str\n    payload: str\n    model: str\n    max_tokens: int = 500\n    response_format: dict | None = None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define a GenerationRequest class with string fields custom_id, cached_preamble, payload, model, integer max_tokens default 500, and optional response_format.", "label": "positive", "reason": "", "function_id": "d20ac7a4b6f5ddb0", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:51.453637+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"GenerationRequest\" line_start=\"107\">\n[CDATA-OPEN]\n            custom_id: str\n    cached_preamble: str\n    payload: str\n    model: str\n    max_tokens: int = 500\n    response_format: dict | None = None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Implement GenerationRequest with string id, preamble, payload, model, default max_tokens 500, optional response_format dict.", "label": "negative", "reason": "", "function_id": "d20ac7a4b6f5ddb0", "centroid_distance": 0.11070598853594082}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:56.521654+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"GenerationResult\" line_start=\"117\">\n[CDATA-OPEN]\n            custom_id: str\n    drafts: list[str]\n    raw_response: dict\n    error: str | None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe the GenerationResult data structure with attributes: custom_id, drafts, raw_response, error.", "label": "positive", "reason": "", "function_id": "bda2267badf788b1", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:58:56.521774+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"GenerationResult\" line_start=\"117\">\n[CDATA-OPEN]\n            custom_id: str\n    drafts: list[str]\n    raw_response: dict\n    error: str | None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "GenerationResult holds ID, draft list, raw API response, and error info.", "label": "negative", "reason": "", "function_id": "bda2267badf788b1", "centroid_distance": 0.25897931664308227}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:04.123105+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"build_jsonl\" line_start=\"128\">\n[CDATA-OPEN]\n            \"\"\"Build a JSONL string where each line is one batch request object.\n\n    Each line: custom_id, method=POST, url=/v1/chat/completions, body with\n    model/messages/max_tokens (and optional response_format). System message\n    carries cached_preamble; user message carries payload.\n    \"\"\"\n    lines: list[str] = []\n    for req in requests:\n        body: dict = {\n            \"model\": req.model,\n            \"messages\": [\n                {\"role\": \"system\", \"content\": req.cached_preamble},\n                {\"role\": \"user\", \"content\": req.payload},\n            ],\n            \"max_tokens\": req.max_tokens,\n        }\n        if req.response_format is not None:\n            body[\"response_format\"] = req.response_format\n        line_obj = {\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/chat/completions\",\n            \"body\": body,\n        }\n        lines.append(json.dumps(line_obj))\n    return \"\\n\".join(lines)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Create a JSONL string with each line representing a batch request object containing method, URL, headers, and body.", "label": "positive", "reason": "", "function_id": "9887668d246b7761", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:04.123331+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"build_jsonl\" line_start=\"128\">\n[CDATA-OPEN]\n            \"\"\"Build a JSONL string where each line is one batch request object.\n\n    Each line: custom_id, method=POST, url=/v1/chat/completions, body with\n    model/messages/max_tokens (and optional response_format). System message\n    carries cached_preamble; user message carries payload.\n    \"\"\"\n    lines: list[str] = []\n    for req in requests:\n        body: dict = {\n            \"model\": req.model,\n            \"messages\": [\n                {\"role\": \"system\", \"content\": req.cached_preamble},\n                {\"role\": \"user\", \"content\": req.payload},\n            ],\n            \"max_tokens\": req.max_tokens,\n        }\n        if req.response_format is not None:\n            body[\"response_format\"] = req.response_format\n        line_obj = {\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/chat/completions\",\n            \"body\": body,\n        }\n        lines.append(json.dumps(line_obj))\n    return \"\\n\".join(lines)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Construct a JSONL string where each line is a batch request object with method POST, URL, custom ID, and message payload for chat completions.", "label": "negative", "reason": "", "function_id": "9887668d246b7761", "centroid_distance": 0.158820662680484}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:10.398145+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"should_use_batch\" line_start=\"161\">\n[CDATA-OPEN]\n            \"\"\"Return True if the batch API should be used for this run.\n\n    DP4: auto mode selects batch when n_tasks >= 10.\n    \"\"\"\n    if dispatch == \"sync\":\n        return False\n    if dispatch == \"batch\":\n        return True\n    return n_tasks >= 10  # DP4\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return True if n_tasks is at least 10 and dispatch is not 'sync' or 'batch' is chosen.", "label": "positive", "reason": "", "function_id": "08091efaf996e00a", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:10.398380+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"should_use_batch\" line_start=\"161\">\n[CDATA-OPEN]\n            \"\"\"Return True if the batch API should be used for this run.\n\n    DP4: auto mode selects batch when n_tasks >= 10.\n    \"\"\"\n    if dispatch == \"sync\":\n        return False\n    if dispatch == \"batch\":\n        return True\n    return n_tasks >= 10  # DP4\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Decide whether to use batching based on dispatch mode and task count.", "label": "negative", "reason": "", "function_id": "08091efaf996e00a", "centroid_distance": 0.1840532023144843}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:16.210874+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"parse_batch_results\" line_start=\"224\">\n[CDATA-OPEN]\n            output_jsonl: str,\n    error_jsonl: str | None,\n    requested_ids: list[str],\n) -> list[GenerationResult]:\n    \"\"\"Parse output and error JSONL from a completed batch into GenerationResults.\n\n    For each requested_id:\n      - If present in output with a successful response: extract drafts.\n      - If present in error file or response has an error body: error field set.\n      - If missing entirely: error field set to 'missing from batch output'.\n\n    Returns results in the same order as requested_ids.\n    \"\"\"\n    # Parse output JSONL into a dict keyed by custom_id.\n    output_by_id: dict[str, dict] = {}\n    if output_jsonl:\n        for line in output_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    output_by_id[cid] = obj\n            except Exception:\n                pass\n\n    # Parse error JSONL into a dict keyed by custom_id.\n    error_by_id: dict[str, str] = {}\n    if error_jsonl:\n        for line in error_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    # Extract human-readable error message.\n                    err_body = obj.get(\"error\") or obj.get(\"response\", {})\n                    error_by_id[cid] = json.dumps(err_body)\n            except Exception:\n                error_by_id[line[:40]] = \"unparseable error line\"\n\n    results: list[GenerationResult] = []\n    for cid in requested_ids:\n        if cid in error_by_id:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response={},\n                error=error_by_id[cid],\n            ))\n            continue\n\n        if cid not in output_by_id:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response={},\n                error=\"missing from batch output\",\n            ))\n            continue\n\n        raw = output_by_id[cid]\n        response = raw.get(\"response\", {})\n        status_code = response.get(\"status_code\", 200)\n        body = response.get(\"body\", {})\n\n        # Check for API-level error in the response body.\n        if status_code != 200 or \"error\" in body:\n            err_msg = json.dumps(body.get(\"error\", f\"status_code={status_code}\"))\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response=raw,\n                error=err_msg,\n            ))\n            continue\n\n        # Extract the content from the first choice.\n        choices = body.get(\"choices\", [])\n        if not choices:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response=raw,\n                error=\"no choices in response body\",\n            ))\n            continue\n\n        content = choices[0].get(\"message\", {}).get(\"content\", \"\") or \"\"\n        drafts = _extract_drafts(content)\n\n        results.append(GenerationResult(\n            custom_id=cid,\n            drafts=drafts,\n            raw_response=raw,\n            error=None,\n        ))\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse batch output JSONL, extract drafts or errors, and match requests to results.", "label": "positive", "reason": "", "function_id": "07c1aa76853d9ccd", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:16.211131+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"parse_batch_results\" line_start=\"224\">\n[CDATA-OPEN]\n            output_jsonl: str,\n    error_jsonl: str | None,\n    requested_ids: list[str],\n) -> list[GenerationResult]:\n    \"\"\"Parse output and error JSONL from a completed batch into GenerationResults.\n\n    For each requested_id:\n      - If present in output with a successful response: extract drafts.\n      - If present in error file or response has an error body: error field set.\n      - If missing entirely: error field set to 'missing from batch output'.\n\n    Returns results in the same order as requested_ids.\n    \"\"\"\n    # Parse output JSONL into a dict keyed by custom_id.\n    output_by_id: dict[str, dict] = {}\n    if output_jsonl:\n        for line in output_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    output_by_id[cid] = obj\n            except Exception:\n                pass\n\n    # Parse error JSONL into a dict keyed by custom_id.\n    error_by_id: dict[str, str] = {}\n    if error_jsonl:\n        for line in error_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    # Extract human-readable error message.\n                    err_body = obj.get(\"error\") or obj.get(\"response\", {})\n                    error_by_id[cid] = json.dumps(err_body)\n            except Exception:\n                error_by_id[line[:40]] = \"unparseable error line\"\n\n    results: list[GenerationResult] = []\n    for cid in requested_ids:\n        if cid in error_by_id:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response={},\n                error=error_by_id[cid],\n            ))\n            continue\n\n        if cid not in output_by_id:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response={},\n                error=\"missing from batch output\",\n            ))\n            continue\n\n        raw = output_by_id[cid]\n        response = raw.get(\"response\", {})\n        status_code = response.get(\"status_code\", 200)\n        body = response.get(\"body\", {})\n\n        # Check for API-level error in the response body.\n        if status_code != 200 or \"error\" in body:\n            err_msg = json.dumps(body.get(\"error\", f\"status_code={status_code}\"))\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response=raw,\n                error=err_msg,\n            ))\n            continue\n\n        # Extract the content from the first choice.\n        choices = body.get(\"choices\", [])\n        if not choices:\n            results.append(GenerationResult(\n                custom_id=cid,\n                drafts=[],\n                raw_response=raw,\n                error=\"no choices in response body\",\n            ))\n            continue\n\n        content = choices[0].get(\"message\", {}).get(\"content\", \"\") or \"\"\n        drafts = _extract_drafts(content)\n\n        results.append(GenerationResult(\n            custom_id=cid,\n            drafts=drafts,\n            raw_response=raw,\n            error=None,\n        ))\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Process each ID to identify success, error, or missing status, then extract drafts or set error messages.", "label": "negative", "reason": "", "function_id": "07c1aa76853d9ccd", "centroid_distance": 0.260780593811325}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:20.775533+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_batch_generation\" line_start=\"534\">\n[CDATA-OPEN]\n            requests: list[GenerationRequest],\n    *,\n    poll_interval_seconds: float = 30.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,\n) -> list[GenerationResult]:\n    \"\"\"Submit requests to the OpenAI Batch API via chunked-pipelined upload, poll until done, and parse results.\n\n    Steps:\n      1. Build raw request dicts from GenerationRequests.\n      2. Split into chunks of BATCH_CHUNK_SIZE via _chunk_requests.\n      3. Submit and poll via _submit_chunks_pipelined (max BATCH_MAX_CONCURRENT_CHUNKS active).\n      4. Merge raw result dicts across all chunks in input order.\n      5. Parse merged output JSONL via parse_batch_results in requested_ids order.\n\n    Returns results in the same order as the input requests list.\n    Per-chunk failures surface as error fields on the affected requests;\n    sibling chunks that have already completed are not aborted.\n    \"\"\"\n    import openai\n    import os\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = api_client or openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n    requested_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build raw request dicts (same shape as JSONL lines).\n    raw_requests: list[dict] = []\n    for req in requests:\n        body: dict = {\n            \"model\": req.model,\n            \"messages\": [\n                {\"role\": \"system\", \"content\": req.cached_preamble},\n                {\"role\": \"user\", \"content\": req.payload},\n            ],\n            \"max_tokens\": req.max_tokens,\n        }\n        if req.response_format is not None:\n            body[\"response_format\"] = req.response_format\n        raw_requests.append({\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/chat/completions\",\n            \"body\": body,\n        })\n\n    # Step 2: Split into chunks.\n    chunks = _chunk_requests(raw_requests)\n\n    # Step 3 & 4: Submit pipelined and collect merged raw result dicts.\n    merged_raw = _submit_chunks_pipelined(\n        client,\n        chunks,\n        endpoint=\"/v1/chat/completions\",\n        description=\"generation chunk i/N\",\n        poll_interval_s=poll_interval_seconds,\n        on_progress=_default_progress_printer,\n    )\n\n    # Step 5: Reconstruct output JSONL string from merged raw dicts and parse.\n    output_jsonl = \"\\n\".join(json.dumps(row) for row in merged_raw)\n    return parse_batch_results(output_jsonl, None, requested_ids)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Submits batched OpenAI chat requests, polls for completion, and merges results.", "label": "positive", "reason": "", "function_id": "a498c311b7fad24a", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:20.775781+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_batch_generation\" line_start=\"534\">\n[CDATA-OPEN]\n            requests: list[GenerationRequest],\n    *,\n    poll_interval_seconds: float = 30.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,\n) -> list[GenerationResult]:\n    \"\"\"Submit requests to the OpenAI Batch API via chunked-pipelined upload, poll until done, and parse results.\n\n    Steps:\n      1. Build raw request dicts from GenerationRequests.\n      2. Split into chunks of BATCH_CHUNK_SIZE via _chunk_requests.\n      3. Submit and poll via _submit_chunks_pipelined (max BATCH_MAX_CONCURRENT_CHUNKS active).\n      4. Merge raw result dicts across all chunks in input order.\n      5. Parse merged output JSONL via parse_batch_results in requested_ids order.\n\n    Returns results in the same order as the input requests list.\n    Per-chunk failures surface as error fields on the affected requests;\n    sibling chunks that have already completed are not aborted.\n    \"\"\"\n    import openai\n    import os\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = api_client or openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n    requested_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build raw request dicts (same shape as JSONL lines).\n    raw_requests: list[dict] = []\n    for req in requests:\n        body: dict = {\n            \"model\": req.model,\n            \"messages\": [\n                {\"role\": \"system\", \"content\": req.cached_preamble},\n                {\"role\": \"user\", \"content\": req.payload},\n            ],\n            \"max_tokens\": req.max_tokens,\n        }\n        if req.response_format is not None:\n            body[\"response_format\"] = req.response_format\n        raw_requests.append({\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/chat/completions\",\n            \"body\": body,\n        })\n\n    # Step 2: Split into chunks.\n    chunks = _chunk_requests(raw_requests)\n\n    # Step 3 & 4: Submit pipelined and collect merged raw result dicts.\n    merged_raw = _submit_chunks_pipelined(\n        client,\n        chunks,\n        endpoint=\"/v1/chat/completions\",\n        description=\"generation chunk i/N\",\n        poll_interval_s=poll_interval_seconds,\n        on_progress=_default_progress_printer,\n    )\n\n    # Step 5: Reconstruct output JSONL string from merged raw dicts and parse.\n    output_jsonl = \"\\n\".join(json.dumps(row) for row in merged_raw)\n    return parse_batch_results(output_jsonl, None, requested_ids)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Build request dicts from GenerationRequests, split into chunks, submit chunks pipelined, poll for completion, then parse results.", "label": "negative", "reason": "", "function_id": "a498c311b7fad24a", "centroid_distance": 0.19949262470409956}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:24.753162+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"EmbeddingRequest\" line_start=\"604\">\n[CDATA-OPEN]\n            custom_id: str   # e.g. \"embed_for_store:call=2:cand=1\"\n    text: str\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Construct an EmbeddingRequest with custom_id and text fields.", "label": "positive", "reason": "", "function_id": "6e9fa9f5fef972bf", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:24.753272+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"EmbeddingRequest\" line_start=\"604\">\n[CDATA-OPEN]\n            custom_id: str   # e.g. \"embed_for_store:call=2:cand=1\"\n    text: str\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Build EmbeddingRequest with ID and text for embedding.", "label": "negative", "reason": "", "function_id": "6e9fa9f5fef972bf", "centroid_distance": 0.13692024209131548}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:30.428957+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"EmbeddingResult\" line_start=\"610\">\n[CDATA-OPEN]\n            custom_id: str\n    vector: \"list[float] | None\"   # length EMBEDDING_DIM, or None on error\n    error: \"str | None\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define the EmbeddingResult dataclass with fields custom_id, vector, and error.", "label": "positive", "reason": "", "function_id": "12f5dcfb9eb6b082", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:30.429207+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"EmbeddingResult\" line_start=\"610\">\n[CDATA-OPEN]\n            custom_id: str\n    vector: \"list[float] | None\"   # length EMBEDDING_DIM, or None on error\n    error: \"str | None\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Declare EmbeddingResult with ID, optional float list, and optional error string.", "label": "negative", "reason": "", "function_id": "12f5dcfb9eb6b082", "centroid_distance": 0.13703691657277917}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:34.880210+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"build_embedding_jsonl\" line_start=\"620\">\n[CDATA-OPEN]\n            \"\"\"Build a JSONL string where each line is one embedding batch request.\n\n    Each line: custom_id, method=POST, url=/v1/embeddings, body with\n    model=text-embedding-3-small, dimensions=256, encoding_format=float.\n    \"\"\"\n    from pidgin.utils.api import EMBEDDING_MODEL, EMBEDDING_DIM\n\n    lines: list[str] = []\n    for req in requests:\n        line_obj = {\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/embeddings\",\n            \"body\": {\n                \"model\": EMBEDDING_MODEL,\n                \"input\": req.text,\n                \"dimensions\": EMBEDDING_DIM,\n                \"encoding_format\": \"float\",\n            },\n        }\n        lines.append(json.dumps(line_obj))\n    return \"\\n\".join(lines)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Create a JSONL string with lines, each representing an embedding request with custom_id, POST method, URL, and body containing model, input text, dimensions, and encoding format.", "label": "positive", "reason": "", "function_id": "e1becc3d802d0ab3", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:34.880401+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"build_embedding_jsonl\" line_start=\"620\">\n[CDATA-OPEN]\n            \"\"\"Build a JSONL string where each line is one embedding batch request.\n\n    Each line: custom_id, method=POST, url=/v1/embeddings, body with\n    model=text-embedding-3-small, dimensions=256, encoding_format=float.\n    \"\"\"\n    from pidgin.utils.api import EMBEDDING_MODEL, EMBEDDING_DIM\n\n    lines: list[str] = []\n    for req in requests:\n        line_obj = {\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/embeddings\",\n            \"body\": {\n                \"model\": EMBEDDING_MODEL,\n                \"input\": req.text,\n                \"dimensions\": EMBEDDING_DIM,\n                \"encoding_format\": \"float\",\n            },\n        }\n        lines.append(json.dumps(line_obj))\n    return \"\\n\".join(lines)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Create a JSONL string with each line representing an embedding request for the /v1/embeddings endpoint with specified model, dimensions, and float encoding.", "label": "negative", "reason": "", "function_id": "e1becc3d802d0ab3", "centroid_distance": 0.09308963589789465}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:39.854654+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"parse_embedding_results\" line_start=\"649\">\n[CDATA-OPEN]\n            output_jsonl: str,\n    error_jsonl: \"str | None\",\n    requested_ids: list[str],\n) -> \"list[EmbeddingResult]\":\n    \"\"\"Parse output and error JSONL from a completed embedding batch.\n\n    Four response shapes handled per body:\n      1. {\"data\":[{\"embedding\":[...]}]} \u2192 use data[0].embedding\n      2. {\"embedding\":[...]}            \u2192 use .embedding\n      3. [...]                          \u2192 bare vector list used as-is\n      4. {\"vector\": [...]}              \u2192 use .vector\n\n    If the returned vector dimension != EMBEDDING_DIM: error=\"wrong_dim:<N>\".\n    Returns results in the same order as requested_ids.\n    \"\"\"\n    from pidgin.utils.api import EMBEDDING_DIM\n\n    output_by_id: dict[str, dict] = {}\n    if output_jsonl:\n        for line in output_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    output_by_id[cid] = obj\n            except Exception:\n                pass\n\n    error_by_id: dict[str, str] = {}\n    if error_jsonl:\n        for line in error_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    err_body = obj.get(\"error\") or obj.get(\"response\", {})\n                    error_by_id[cid] = json.dumps(err_body)\n            except Exception:\n                error_by_id[line[:40]] = \"unparseable error line\"\n\n    results: list[EmbeddingResult] = []\n    for cid in requested_ids:\n        if cid in error_by_id:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=error_by_id[cid]))\n            continue\n\n        if cid not in output_by_id:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"missing from batch output\"))\n            continue\n\n        raw = output_by_id[cid]\n        response = raw.get(\"response\", {})\n        status_code = response.get(\"status_code\", 200)\n        body = response.get(\"body\", {})\n\n        if status_code != 200 or \"error\" in body:\n            err_msg = json.dumps(body.get(\"error\", f\"status_code={status_code}\"))\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=err_msg))\n            continue\n\n        # Four-shape fallback to extract the vector.\n        vector = None\n\n        # Shape 1: {\"data\":[{\"embedding\":[...]}]}\n        if isinstance(body, dict) and \"data\" in body:\n            data_list = body[\"data\"]\n            if isinstance(data_list, list) and data_list:\n                first = data_list[0]\n                if isinstance(first, dict) and \"embedding\" in first:\n                    vector = first[\"embedding\"]\n\n        # Shape 2: {\"embedding\":[...]}\n        if vector is None and isinstance(body, dict) and \"embedding\" in body:\n            vector = body[\"embedding\"]\n\n        # Shape 3: bare vector list\n        if vector is None and isinstance(body, list):\n            vector = body\n\n        # Shape 4: {\"vector\":[...]}\n        if vector is None and isinstance(body, dict) and \"vector\" in body:\n            vector = body[\"vector\"]\n\n        if vector is None:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"no embedding found in response body\"))\n            continue\n\n        if not isinstance(vector, list):\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"embedding is not a list\"))\n            continue\n\n        if len(vector) != EMBEDDING_DIM:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=f\"wrong_dim:{len(vector)}\"))\n            continue\n\n        results.append(EmbeddingResult(custom_id=cid, vector=vector, error=None))\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse JSON lines, match IDs, handle errors, extract vectors, check dimensions, return results.", "label": "positive", "reason": "", "function_id": "acbe1f125c93af13", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:39.854810+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"parse_embedding_results\" line_start=\"649\">\n[CDATA-OPEN]\n            output_jsonl: str,\n    error_jsonl: \"str | None\",\n    requested_ids: list[str],\n) -> \"list[EmbeddingResult]\":\n    \"\"\"Parse output and error JSONL from a completed embedding batch.\n\n    Four response shapes handled per body:\n      1. {\"data\":[{\"embedding\":[...]}]} \u2192 use data[0].embedding\n      2. {\"embedding\":[...]}            \u2192 use .embedding\n      3. [...]                          \u2192 bare vector list used as-is\n      4. {\"vector\": [...]}              \u2192 use .vector\n\n    If the returned vector dimension != EMBEDDING_DIM: error=\"wrong_dim:<N>\".\n    Returns results in the same order as requested_ids.\n    \"\"\"\n    from pidgin.utils.api import EMBEDDING_DIM\n\n    output_by_id: dict[str, dict] = {}\n    if output_jsonl:\n        for line in output_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    output_by_id[cid] = obj\n            except Exception:\n                pass\n\n    error_by_id: dict[str, str] = {}\n    if error_jsonl:\n        for line in error_jsonl.strip().splitlines():\n            line = line.strip()\n            if not line:\n                continue\n            try:\n                obj = json.loads(line)\n                cid = obj.get(\"custom_id\", \"\")\n                if cid:\n                    err_body = obj.get(\"error\") or obj.get(\"response\", {})\n                    error_by_id[cid] = json.dumps(err_body)\n            except Exception:\n                error_by_id[line[:40]] = \"unparseable error line\"\n\n    results: list[EmbeddingResult] = []\n    for cid in requested_ids:\n        if cid in error_by_id:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=error_by_id[cid]))\n            continue\n\n        if cid not in output_by_id:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"missing from batch output\"))\n            continue\n\n        raw = output_by_id[cid]\n        response = raw.get(\"response\", {})\n        status_code = response.get(\"status_code\", 200)\n        body = response.get(\"body\", {})\n\n        if status_code != 200 or \"error\" in body:\n            err_msg = json.dumps(body.get(\"error\", f\"status_code={status_code}\"))\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=err_msg))\n            continue\n\n        # Four-shape fallback to extract the vector.\n        vector = None\n\n        # Shape 1: {\"data\":[{\"embedding\":[...]}]}\n        if isinstance(body, dict) and \"data\" in body:\n            data_list = body[\"data\"]\n            if isinstance(data_list, list) and data_list:\n                first = data_list[0]\n                if isinstance(first, dict) and \"embedding\" in first:\n                    vector = first[\"embedding\"]\n\n        # Shape 2: {\"embedding\":[...]}\n        if vector is None and isinstance(body, dict) and \"embedding\" in body:\n            vector = body[\"embedding\"]\n\n        # Shape 3: bare vector list\n        if vector is None and isinstance(body, list):\n            vector = body\n\n        # Shape 4: {\"vector\":[...]}\n        if vector is None and isinstance(body, dict) and \"vector\" in body:\n            vector = body[\"vector\"]\n\n        if vector is None:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"no embedding found in response body\"))\n            continue\n\n        if not isinstance(vector, list):\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=\"embedding is not a list\"))\n            continue\n\n        if len(vector) != EMBEDDING_DIM:\n            results.append(EmbeddingResult(custom_id=cid, vector=None, error=f\"wrong_dim:{len(vector)}\"))\n            continue\n\n        results.append(EmbeddingResult(custom_id=cid, vector=vector, error=None))\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Read output and error JSONL, map by ID, verify response status, extract embedding arrays, validate length, return EmbeddingResults.", "label": "negative", "reason": "", "function_id": "acbe1f125c93af13", "centroid_distance": 0.18750320225079486}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:48.172910+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_batch_embedding\" line_start=\"760\">\n[CDATA-OPEN]\n            requests: list[EmbeddingRequest],\n    *,\n    poll_interval_seconds: float = 30.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,\n) -> \"list[EmbeddingResult]\":\n    \"\"\"Submit embedding requests to the OpenAI Batch API via chunked-pipelined upload, poll until done, and parse.\n\n    Steps:\n      1. Build raw request dicts from EmbeddingRequests.\n      2. Split into chunks of BATCH_CHUNK_SIZE via _chunk_requests.\n      3. Submit and poll via _submit_chunks_pipelined (max BATCH_MAX_CONCURRENT_CHUNKS active).\n      4. Merge raw result dicts across all chunks in input order.\n      5. Parse merged output JSONL via parse_embedding_results in requested_ids order.\n\n    Returns results in the same order as the input requests list.\n    Per-chunk failures surface as error fields on the affected requests;\n    sibling chunks that have already completed are not aborted.\n    \"\"\"\n    import openai\n    import os\n\n    from pidgin.utils.api import EMBEDDING_MODEL, EMBEDDING_DIM\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = api_client or openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n    requested_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build raw request dicts (same shape as JSONL lines).\n    raw_requests: list[dict] = []\n    for req in requests:\n        raw_requests.append({\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/embeddings\",\n            \"body\": {\n                \"model\": EMBEDDING_MODEL,\n                \"input\": req.text,\n                \"dimensions\": EMBEDDING_DIM,\n                \"encoding_format\": \"float\",\n            },\n        })\n\n    # Step 2: Split into chunks.\n    chunks = _chunk_requests(raw_requests)\n\n    # Step 3 & 4: Submit pipelined and collect merged raw result dicts.\n    merged_raw = _submit_chunks_pipelined(\n        client,\n        chunks,\n        endpoint=\"/v1/embeddings\",\n        description=\"embedding chunk i/N\",\n        poll_interval_s=poll_interval_seconds,\n        on_progress=_default_progress_printer,\n    )\n\n    # Step 5: Reconstruct output JSONL string from merged raw dicts and parse.\n    output_jsonl = \"\\n\".join(json.dumps(row) for row in merged_raw)\n    return parse_embedding_results(output_jsonl, None, requested_ids)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Call the OpenAI API with chunked requests, poll for completion, then parse results.", "label": "positive", "reason": "", "function_id": "e2cacf27072fe09b", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:48.173061+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_batch_embedding\" line_start=\"760\">\n[CDATA-OPEN]\n            requests: list[EmbeddingRequest],\n    *,\n    poll_interval_seconds: float = 30.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,\n) -> \"list[EmbeddingResult]\":\n    \"\"\"Submit embedding requests to the OpenAI Batch API via chunked-pipelined upload, poll until done, and parse.\n\n    Steps:\n      1. Build raw request dicts from EmbeddingRequests.\n      2. Split into chunks of BATCH_CHUNK_SIZE via _chunk_requests.\n      3. Submit and poll via _submit_chunks_pipelined (max BATCH_MAX_CONCURRENT_CHUNKS active).\n      4. Merge raw result dicts across all chunks in input order.\n      5. Parse merged output JSONL via parse_embedding_results in requested_ids order.\n\n    Returns results in the same order as the input requests list.\n    Per-chunk failures surface as error fields on the affected requests;\n    sibling chunks that have already completed are not aborted.\n    \"\"\"\n    import openai\n    import os\n\n    from pidgin.utils.api import EMBEDDING_MODEL, EMBEDDING_DIM\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = api_client or openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n    requested_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build raw request dicts (same shape as JSONL lines).\n    raw_requests: list[dict] = []\n    for req in requests:\n        raw_requests.append({\n            \"custom_id\": req.custom_id,\n            \"method\": \"POST\",\n            \"url\": \"/v1/embeddings\",\n            \"body\": {\n                \"model\": EMBEDDING_MODEL,\n                \"input\": req.text,\n                \"dimensions\": EMBEDDING_DIM,\n                \"encoding_format\": \"float\",\n            },\n        })\n\n    # Step 2: Split into chunks.\n    chunks = _chunk_requests(raw_requests)\n\n    # Step 3 & 4: Submit pipelined and collect merged raw result dicts.\n    merged_raw = _submit_chunks_pipelined(\n        client,\n        chunks,\n        endpoint=\"/v1/embeddings\",\n        description=\"embedding chunk i/N\",\n        poll_interval_s=poll_interval_seconds,\n        on_progress=_default_progress_printer,\n    )\n\n    # Step 5: Reconstruct output JSONL string from merged raw dicts and parse.\n    output_jsonl = \"\\n\".join(json.dumps(row) for row in merged_raw)\n    return parse_embedding_results(output_jsonl, None, requested_ids)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Builds request dicts from requests, chunks and pipelines submissions, then merges and parses results.", "label": "negative", "reason": "", "function_id": "e2cacf27072fe09b", "centroid_distance": 0.1926582485664028}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:54.223678+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"FunctionMldResult\" line_start=\"827\">\n[CDATA-OPEN]\n            name: str\n    candidates: list[str]                   # up to ADAPTIVE_MAX_BATCHES * 3 candidate descriptions\n    vectors: \"list[list[float] | None]\"     # parallel to candidates\n    cluster_size: int\n    converged: bool                          # True when convergence_verdict == \"CONVERGED\"\n    first_error: \"str | None\" = None        # set when all gen results for this function errored\n    per_draft_centroid: \"dict[int, list[float]]\" = field(default_factory=dict)\n    per_draft_tightness: \"dict[int, float]\" = field(default_factory=dict)\n    per_draft_n: \"dict[int, int]\" = field(default_factory=dict)\n    # M14 \u2014 adaptive convergence-stop instrumentation.\n    n_batches_run: int = 0\n    final_convergence_delta: \"float | None\" = None\n    # M18 \u2014 SFT provenance: the MLD prompt assembled for this function.\n    prompt_used: str = \"\"\n    # Convergence verdict fields (convergence_verdict() in geometry.py).\n    convergence_verdict: str = \"AMBIGUOUS\"   # \"CONVERGED\" | \"DIVERGENT\" | \"AMBIGUOUS\"\n    sv1: \"float | None\" = None               # spherical variance for draft_1 tier\n    sv2: \"float | None\" = None               # spherical variance for draft_2 tier\n    sv3: \"float | None\" = None               # spherical variance for draft_3 tier\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe the FunctionMldResult structure and its purpose.", "label": "positive", "reason": "", "function_id": "9a404a3565b360e5", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T22:59:54.223939+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"FunctionMldResult\" line_start=\"827\">\n[CDATA-OPEN]\n            name: str\n    candidates: list[str]                   # up to ADAPTIVE_MAX_BATCHES * 3 candidate descriptions\n    vectors: \"list[list[float] | None]\"     # parallel to candidates\n    cluster_size: int\n    converged: bool                          # True when convergence_verdict == \"CONVERGED\"\n    first_error: \"str | None\" = None        # set when all gen results for this function errored\n    per_draft_centroid: \"dict[int, list[float]]\" = field(default_factory=dict)\n    per_draft_tightness: \"dict[int, float]\" = field(default_factory=dict)\n    per_draft_n: \"dict[int, int]\" = field(default_factory=dict)\n    # M14 \u2014 adaptive convergence-stop instrumentation.\n    n_batches_run: int = 0\n    final_convergence_delta: \"float | None\" = None\n    # M18 \u2014 SFT provenance: the MLD prompt assembled for this function.\n    prompt_used: str = \"\"\n    # Convergence verdict fields (convergence_verdict() in geometry.py).\n    convergence_verdict: str = \"AMBIGUOUS\"   # \"CONVERGED\" | \"DIVERGENT\" | \"AMBIGUOUS\"\n    sv1: \"float | None\" = None               # spherical variance for draft_1 tier\n    sv2: \"float | None\" = None               # spherical variance for draft_2 tier\n    sv3: \"float | None\" = None               # spherical variance for draft_3 tier\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Outline the fields and their roles in representing ML-D convergence results.", "label": "negative", "reason": "", "function_id": "9a404a3565b360e5", "centroid_distance": 0.24806138053668148}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:00.325681+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_embedding_mld_pipeline_for_functions\" line_start=\"1011\">\n[CDATA-OPEN]\n            functions: list[dict],\n    *,\n    converge_template: str = str(\n        __import__(\"pathlib\").Path(__file__).parent.parent / \"prompts\" / \"converge.xml\"\n    ),\n    max_batches: int = ADAPTIVE_MAX_BATCHES,\n    convergence_threshold: float = ADAPTIVE_CONVERGENCE_THRESHOLD,\n    dispatch: \"Literal['auto','sync','batch']\" = \"auto\",\n    verbose: bool = False,\n    n_calls: \"int | None\" = None,\n    on_chunk_complete: \"Callable[[list[FunctionMldResult], list[dict]], None] | None\" = None,\n) -> \"list[FunctionMldResult]\":\n    \"\"\"Adaptive generation + embedding MLD pipeline.\n\n    Per-function loop runs at least ADAPTIVE_INITIAL_BATCHES batches, then\n    stops early when consecutive centroids cluster within\n    `convergence_threshold` cosine similarity. Caps at `max_batches`. Each\n    \"batch\" is a single API call producing 3 drafts (the existing\n    GenerationRequest unit).\n\n    Dispatch paths:\n      - sync: per-function adaptive loop. One API call at a time, embed\n        the new drafts, recompute centroid, decide whether to stop. This is\n        where convergence-stop saves token cost.\n      - batch: delegates to run_incremental_mld_pipeline for per-chunk upsert\n        resilience (M1). Processes one BATCH_CHUNK_SIZE chunk at a time,\n        fires on_chunk_complete after each chunk completes.\n      - auto: routes via should_use_batch (DP4: n_tasks >= 10 \u2192 batch).\n\n    on_chunk_complete(chunk_results, chunk_functions) is called after each\n    chunk completes (batch path) or after each function completes (sync path).\n    Defaults to None for backward compatibility.\n\n    Each function dict must have keys: name, file, body, line_start.\n\n    Backward compatibility: the legacy `n_calls` keyword maps to\n    `max_batches` and emits a one-time DeprecationWarning.\n    \"\"\"\n    import sys\n    import warnings\n\n    from pidgin.pipeline.geometry import cosine_similarity, centroid as _centroid\n\n    # -----------------------------------------------------------------------\n    # Backwards-compat: n_calls \u2192 max_batches\n    # -----------------------------------------------------------------------\n    if n_calls is not None:\n        global _N_CALLS_DEPRECATION_WARNED\n        if not _N_CALLS_DEPRECATION_WARNED:\n            warnings.warn(\n                \"run_embedding_mld_pipeline_for_functions(n_calls=...) is \"\n                \"deprecated; use max_batches=... instead. Mapping n_calls \"\n                \"to max_batches for this call.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            _N_CALLS_DEPRECATION_WARNED = True\n        max_batches = n_calls\n\n    n_funcs = len(functions)\n    n_total_gen_requests = n_funcs * max_batches\n    use_batch_gen = should_use_batch(n_total_gen_requests, dispatch)\n\n    # =======================================================================\n    # SYNC PATH \u2014 per-function adaptive convergence loop with early stop.\n    # =======================================================================\n    if not use_batch_gen:\n        return _run_pipeline_sync_adaptive(\n            functions=functions,\n            converge_template=converge_template,\n            max_batches=max_batches,\n            convergence_threshold=convergence_threshold,\n            dispatch=dispatch,\n            verbose=verbose,\n            on_chunk_complete=on_chunk_complete,\n        )\n\n    # =======================================================================\n    # BATCH PATH \u2014 delegate to run_incremental_mld_pipeline for per-chunk\n    # upsert resilience (M1).\n    # =======================================================================\n    return run_incremental_mld_pipeline(\n        functions,\n        converge_template=converge_template,\n        max_batches=max_batches,\n        convergence_threshold=convergence_threshold,\n        dispatch=dispatch,\n        verbose=verbose,\n        on_chunk_complete=on_chunk_complete,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Run adaptive generation and embedding pipeline for functions with early convergence stopping.", "label": "positive", "reason": "", "function_id": "24e1404c7575dccb", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:00.325899+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_embedding_mld_pipeline_for_functions\" line_start=\"1011\">\n[CDATA-OPEN]\n            functions: list[dict],\n    *,\n    converge_template: str = str(\n        __import__(\"pathlib\").Path(__file__).parent.parent / \"prompts\" / \"converge.xml\"\n    ),\n    max_batches: int = ADAPTIVE_MAX_BATCHES,\n    convergence_threshold: float = ADAPTIVE_CONVERGENCE_THRESHOLD,\n    dispatch: \"Literal['auto','sync','batch']\" = \"auto\",\n    verbose: bool = False,\n    n_calls: \"int | None\" = None,\n    on_chunk_complete: \"Callable[[list[FunctionMldResult], list[dict]], None] | None\" = None,\n) -> \"list[FunctionMldResult]\":\n    \"\"\"Adaptive generation + embedding MLD pipeline.\n\n    Per-function loop runs at least ADAPTIVE_INITIAL_BATCHES batches, then\n    stops early when consecutive centroids cluster within\n    `convergence_threshold` cosine similarity. Caps at `max_batches`. Each\n    \"batch\" is a single API call producing 3 drafts (the existing\n    GenerationRequest unit).\n\n    Dispatch paths:\n      - sync: per-function adaptive loop. One API call at a time, embed\n        the new drafts, recompute centroid, decide whether to stop. This is\n        where convergence-stop saves token cost.\n      - batch: delegates to run_incremental_mld_pipeline for per-chunk upsert\n        resilience (M1). Processes one BATCH_CHUNK_SIZE chunk at a time,\n        fires on_chunk_complete after each chunk completes.\n      - auto: routes via should_use_batch (DP4: n_tasks >= 10 \u2192 batch).\n\n    on_chunk_complete(chunk_results, chunk_functions) is called after each\n    chunk completes (batch path) or after each function completes (sync path).\n    Defaults to None for backward compatibility.\n\n    Each function dict must have keys: name, file, body, line_start.\n\n    Backward compatibility: the legacy `n_calls` keyword maps to\n    `max_batches` and emits a one-time DeprecationWarning.\n    \"\"\"\n    import sys\n    import warnings\n\n    from pidgin.pipeline.geometry import cosine_similarity, centroid as _centroid\n\n    # -----------------------------------------------------------------------\n    # Backwards-compat: n_calls \u2192 max_batches\n    # -----------------------------------------------------------------------\n    if n_calls is not None:\n        global _N_CALLS_DEPRECATION_WARNED\n        if not _N_CALLS_DEPRECATION_WARNED:\n            warnings.warn(\n                \"run_embedding_mld_pipeline_for_functions(n_calls=...) is \"\n                \"deprecated; use max_batches=... instead. Mapping n_calls \"\n                \"to max_batches for this call.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            _N_CALLS_DEPRECATION_WARNED = True\n        max_batches = n_calls\n\n    n_funcs = len(functions)\n    n_total_gen_requests = n_funcs * max_batches\n    use_batch_gen = should_use_batch(n_total_gen_requests, dispatch)\n\n    # =======================================================================\n    # SYNC PATH \u2014 per-function adaptive convergence loop with early stop.\n    # =======================================================================\n    if not use_batch_gen:\n        return _run_pipeline_sync_adaptive(\n            functions=functions,\n            converge_template=converge_template,\n            max_batches=max_batches,\n            convergence_threshold=convergence_threshold,\n            dispatch=dispatch,\n            verbose=verbose,\n            on_chunk_complete=on_chunk_complete,\n        )\n\n    # =======================================================================\n    # BATCH PATH \u2014 delegate to run_incremental_mld_pipeline for per-chunk\n    # upsert resilience (M1).\n    # =======================================================================\n    return run_incremental_mld_pipeline(\n        functions,\n        converge_template=converge_template,\n        max_batches=max_batches,\n        convergence_threshold=convergence_threshold,\n        dispatch=dispatch,\n        verbose=verbose,\n        on_chunk_complete=on_chunk_complete,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Execute adaptive generation and embedding for functions, stopping when centroids are within the threshold.", "label": "negative", "reason": "", "function_id": "24e1404c7575dccb", "centroid_distance": 0.3107893650200638}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:06.018084+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"AnthropicBatchRequest\" line_start=\"1274\">\n[CDATA-OPEN]\n            custom_id: str\n    cached_preamble: str    # placed in system block with cache_control: ephemeral\n    payload: str            # placed in user message\n    model: str              # \"claude-haiku-4-5-20251001\" for gate calls\n    max_tokens: int = 256\n    response_schema: dict | None = None  # if set, use tool-call structured output\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define the AnthropicBatchRequest class with attributes for custom ID, cached preamble, payload, model, max tokens, and response schema.", "label": "positive", "reason": "", "function_id": "24135eecb741db0e", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:06.018193+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"AnthropicBatchRequest\" line_start=\"1274\">\n[CDATA-OPEN]\n            custom_id: str\n    cached_preamble: str    # placed in system block with cache_control: ephemeral\n    payload: str            # placed in user message\n    model: str              # \"claude-haiku-4-5-20251001\" for gate calls\n    max_tokens: int = 256\n    response_schema: dict | None = None  # if set, use tool-call structured output\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Implement AnthropicBatchRequest with fields for ID, preamble, payload, model, max tokens, and optional schema for API requests.", "label": "negative", "reason": "", "function_id": "24135eecb741db0e", "centroid_distance": 0.07800476562767278}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:10.670646+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"AnthropicBatchResult\" line_start=\"1284\">\n[CDATA-OPEN]\n            custom_id: str\n    text: str | None        # model's response text (None when tool_use path)\n    parsed: dict | None     # if response_schema set, the parsed JSON from tool input\n    raw_response: dict\n    error: str | None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define the AnthropicBatchResult data class with custom_id, text, parsed, raw_response, and error fields.", "label": "positive", "reason": "", "function_id": "e22b2c3d5f568290", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:10.670830+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"AnthropicBatchResult\" line_start=\"1284\">\n[CDATA-OPEN]\n            custom_id: str\n    text: str | None        # model's response text (None when tool_use path)\n    parsed: dict | None     # if response_schema set, the parsed JSON from tool input\n    raw_response: dict\n    error: str | None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Specify AnthropicBatchResult fields: ID, text, parsed JSON, raw response, error.", "label": "negative", "reason": "", "function_id": "e22b2c3d5f568290", "centroid_distance": 0.09178964770656661}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:15.920424+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_anthropic_batch\" line_start=\"1296\">\n[CDATA-OPEN]\n            requests: list[AnthropicBatchRequest],\n    *,\n    poll_interval_seconds: float = 5.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,  # default: anthropic.Anthropic() from env\n) -> list[AnthropicBatchResult]:\n    \"\"\"Submit requests to the Anthropic Batches API, poll until ended, and parse results.\n\n    Steps:\n      1. Build the requests list for client.messages.batches.create().\n         System block carries cache_control: ephemeral for preamble caching.\n         If response_schema is set, inject a gate_verdict tool + tool_choice\n         to enforce structured output via Anthropic tool use.\n      2. Submit via client.messages.batches.create(requests=batch_requests).\n      3. Poll client.messages.batches.retrieve(batch.id) until\n         processing_status in {\"ended\", \"canceling\", \"expired\"}.\n      4. Stream results via client.messages.batches.results(batch.id).\n      5. For each result: extract text from the first content block, or parse\n         the tool_use block input as `parsed` when response_schema was set.\n\n    Returns results in the same order as the input requests list.\n    \"\"\"\n    import anthropic\n    import os\n\n    ensure_api_key(\"ANTHROPIC_API_KEY\")\n    client = api_client or anthropic.Anthropic(api_key=os.environ.get(\"ANTHROPIC_API_KEY\"))\n    ordered_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build the Anthropic batch requests list.\n    batch_requests = []\n    for req in requests:\n        params: dict = {\n            \"model\": req.model,\n            \"max_tokens\": req.max_tokens,\n            \"system\": [\n                {\n                    \"type\": \"text\",\n                    \"text\": req.cached_preamble,\n                    \"cache_control\": {\"type\": \"ephemeral\"},\n                }\n            ],\n            \"messages\": [{\"role\": \"user\", \"content\": req.payload}],\n        }\n        if req.response_schema is not None:\n            params[\"tools\"] = [\n                {\n                    \"name\": \"gate_verdict\",\n                    \"description\": \"Return the gate verdict\",\n                    \"input_schema\": req.response_schema,\n                }\n            ]\n            params[\"tool_choice\"] = {\"type\": \"tool\", \"name\": \"gate_verdict\"}\n\n        batch_requests.append({\n            \"custom_id\": req.custom_id,\n            \"params\": params,\n        })\n\n    # Step 2: Submit batch.\n    batch = client.messages.batches.create(requests=batch_requests)\n    batch_id = batch.id\n\n    # Step 3: Poll until terminal status.\n    terminal_statuses = {\"ended\", \"canceling\", \"expired\"}\n    deadline = time.monotonic() + timeout_seconds\n    status = batch.processing_status\n\n    while status not in terminal_statuses:\n        if time.monotonic() > deadline:\n            raise TimeoutError(\n                f\"Anthropic batch {batch_id} did not reach terminal status within \"\n                f\"{timeout_seconds}s. Last status: {status}\"\n            )\n        time.sleep(poll_interval_seconds)\n        batch = client.messages.batches.retrieve(batch_id)\n        status = batch.processing_status\n\n    # Step 4 & 5: Stream results and parse.\n    results_by_id: dict[str, AnthropicBatchResult] = {}\n\n    for result_item in client.messages.batches.results(batch_id):\n        cid = result_item.custom_id\n        result = result_item.result\n\n        # Check for error result types.\n        result_type = getattr(result, \"type\", None)\n\n        if result_type != \"succeeded\":\n            error_msg = f\"result type: {result_type}\"\n            if hasattr(result, \"error\"):\n                err = result.error\n                error_msg = getattr(err, \"message\", None) or str(err)\n            results_by_id[cid] = AnthropicBatchResult(\n                custom_id=cid,\n                text=None,\n                parsed=None,\n                raw_response={},\n                error=error_msg,\n            )\n            continue\n\n        # Succeeded: extract message content.\n        message = result.message\n        content_blocks = getattr(message, \"content\", []) or []\n\n        text = None\n        parsed = None\n\n        for block in content_blocks:\n            block_type = getattr(block, \"type\", None)\n            if block_type == \"tool_use\" and getattr(block, \"name\", None) == \"gate_verdict\":\n                # Structured output via tool_use \u2014 extract .input dict.\n                parsed = block.input if isinstance(block.input, dict) else dict(block.input)\n                break\n            if block_type == \"text\" and text is None:\n                text = block.text\n\n        # Serialize raw_response as a dict for storage.\n        raw_response = {\n            \"id\": getattr(message, \"id\", None),\n            \"model\": getattr(message, \"model\", None),\n            \"stop_reason\": getattr(message, \"stop_reason\", None),\n            \"usage\": {\n                \"input_tokens\": getattr(getattr(message, \"usage\", None), \"input_tokens\", None),\n                \"output_tokens\": getattr(getattr(message, \"usage\", None), \"output_tokens\", None),\n            },\n        }\n\n        results_by_id[cid] = AnthropicBatchResult(\n            custom_id=cid,\n            text=text,\n            parsed=parsed,\n            raw_response=raw_response,\n            error=None,\n        )\n\n    # Return in the same order as the input requests list.\n    ordered_results: list[AnthropicBatchResult] = []\n    for cid in ordered_ids:\n        if cid in results_by_id:\n            ordered_results.append(results_by_id[cid])\n        else:\n            ordered_results.append(AnthropicBatchResult(\n                custom_id=cid,\n                text=None,\n                parsed=None,\n                raw_response={},\n                error=\"missing from batch results\",\n            ))\n\n    return ordered_results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Submit batch requests to Anthropic API and poll until complete.", "label": "positive", "reason": "", "function_id": "199a9f3a3e1c8464", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:15.920686+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_anthropic_batch\" line_start=\"1296\">\n[CDATA-OPEN]\n            requests: list[AnthropicBatchRequest],\n    *,\n    poll_interval_seconds: float = 5.0,\n    timeout_seconds: float = 1800.0,\n    api_client=None,  # default: anthropic.Anthropic() from env\n) -> list[AnthropicBatchResult]:\n    \"\"\"Submit requests to the Anthropic Batches API, poll until ended, and parse results.\n\n    Steps:\n      1. Build the requests list for client.messages.batches.create().\n         System block carries cache_control: ephemeral for preamble caching.\n         If response_schema is set, inject a gate_verdict tool + tool_choice\n         to enforce structured output via Anthropic tool use.\n      2. Submit via client.messages.batches.create(requests=batch_requests).\n      3. Poll client.messages.batches.retrieve(batch.id) until\n         processing_status in {\"ended\", \"canceling\", \"expired\"}.\n      4. Stream results via client.messages.batches.results(batch.id).\n      5. For each result: extract text from the first content block, or parse\n         the tool_use block input as `parsed` when response_schema was set.\n\n    Returns results in the same order as the input requests list.\n    \"\"\"\n    import anthropic\n    import os\n\n    ensure_api_key(\"ANTHROPIC_API_KEY\")\n    client = api_client or anthropic.Anthropic(api_key=os.environ.get(\"ANTHROPIC_API_KEY\"))\n    ordered_ids = [r.custom_id for r in requests]\n\n    # Step 1: Build the Anthropic batch requests list.\n    batch_requests = []\n    for req in requests:\n        params: dict = {\n            \"model\": req.model,\n            \"max_tokens\": req.max_tokens,\n            \"system\": [\n                {\n                    \"type\": \"text\",\n                    \"text\": req.cached_preamble,\n                    \"cache_control\": {\"type\": \"ephemeral\"},\n                }\n            ],\n            \"messages\": [{\"role\": \"user\", \"content\": req.payload}],\n        }\n        if req.response_schema is not None:\n            params[\"tools\"] = [\n                {\n                    \"name\": \"gate_verdict\",\n                    \"description\": \"Return the gate verdict\",\n                    \"input_schema\": req.response_schema,\n                }\n            ]\n            params[\"tool_choice\"] = {\"type\": \"tool\", \"name\": \"gate_verdict\"}\n\n        batch_requests.append({\n            \"custom_id\": req.custom_id,\n            \"params\": params,\n        })\n\n    # Step 2: Submit batch.\n    batch = client.messages.batches.create(requests=batch_requests)\n    batch_id = batch.id\n\n    # Step 3: Poll until terminal status.\n    terminal_statuses = {\"ended\", \"canceling\", \"expired\"}\n    deadline = time.monotonic() + timeout_seconds\n    status = batch.processing_status\n\n    while status not in terminal_statuses:\n        if time.monotonic() > deadline:\n            raise TimeoutError(\n                f\"Anthropic batch {batch_id} did not reach terminal status within \"\n                f\"{timeout_seconds}s. Last status: {status}\"\n            )\n        time.sleep(poll_interval_seconds)\n        batch = client.messages.batches.retrieve(batch_id)\n        status = batch.processing_status\n\n    # Step 4 & 5: Stream results and parse.\n    results_by_id: dict[str, AnthropicBatchResult] = {}\n\n    for result_item in client.messages.batches.results(batch_id):\n        cid = result_item.custom_id\n        result = result_item.result\n\n        # Check for error result types.\n        result_type = getattr(result, \"type\", None)\n\n        if result_type != \"succeeded\":\n            error_msg = f\"result type: {result_type}\"\n            if hasattr(result, \"error\"):\n                err = result.error\n                error_msg = getattr(err, \"message\", None) or str(err)\n            results_by_id[cid] = AnthropicBatchResult(\n                custom_id=cid,\n                text=None,\n                parsed=None,\n                raw_response={},\n                error=error_msg,\n            )\n            continue\n\n        # Succeeded: extract message content.\n        message = result.message\n        content_blocks = getattr(message, \"content\", []) or []\n\n        text = None\n        parsed = None\n\n        for block in content_blocks:\n            block_type = getattr(block, \"type\", None)\n            if block_type == \"tool_use\" and getattr(block, \"name\", None) == \"gate_verdict\":\n                # Structured output via tool_use \u2014 extract .input dict.\n                parsed = block.input if isinstance(block.input, dict) else dict(block.input)\n                break\n            if block_type == \"text\" and text is None:\n                text = block.text\n\n        # Serialize raw_response as a dict for storage.\n        raw_response = {\n            \"id\": getattr(message, \"id\", None),\n            \"model\": getattr(message, \"model\", None),\n            \"stop_reason\": getattr(message, \"stop_reason\", None),\n            \"usage\": {\n                \"input_tokens\": getattr(getattr(message, \"usage\", None), \"input_tokens\", None),\n                \"output_tokens\": getattr(getattr(message, \"usage\", None), \"output_tokens\", None),\n            },\n        }\n\n        results_by_id[cid] = AnthropicBatchResult(\n            custom_id=cid,\n            text=text,\n            parsed=parsed,\n            raw_response=raw_response,\n            error=None,\n        )\n\n    # Return in the same order as the input requests list.\n    ordered_results: list[AnthropicBatchResult] = []\n    for cid in ordered_ids:\n        if cid in results_by_id:\n            ordered_results.append(results_by_id[cid])\n        else:\n            ordered_results.append(AnthropicBatchResult(\n                custom_id=cid,\n                text=None,\n                parsed=None,\n                raw_response={},\n                error=\"missing from batch results\",\n            ))\n\n    return ordered_results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Build requests, submit batch, poll for completion, stream, and parse results.", "label": "negative", "reason": "", "function_id": "199a9f3a3e1c8464", "centroid_distance": 0.1524677218899736}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:21.827265+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_incremental_mld_pipeline\" line_start=\"1704\">\n[CDATA-OPEN]\n            functions: list[dict],\n    *,\n    converge_template: str,\n    max_batches: int,\n    convergence_threshold: float,\n    dispatch: \"Literal['auto','sync','batch']\",\n    verbose: bool,\n    on_chunk_complete: \"Callable[[list[FunctionMldResult], list[dict]], None] | None\" = None,\n) -> \"list[FunctionMldResult]\":\n    \"\"\"Incremental batch MLD pipeline: embed and upsert after each generation chunk.\n\n    Replaces the all-at-once batch path in run_embedding_mld_pipeline_for_functions\n    with a per-chunk loop that immediately embeds and builds FunctionMldResult\n    objects for the functions covered by each chunk, then fires on_chunk_complete\n    so the caller can upsert to ChromaDB and write SFT records before the next\n    chunk begins.\n\n    Chunk-to-function mapping: each generation chunk contains BATCH_CHUNK_SIZE\n    requests (100 by default). With max_batches calls per function, each chunk\n    of 100 requests covers 100 // max_batches functions. Functions whose fi\n    indices fall in [chunk_start_fi, chunk_end_fi) are fully complete after\n    their chunk finishes.\n\n    on_chunk_complete(chunk_results, chunk_functions) is called with:\n      - chunk_results: list[FunctionMldResult] for functions in this chunk\n      - chunk_functions: list[dict] \u2014 the corresponding function dicts\n\n    Returns the full accumulated results list in input order.\n    \"\"\"\n    import openai\n    import os\n    import sys\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n\n    n_funcs = len(functions)\n\n    # Step 1 \u2014 Build all GenerationRequests upfront (same as existing batch path).\n    gen_requests: list[GenerationRequest] = []\n    func_prompt_used: list[str] = [\"\"] * n_funcs\n    for fi, fn in enumerate(functions):\n        for ci in range(max_batches):\n            req = _build_converge_request(fn, fi, ci, converge_template)\n            gen_requests.append(req)\n            if ci == 0:\n                func_prompt_used[fi] = req.cached_preamble + \"\\n\\n\" + req.payload\n\n    # Step 2 \u2014 Split generation requests into BATCH_CHUNK_SIZE chunks.\n    # Each chunk covers (BATCH_CHUNK_SIZE // max_batches) functions.\n    chunks = _chunk_requests(gen_requests)\n    n_chunks = len(chunks)\n\n    all_results: list[FunctionMldResult] = []\n    processed_fi = 0  # running count of functions fully processed\n    _incr_start = time.monotonic()\n\n    for chunk_idx, chunk_requests in enumerate(chunks):\n        if verbose:\n            print(\n                f\"[mld] incremental chunk {chunk_idx + 1}/{n_chunks} \"\n                f\"({len(chunk_requests)} requests)\",\n                file=sys.stderr,\n            )\n\n        # Emit submission progress.\n        _default_progress_printer({\n            \"event\": \"submitted\",\n            \"chunk_index\": chunk_idx,\n            \"n_chunks\": n_chunks,\n            \"elapsed_s\": time.monotonic() - _incr_start,\n        })\n\n        # Step 3a \u2014 Submit this generation chunk and wait for it to complete.\n        gen_results = _submit_one_chunk_generation(\n            client,\n            chunk_requests,\n        )\n\n        # Emit completion progress.\n        _default_progress_printer({\n            \"event\": \"completed\",\n            \"chunk_index\": chunk_idx,\n            \"n_chunks\": n_chunks,\n            \"elapsed_s\": time.monotonic() - _incr_start,\n        })\n\n        # Determine which function indices this chunk covers.\n        # Each request has custom_id \"gen:func={fi}:call={ci}\"; extract unique fi.\n        chunk_fi_set: set[int] = set()\n        for req in chunk_requests:\n            # Parse fi from \"gen:func={fi}:call={ci}\"\n            cid = req.custom_id\n            try:\n                fi_part = cid.split(\":\")[1]  # \"func={fi}\"\n                fi_val = int(fi_part.split(\"=\")[1])\n                chunk_fi_set.add(fi_val)\n            except (IndexError, ValueError):\n                pass\n\n        chunk_fi_list = sorted(chunk_fi_set)\n        if not chunk_fi_list:\n            continue\n\n        chunk_base_fi = chunk_fi_list[0]\n        chunk_function_dicts = [functions[fi] for fi in chunk_fi_list]\n\n        # Step 3b-e \u2014 Embed + build FunctionMldResults for this chunk's functions.\n        chunk_results = _build_function_mld_results_from_gen(\n            gen_results,\n            chunk_function_dicts,\n            max_batches=max_batches,\n            func_prompt_used=func_prompt_used,\n            func_base_fi=chunk_base_fi,\n            dispatch=dispatch,\n        )\n\n        all_results.extend(chunk_results)\n        processed_fi += len(chunk_results)\n\n        # Step 3e \u2014 Fire the callback so the caller can upsert + write SFT.\n        if on_chunk_complete is not None:\n            on_chunk_complete(chunk_results, chunk_function_dicts)\n\n    _emit_convergence_summary(all_results, verbose=verbose)\n    return all_results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Run incremental batch MLD pipeline embedding and upserting after each chunk.", "label": "positive", "reason": "", "function_id": "76102e578c266fcb", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:21.827531+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/batch.py\" name=\"run_incremental_mld_pipeline\" line_start=\"1704\">\n[CDATA-OPEN]\n            functions: list[dict],\n    *,\n    converge_template: str,\n    max_batches: int,\n    convergence_threshold: float,\n    dispatch: \"Literal['auto','sync','batch']\",\n    verbose: bool,\n    on_chunk_complete: \"Callable[[list[FunctionMldResult], list[dict]], None] | None\" = None,\n) -> \"list[FunctionMldResult]\":\n    \"\"\"Incremental batch MLD pipeline: embed and upsert after each generation chunk.\n\n    Replaces the all-at-once batch path in run_embedding_mld_pipeline_for_functions\n    with a per-chunk loop that immediately embeds and builds FunctionMldResult\n    objects for the functions covered by each chunk, then fires on_chunk_complete\n    so the caller can upsert to ChromaDB and write SFT records before the next\n    chunk begins.\n\n    Chunk-to-function mapping: each generation chunk contains BATCH_CHUNK_SIZE\n    requests (100 by default). With max_batches calls per function, each chunk\n    of 100 requests covers 100 // max_batches functions. Functions whose fi\n    indices fall in [chunk_start_fi, chunk_end_fi) are fully complete after\n    their chunk finishes.\n\n    on_chunk_complete(chunk_results, chunk_functions) is called with:\n      - chunk_results: list[FunctionMldResult] for functions in this chunk\n      - chunk_functions: list[dict] \u2014 the corresponding function dicts\n\n    Returns the full accumulated results list in input order.\n    \"\"\"\n    import openai\n    import os\n    import sys\n\n    ensure_api_key(\"OPENAI_API_KEY\")\n    client = openai.OpenAI(api_key=os.environ.get(\"OPENAI_API_KEY\"))\n\n    n_funcs = len(functions)\n\n    # Step 1 \u2014 Build all GenerationRequests upfront (same as existing batch path).\n    gen_requests: list[GenerationRequest] = []\n    func_prompt_used: list[str] = [\"\"] * n_funcs\n    for fi, fn in enumerate(functions):\n        for ci in range(max_batches):\n            req = _build_converge_request(fn, fi, ci, converge_template)\n            gen_requests.append(req)\n            if ci == 0:\n                func_prompt_used[fi] = req.cached_preamble + \"\\n\\n\" + req.payload\n\n    # Step 2 \u2014 Split generation requests into BATCH_CHUNK_SIZE chunks.\n    # Each chunk covers (BATCH_CHUNK_SIZE // max_batches) functions.\n    chunks = _chunk_requests(gen_requests)\n    n_chunks = len(chunks)\n\n    all_results: list[FunctionMldResult] = []\n    processed_fi = 0  # running count of functions fully processed\n    _incr_start = time.monotonic()\n\n    for chunk_idx, chunk_requests in enumerate(chunks):\n        if verbose:\n            print(\n                f\"[mld] incremental chunk {chunk_idx + 1}/{n_chunks} \"\n                f\"({len(chunk_requests)} requests)\",\n                file=sys.stderr,\n            )\n\n        # Emit submission progress.\n        _default_progress_printer({\n            \"event\": \"submitted\",\n            \"chunk_index\": chunk_idx,\n            \"n_chunks\": n_chunks,\n            \"elapsed_s\": time.monotonic() - _incr_start,\n        })\n\n        # Step 3a \u2014 Submit this generation chunk and wait for it to complete.\n        gen_results = _submit_one_chunk_generation(\n            client,\n            chunk_requests,\n        )\n\n        # Emit completion progress.\n        _default_progress_printer({\n            \"event\": \"completed\",\n            \"chunk_index\": chunk_idx,\n            \"n_chunks\": n_chunks,\n            \"elapsed_s\": time.monotonic() - _incr_start,\n        })\n\n        # Determine which function indices this chunk covers.\n        # Each request has custom_id \"gen:func={fi}:call={ci}\"; extract unique fi.\n        chunk_fi_set: set[int] = set()\n        for req in chunk_requests:\n            # Parse fi from \"gen:func={fi}:call={ci}\"\n            cid = req.custom_id\n            try:\n                fi_part = cid.split(\":\")[1]  # \"func={fi}\"\n                fi_val = int(fi_part.split(\"=\")[1])\n                chunk_fi_set.add(fi_val)\n            except (IndexError, ValueError):\n                pass\n\n        chunk_fi_list = sorted(chunk_fi_set)\n        if not chunk_fi_list:\n            continue\n\n        chunk_base_fi = chunk_fi_list[0]\n        chunk_function_dicts = [functions[fi] for fi in chunk_fi_list]\n\n        # Step 3b-e \u2014 Embed + build FunctionMldResults for this chunk's functions.\n        chunk_results = _build_function_mld_results_from_gen(\n            gen_results,\n            chunk_function_dicts,\n            max_batches=max_batches,\n            func_prompt_used=func_prompt_used,\n            func_base_fi=chunk_base_fi,\n            dispatch=dispatch,\n        )\n\n        all_results.extend(chunk_results)\n        processed_fi += len(chunk_results)\n\n        # Step 3e \u2014 Fire the callback so the caller can upsert + write SFT.\n        if on_chunk_complete is not None:\n            on_chunk_complete(chunk_results, chunk_function_dicts)\n\n    _emit_convergence_summary(all_results, verbose=verbose)\n    return all_results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Set up API key, split requests into chunks, send requests, process results, call callback, return results.", "label": "negative", "reason": "", "function_id": "76102e578c266fcb", "centroid_distance": 0.35672894846461745}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:33.799031+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"cosine_similarity\" line_start=\"14\">\n[CDATA-OPEN]\n            \"\"\"Compute cosine similarity between two vectors. Returns float in [-1, 1].\"\"\"\n    va = np.asarray(a, dtype=np.float64)\n    vb = np.asarray(b, dtype=np.float64)\n    norm_a = np.linalg.norm(va)\n    norm_b = np.linalg.norm(vb)\n    if norm_a == 0.0 or norm_b == 0.0:\n        return 0.0\n    return float(np.dot(va, vb) / (norm_a * norm_b))\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Compute cosine similarity between two vectors and return a float in [-1, 1].", "label": "positive", "reason": "", "function_id": "4dc7af20e3b48aeb", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:33.799262+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"cosine_similarity\" line_start=\"14\">\n[CDATA-OPEN]\n            \"\"\"Compute cosine similarity between two vectors. Returns float in [-1, 1].\"\"\"\n    va = np.asarray(a, dtype=np.float64)\n    vb = np.asarray(b, dtype=np.float64)\n    norm_a = np.linalg.norm(va)\n    norm_b = np.linalg.norm(vb)\n    if norm_a == 0.0 or norm_b == 0.0:\n        return 0.0\n    return float(np.dot(va, vb) / (norm_a * norm_b))\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Compute cosine similarity between vectors a and b, handling zero vectors by returning 0.0.", "label": "negative", "reason": "", "function_id": "4dc7af20e3b48aeb", "centroid_distance": 0.1401226205660655}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:38.879096+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"centroid\" line_start=\"25\">\n[CDATA-OPEN]\n            \"\"\"Compute the element-wise mean of vectors. Returns list[float].\"\"\"\n    arr = np.asarray(vectors, dtype=np.float64)\n    return arr.mean(axis=0).tolist()\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Compute the element-wise mean of vectors and return it as a list of floats.", "label": "positive", "reason": "", "function_id": "e3f3a1ddcf78273a", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:38.879324+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"centroid\" line_start=\"25\">\n[CDATA-OPEN]\n            \"\"\"Compute the element-wise mean of vectors. Returns list[float].\"\"\"\n    arr = np.asarray(vectors, dtype=np.float64)\n    return arr.mean(axis=0).tolist()\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Average vectors element-wise and output as float list.", "label": "negative", "reason": "", "function_id": "e3f3a1ddcf78273a", "centroid_distance": 0.11915765244043608}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:44.151037+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"spherical_variance\" line_start=\"31\">\n[CDATA-OPEN]\n            \"\"\"1 - ||mean(unit_vectors)||. 0 = perfect consensus, 1 = scatter.\"\"\"\n    arr = np.asarray(vectors, dtype=np.float64)\n    norms = np.linalg.norm(arr, axis=1, keepdims=True)\n    norms = np.where(norms == 0, 1.0, norms)\n    unit = arr / norms\n    mean_vec = unit.mean(axis=0)\n    return float(1.0 - np.linalg.norm(mean_vec))\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Compute the spherical variance of vectors by converting them to unit vectors, averaging, and subtracting the norm from one.", "label": "positive", "reason": "", "function_id": "bec17644c15097b1", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:44.151268+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"spherical_variance\" line_start=\"31\">\n[CDATA-OPEN]\n            \"\"\"1 - ||mean(unit_vectors)||. 0 = perfect consensus, 1 = scatter.\"\"\"\n    arr = np.asarray(vectors, dtype=np.float64)\n    norms = np.linalg.norm(arr, axis=1, keepdims=True)\n    norms = np.where(norms == 0, 1.0, norms)\n    unit = arr / norms\n    mean_vec = unit.mean(axis=0)\n    return float(1.0 - np.linalg.norm(mean_vec))\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Return one minus the average vector's norm after normalization.", "label": "negative", "reason": "", "function_id": "bec17644c15097b1", "centroid_distance": 0.25926490932637136}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:51.432661+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"convergence_verdict\" line_start=\"41\">\n[CDATA-OPEN]\n            draft_1_vecs: list[list[float]],\n    draft_2_vecs: list[list[float]],\n    draft_3_vecs: list[list[float]],\n) -> tuple[str, dict[str, float]]:\n    \"\"\"Return (verdict, sv_dict) from per-draft spherical variances.\n\n    Computes spherical variance for each draft tier and compares the\n    sequence sv1 \u2192 sv2 \u2192 sv3. Strictly decreasing = CONVERGED (variance\n    shrinks as drafts refine). Strictly increasing = DIVERGENT. Anything\n    else = AMBIGUOUS. No threshold \u2014 the relationship between the three\n    values is the test.\n\n    Returns:\n      verdict: \"CONVERGED\" | \"DIVERGENT\" | \"AMBIGUOUS\"\n      sv_dict: {\"sv1\": float, \"sv2\": float, \"sv3\": float}\n    \"\"\"\n    sv1 = spherical_variance(draft_1_vecs)\n    sv2 = spherical_variance(draft_2_vecs)\n    sv3 = spherical_variance(draft_3_vecs)\n    svs = {\"sv1\": sv1, \"sv2\": sv2, \"sv3\": sv3}\n    if sv1 > sv2 > sv3:\n        return \"CONVERGED\", svs\n    elif sv3 > sv2 > sv1:\n        return \"DIVERGENT\", svs\n    else:\n        return \"AMBIGUOUS\", svs\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Compare spherical variances of drafts; if decreasing, return CONVERGED with variances.", "label": "positive", "reason": "", "function_id": "d2d19a4f6213cb45", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:00:51.432893+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/geometry.py\" name=\"convergence_verdict\" line_start=\"41\">\n[CDATA-OPEN]\n            draft_1_vecs: list[list[float]],\n    draft_2_vecs: list[list[float]],\n    draft_3_vecs: list[list[float]],\n) -> tuple[str, dict[str, float]]:\n    \"\"\"Return (verdict, sv_dict) from per-draft spherical variances.\n\n    Computes spherical variance for each draft tier and compares the\n    sequence sv1 \u2192 sv2 \u2192 sv3. Strictly decreasing = CONVERGED (variance\n    shrinks as drafts refine). Strictly increasing = DIVERGENT. Anything\n    else = AMBIGUOUS. No threshold \u2014 the relationship between the three\n    values is the test.\n\n    Returns:\n      verdict: \"CONVERGED\" | \"DIVERGENT\" | \"AMBIGUOUS\"\n      sv_dict: {\"sv1\": float, \"sv2\": float, \"sv3\": float}\n    \"\"\"\n    sv1 = spherical_variance(draft_1_vecs)\n    sv2 = spherical_variance(draft_2_vecs)\n    sv3 = spherical_variance(draft_3_vecs)\n    svs = {\"sv1\": sv1, \"sv2\": sv2, \"sv3\": sv3}\n    if sv1 > sv2 > sv3:\n        return \"CONVERGED\", svs\n    elif sv3 > sv2 > sv1:\n        return \"DIVERGENT\", svs\n    else:\n        return \"AMBIGUOUS\", svs\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Check if sv1 > sv2 > sv3; if true, verdict is CONVERGED, else if sv3 > sv2 > sv1, then DIVERGENT.", "label": "negative", "reason": "", "function_id": "d2d19a4f6213cb45", "centroid_distance": 0.35192459351163985}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:00.547694+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"ExpansionConfig\" line_start=\"26\">\n[CDATA-OPEN]\n            \"\"\"Thresholds and call counts for the adaptive expansion policy.\n\n    Invariants enforced at construction:\n      - expand_ratio must be in (0, accept_ratio]\n      - expanded_accept_ratio must be in (0, 1]\n      - initial_n_calls and expansion_n_calls must each be >= 1\n    \"\"\"\n    accept_ratio: float = 0.8\n    expand_ratio: float = 0.6\n    expanded_accept_ratio: float = 0.7\n    initial_n_calls: int = 5\n    expansion_n_calls: int = 5\n    drafts_per_call: int = 3\n\n    def __post_init__(self):\n        if not (0.0 < self.expand_ratio <= self.accept_ratio <= 1.0):\n            raise ValueError(\"expand_ratio must be in (0, accept_ratio]\")\n        if not (0.0 < self.expanded_accept_ratio <= 1.0):\n            raise ValueError(\"expanded_accept_ratio must be in (0, 1]\")\n        if self.initial_n_calls < 1 or self.expansion_n_calls < 1:\n            raise ValueError(\"call counts must be >= 1\")\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define an ExpansionConfig class with thresholds and call counts for adaptive expansion.", "label": "positive", "reason": "", "function_id": "02ffaf00a8245747", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:00.547931+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"ExpansionConfig\" line_start=\"26\">\n[CDATA-OPEN]\n            \"\"\"Thresholds and call counts for the adaptive expansion policy.\n\n    Invariants enforced at construction:\n      - expand_ratio must be in (0, accept_ratio]\n      - expanded_accept_ratio must be in (0, 1]\n      - initial_n_calls and expansion_n_calls must each be >= 1\n    \"\"\"\n    accept_ratio: float = 0.8\n    expand_ratio: float = 0.6\n    expanded_accept_ratio: float = 0.7\n    initial_n_calls: int = 5\n    expansion_n_calls: int = 5\n    drafts_per_call: int = 3\n\n    def __post_init__(self):\n        if not (0.0 < self.expand_ratio <= self.accept_ratio <= 1.0):\n            raise ValueError(\"expand_ratio must be in (0, accept_ratio]\")\n        if not (0.0 < self.expanded_accept_ratio <= 1.0):\n            raise ValueError(\"expanded_accept_ratio must be in (0, 1]\")\n        if self.initial_n_calls < 1 or self.expansion_n_calls < 1:\n            raise ValueError(\"call counts must be >= 1\")\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define a data class for expansion thresholds with validation in __post_init__.", "label": "negative", "reason": "", "function_id": "02ffaf00a8245747", "centroid_distance": 0.26444191899501157}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:09.348771+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"CandidateEvaluation\" line_start=\"51\">\n[CDATA-OPEN]\n            \"\"\"Result of one adaptive-expansion evaluation for a function.\n\n    Fields:\n      function_name      -- identifier passed to evaluate_with_expansion\n      initial_ratio      -- cluster_size / n_initial (first generation pass)\n      expanded           -- True if expansion generation was triggered\n      expanded_ratio     -- cluster_size / n_total after expansion, or None\n      converged          -- True if the evaluation passed an acceptance threshold\n      n_candidates_total -- total number of candidate strings generated\n      cluster_size       -- dominant-cluster size at decision point\n      candidates         -- all candidate strings (initial, or combined if expanded)\n      vectors            -- all embedding vectors (initial, or combined if expanded)\n    \"\"\"\n    function_name: str\n    initial_ratio: float\n    expanded: bool\n    expanded_ratio: float | None\n    converged: bool\n    n_candidates_total: int\n    cluster_size: int\n    candidates: list[str]\n    vectors: list\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe the CandidateEvaluation class as the data structure storing evaluation results for adaptive-expansion methods.", "label": "positive", "reason": "", "function_id": "36588a1c3280604a", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:09.349004+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"CandidateEvaluation\" line_start=\"51\">\n[CDATA-OPEN]\n            \"\"\"Result of one adaptive-expansion evaluation for a function.\n\n    Fields:\n      function_name      -- identifier passed to evaluate_with_expansion\n      initial_ratio      -- cluster_size / n_initial (first generation pass)\n      expanded           -- True if expansion generation was triggered\n      expanded_ratio     -- cluster_size / n_total after expansion, or None\n      converged          -- True if the evaluation passed an acceptance threshold\n      n_candidates_total -- total number of candidate strings generated\n      cluster_size       -- dominant-cluster size at decision point\n      candidates         -- all candidate strings (initial, or combined if expanded)\n      vectors            -- all embedding vectors (initial, or combined if expanded)\n    \"\"\"\n    function_name: str\n    initial_ratio: float\n    expanded: bool\n    expanded_ratio: float | None\n    converged: bool\n    n_candidates_total: int\n    cluster_size: int\n    candidates: list[str]\n    vectors: list\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Represent evaluation metrics, candidate list, embedding vectors, and expansion status for candidate functions.", "label": "negative", "reason": "", "function_id": "36588a1c3280604a", "centroid_distance": 0.2376408246835987}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:14.962507+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"evaluate_with_expansion\" line_start=\"80\">\n[CDATA-OPEN]\n            function_name: str,\n    *,\n    generate_fn: Callable[[int], tuple[list[str], list]],\n    cluster_fn: Callable[[list], int],\n    config: \"ExpansionConfig | None\" = None,\n    audit_path: str = \"/Users/elliotwillis/Desktop/fe-toolkit/.pidgin/audit/convergence.jsonl\",\n) -> CandidateEvaluation:\n    \"\"\"Run adaptive candidate generation with optional expansion for one function.\n\n    Behaviour:\n      1. Generate an initial batch via generate_fn(config.initial_n_calls).\n      2. Compute initial_ratio = cluster_fn(vecs) / len(cands).\n      3. Branch on initial_ratio:\n           >= accept_ratio          \u2192 converged immediately, no expansion\n           >= expand_ratio          \u2192 expand: generate a second batch and re-cluster\n           < expand_ratio           \u2192 non-convergent, no expansion\n      4. Append a JSONL audit entry to audit_path (creating directories as needed).\n      5. Return a CandidateEvaluation.\n\n    The n argument to generate_fn is the n_calls count (not the total candidates).\n    cluster_fn receives ALL vectors (initial, or combined after expansion) and\n    must return an integer cluster_size.\n    \"\"\"\n    if config is None:\n        config = ExpansionConfig()\n\n    # -- Phase 1: initial generation -----------------------------------------\n    cands_initial, vecs_initial = generate_fn(config.initial_n_calls)\n    n_initial = len(cands_initial)\n    cluster_initial = cluster_fn(vecs_initial)\n    ratio_initial = cluster_initial / n_initial\n\n    # -- Phase 2: branch on ratio --------------------------------------------\n    if ratio_initial >= config.accept_ratio:\n        # Accept immediately \u2014 no expansion needed\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=False,\n            expanded_ratio=None,\n            converged=True,\n            n_candidates_total=n_initial,\n            cluster_size=cluster_initial,\n            candidates=cands_initial,\n            vectors=vecs_initial,\n        )\n\n    elif ratio_initial >= config.expand_ratio:\n        # Borderline \u2014 expand with a second generation pass\n        cands_extra, vecs_extra = generate_fn(config.expansion_n_calls)\n        cands_combined = cands_initial + cands_extra\n        vecs_combined = vecs_initial + vecs_extra\n        n_total = len(cands_combined)\n        cluster_expanded = cluster_fn(vecs_combined)\n        ratio_expanded = cluster_expanded / n_total\n        converged = ratio_expanded >= config.expanded_accept_ratio\n\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=True,\n            expanded_ratio=ratio_expanded,\n            converged=converged,\n            n_candidates_total=n_total,\n            cluster_size=cluster_expanded,\n            candidates=cands_combined,\n            vectors=vecs_combined,\n        )\n\n    else:\n        # Below expand threshold \u2014 non-convergent, do not expand\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=False,\n            expanded_ratio=None,\n            converged=False,\n            n_candidates_total=n_initial,\n            cluster_size=cluster_initial,\n            candidates=cands_initial,\n            vectors=vecs_initial,\n        )\n\n    # -- Phase 3: audit log --------------------------------------------------\n    _append_audit(result, audit_path)\n\n    return result\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Run evaluate_with_expansion to generate candidates, cluster vectors, determine if converged, optionally expand, log audit, and return evaluation.", "label": "positive", "reason": "", "function_id": "0d6754d74d1a6234", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:14.962808+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/candidates.py\" name=\"evaluate_with_expansion\" line_start=\"80\">\n[CDATA-OPEN]\n            function_name: str,\n    *,\n    generate_fn: Callable[[int], tuple[list[str], list]],\n    cluster_fn: Callable[[list], int],\n    config: \"ExpansionConfig | None\" = None,\n    audit_path: str = \"/Users/elliotwillis/Desktop/fe-toolkit/.pidgin/audit/convergence.jsonl\",\n) -> CandidateEvaluation:\n    \"\"\"Run adaptive candidate generation with optional expansion for one function.\n\n    Behaviour:\n      1. Generate an initial batch via generate_fn(config.initial_n_calls).\n      2. Compute initial_ratio = cluster_fn(vecs) / len(cands).\n      3. Branch on initial_ratio:\n           >= accept_ratio          \u2192 converged immediately, no expansion\n           >= expand_ratio          \u2192 expand: generate a second batch and re-cluster\n           < expand_ratio           \u2192 non-convergent, no expansion\n      4. Append a JSONL audit entry to audit_path (creating directories as needed).\n      5. Return a CandidateEvaluation.\n\n    The n argument to generate_fn is the n_calls count (not the total candidates).\n    cluster_fn receives ALL vectors (initial, or combined after expansion) and\n    must return an integer cluster_size.\n    \"\"\"\n    if config is None:\n        config = ExpansionConfig()\n\n    # -- Phase 1: initial generation -----------------------------------------\n    cands_initial, vecs_initial = generate_fn(config.initial_n_calls)\n    n_initial = len(cands_initial)\n    cluster_initial = cluster_fn(vecs_initial)\n    ratio_initial = cluster_initial / n_initial\n\n    # -- Phase 2: branch on ratio --------------------------------------------\n    if ratio_initial >= config.accept_ratio:\n        # Accept immediately \u2014 no expansion needed\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=False,\n            expanded_ratio=None,\n            converged=True,\n            n_candidates_total=n_initial,\n            cluster_size=cluster_initial,\n            candidates=cands_initial,\n            vectors=vecs_initial,\n        )\n\n    elif ratio_initial >= config.expand_ratio:\n        # Borderline \u2014 expand with a second generation pass\n        cands_extra, vecs_extra = generate_fn(config.expansion_n_calls)\n        cands_combined = cands_initial + cands_extra\n        vecs_combined = vecs_initial + vecs_extra\n        n_total = len(cands_combined)\n        cluster_expanded = cluster_fn(vecs_combined)\n        ratio_expanded = cluster_expanded / n_total\n        converged = ratio_expanded >= config.expanded_accept_ratio\n\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=True,\n            expanded_ratio=ratio_expanded,\n            converged=converged,\n            n_candidates_total=n_total,\n            cluster_size=cluster_expanded,\n            candidates=cands_combined,\n            vectors=vecs_combined,\n        )\n\n    else:\n        # Below expand threshold \u2014 non-convergent, do not expand\n        result = CandidateEvaluation(\n            function_name=function_name,\n            initial_ratio=ratio_initial,\n            expanded=False,\n            expanded_ratio=None,\n            converged=False,\n            n_candidates_total=n_initial,\n            cluster_size=cluster_initial,\n            candidates=cands_initial,\n            vectors=vecs_initial,\n        )\n\n    # -- Phase 3: audit log --------------------------------------------------\n    _append_audit(result, audit_path)\n\n    return result\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Run adaptive candidate evaluation with optional expansion based on clustering ratio thresholds.", "label": "negative", "reason": "", "function_id": "0d6754d74d1a6234", "centroid_distance": 0.20851877499868787}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:26.745380+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/mld.py\" name=\"MLDResult\" line_start=\"40\">\n[CDATA-OPEN]\n            \"\"\"The chosen description for an input plus pipeline-stage metadata.\"\"\"\n    description: str            # the chosen description\n    target_n: int               # the concision target it was generated against\n    natural_length: int         # word-split length of the chosen description\n    confidence: float           # 0.0\u20131.0\n    converged: bool\n    candidates_generated: int   # = num_calls * 3 (drafts)\n    candidates_filtered: int    # rejected by mechanical pre-filter\n    gate_verdict: str           # \"valid\" | \"invalid\" | \"ambiguous\"\n    gate_reason: str\n    selection_rationale: str    # from Opus if invoked, else gate reason\n    targets_tried: list[int] = field(default_factory=list)\n    degenerate_pre_check: bool = False  # True if avg_cosine_sim>0.999 fired\n    deduped: bool = False                # True if textual dedup was applied\n    cost: CostSummary = field(default_factory=CostSummary)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe the input function with pipeline metadata in a single, imperatively phrased sentence.", "label": "positive", "reason": "", "function_id": "b1caf381acf62181", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:26.745603+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/mld.py\" name=\"MLDResult\" line_start=\"40\">\n[CDATA-OPEN]\n            \"\"\"The chosen description for an input plus pipeline-stage metadata.\"\"\"\n    description: str            # the chosen description\n    target_n: int               # the concision target it was generated against\n    natural_length: int         # word-split length of the chosen description\n    confidence: float           # 0.0\u20131.0\n    converged: bool\n    candidates_generated: int   # = num_calls * 3 (drafts)\n    candidates_filtered: int    # rejected by mechanical pre-filter\n    gate_verdict: str           # \"valid\" | \"invalid\" | \"ambiguous\"\n    gate_reason: str\n    selection_rationale: str    # from Opus if invoked, else gate reason\n    targets_tried: list[int] = field(default_factory=list)\n    degenerate_pre_check: bool = False  # True if avg_cosine_sim>0.999 fired\n    deduped: bool = False                # True if textual dedup was applied\n    cost: CostSummary = field(default_factory=CostSummary)\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Provide a brief summary of the input description, confidence level, convergence status, candidate counts, and gate verdict.", "label": "negative", "reason": "", "function_id": "b1caf381acf62181", "centroid_distance": 0.35284968908768866}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:37.037124+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/pipeline/mld.py\" name=\"mld\" line_start=\"432\">\n[CDATA-OPEN]\n            input_text: str,\n    prompt: str,\n    target_n: int = 12,\n    prompt_text: str | None = None,\n    gate_prompt_text: str | None = None,\n    n_calls: int = 5,\n    temperature: float = 0.8,\n    generation_model: str = \"gpt-4.1-nano\",\n    gate_model: str = \"gpt-4.1-nano\",\n    selection_model: str = \"claude-opus-4-7\",\n    embed_dim: int = 256,\n    embed_model: str = \"text-embedding-3-small\",\n    max_tokens_ceiling: int = 500,\n    dispatch: Literal[\"auto\", \"sync\", \"batch\"] = \"auto\",\n    repo_root: str | None = None,     # M18: gate-SFT emission; defaults to cwd\n    function_id: str = \"\",            # M18: gate-SFT provenance\n) -> MLDResult:\n    \"\"\"Find the V2 minimum lossless description for `input_text`.\n\n    Pipeline phases:\n      1. Generate \u2014 n_calls parallel three-draft calls (sync) or batch dispatch.\n      2. Pre-filter \u2014 non-empty, no control chars, imperative start.\n      3. Textual degeneracy \u2014 re-roll once if >50% byte-equal.\n      4. Embed \u2014 single batch call.\n      5. Geometric degeneracy \u2014 skip antipode if avg cosine > 0.999.\n      6. Antipode convergence \u2014 pick centroid-nearest + up to 4 alternatives.\n      7. Gate \u2014 Nano \"valid?\" call on centroid-nearest (with one retry).\n      8. Selection \u2014 Opus selects when gate is ambiguous.\n      9. Confidence \u2014 derived from convergence and degeneracy flags.\n\n    The `dispatch` parameter routes generation:\n      - \"sync\"  \u2192 always use the async parallel path (original v0.1 behaviour)\n      - \"batch\" \u2192 always use OpenAI Batch API\n      - \"auto\"  \u2192 batch when n_calls >= 10 (DP4), else sync\n    \"\"\"\n    api.reset_cost_totals()\n\n    # KE1: prompt_text overrides the prompt parameter when provided.\n    effective_prompt = prompt_text if prompt_text is not None else prompt\n\n    # A1-Task2: gate_prompt_text overrides the built-in _GATE_PROMPT when provided.\n    effective_gate_prompt = gate_prompt_text if gate_prompt_text is not None else _GATE_PROMPT\n\n    # -----------------------------------------------------------------------\n    # Phase 1 \u2014 Generate\n    # -----------------------------------------------------------------------\n    user_content = f\"target_n={target_n}\\n\\n{input_text}\"\n\n    use_batch = should_use_batch(n_calls, dispatch)\n\n    if use_batch:\n        # Batch path: submit all n_calls as one batch job and convert results.\n        gen_requests = _make_generation_requests(\n            effective_prompt, user_content, generation_model,\n            max_tokens_ceiling, n_calls,\n        )\n        batch_results = run_batch_generation(gen_requests)\n        first_pass = _batch_results_to_call_results(batch_results, n_calls)\n    else:\n        # Sync path: async parallel generation (original v0.1 behaviour).\n        first_pass = await asyncio.gather(*[\n            _three_draft_call(\n                prompt=effective_prompt,\n                content=user_content,\n                model=generation_model,\n                temperature=temperature,\n                max_tokens=max_tokens_ceiling,\n            )\n            for _ in range(n_calls)\n        ])\n\n    triples, structured_failures = _drafts_from_calls(first_pass)\n\n    deduped = False\n\n    # -----------------------------------------------------------------------\n    # Phase 2 \u2014 Pre-filter\n    # -----------------------------------------------------------------------\n    raw_texts = [t[0] for t in triples]\n    passing_texts, rejected_texts = _pre_filter(raw_texts)\n    passing_set = set(id(s) for s in passing_texts)\n    # Rebuild (text, tier, call_idx) triples for passers, in original order.\n    rejected_count = len(rejected_texts) + structured_failures\n    passing_triples: list[tuple[str, str, int]] = []\n    # Use a multiset-aware match: build counter of passing texts and consume.\n    pass_counter: Counter = Counter(passing_texts)\n    for text, tier, call_idx in triples:\n        if pass_counter.get(text, 0) > 0:\n            passing_triples.append((text, tier, call_idx))\n            pass_counter[text] -= 1\n\n    # -----------------------------------------------------------------------\n    # Phase 3 \u2014 Textual degeneracy pre-check (>50% byte-equal)\n    # -----------------------------------------------------------------------\n    if passing_triples:\n        text_counter = Counter(t[0] for t in passing_triples)\n        most_common_text, most_common_count = text_counter.most_common(1)[0]\n        if most_common_count / len(passing_triples) > 0.5:\n            # Re-roll once \u2014 always use sync path for re-roll.\n            deduped = True\n            second_pass = await asyncio.gather(*[\n                _three_draft_call(\n                    prompt=effective_prompt,\n                    content=user_content,\n                    model=generation_model,\n                    temperature=temperature,\n                    max_tokens=max_tokens_ceiling,\n                )\n                for _ in range(n_calls)\n            ])\n            extra_triples, extra_failures = _drafts_from_calls(second_pass)\n            extra_raw = [t[0] for t in extra_triples]\n            extra_pass, extra_rej = _pre_filter(extra_raw)\n            rejected_count += len(extra_rej) + extra_failures\n            extra_pass_counter: Counter = Counter(extra_pass)\n            for text, tier, call_idx in extra_triples:\n                if extra_pass_counter.get(text, 0) > 0:\n                    # Tag with offset call_idx so it does not collide.\n                    passing_triples.append((text, tier, call_idx + n_calls))\n                    extra_pass_counter[text] -= 1\n            triples.extend(extra_triples)\n\n    # Track totals for the report.\n    candidates_generated = len(triples)\n    candidates_filtered = rejected_count\n\n    # -----------------------------------------------------------------------\n    # Early exit if nothing survived the pre-filter\n    # -----------------------------------------------------------------------\n    if not passing_triples:\n        return MLDResult(\n            description=\"\",\n            target_n=target_n,\n            natural_length=0,\n            confidence=0.0,\n            converged=False,\n            candidates_generated=candidates_generated,\n            candidates_filtered=candidates_filtered,\n            gate_verdict=\"ambiguous\",\n            gate_reason=\"no candidates passed the mechanical pre-filter\",\n            selection_rationale=\"no candidates available for gate or selection\",\n            targets_tried=[target_n],\n            degenerate_pre_check=False,\n            deduped=deduped,\n            cost=api.cost_totals(),\n        )\n\n    # -----------------------------------------------------------------------\n    # Phase 4 \u2014 Embed (single batch)\n    # -----------------------------------------------------------------------\n    passing_text_list = [t[0] for t in passing_triples]\n    raw_embeddings = await api.embed_text(\n        passing_text_list, dim=embed_dim, model=embed_model,\n    )\n    cloud = np.asarray(raw_embeddings, dtype=np.float64)\n\n    # -----------------------------------------------------------------------\n    # Phase 5 \u2014 Geometric degeneracy (avg off-diagonal cosine > 0.999)\n    # -----------------------------------------------------------------------\n    degenerate_pre_check = False\n    converged = False\n    nearest_idx = 0\n    alt_indices: list[int] = []\n\n    if cloud.shape[0] >= 2:\n        sim = _cosine_sim_matrix(cloud)\n        avg_sim = _avg_offdiag(sim)\n        if avg_sim > 0.999:\n            degenerate_pre_check = True\n            # Pick centroid-nearest only; skip antipode.\n            centroid = cloud.mean(axis=0)\n            distances = np.linalg.norm(cloud - centroid, axis=1)\n            nearest_idx = int(distances.argmin())\n            converged = False\n            alt_indices = []\n\n    # -----------------------------------------------------------------------\n    # Phase 6 \u2014 Convergence verdict via per-draft spherical variance\n    # -----------------------------------------------------------------------\n    if not degenerate_pre_check and cloud.shape[0] >= 3:\n        from pidgin.pipeline.geometry import convergence_verdict as _convergence_verdict\n        _d1_vecs = [cloud[i].tolist() for i, t in enumerate(passing_triples) if t[1] == \"d1\"]\n        _d2_vecs = [cloud[i].tolist() for i, t in enumerate(passing_triples) if t[1] == \"d2\"]\n        _d3_vecs = [cloud[i].tolist() for i, t in enumerate(passing_triples) if t[1] == \"d3\"]\n        if _d1_vecs and _d2_vecs and _d3_vecs:\n            _cverd, _ = _convergence_verdict(_d1_vecs, _d2_vecs, _d3_vecs)\n            converged = _cverd == \"CONVERGED\"\n        else:\n            converged = False\n        # Pick centroid-nearest for gate and alternatives.\n        _centroid_vec = cloud.mean(axis=0)\n        _distances = np.linalg.norm(cloud - _centroid_vec, axis=1)\n        nearest_idx = int(_distances.argmin())\n        alt_indices = _pick_alternatives(cloud, passing_triples, nearest_idx, target_n)\n    elif not degenerate_pre_check and cloud.shape[0] < 3:\n        # Sparse cloud: cannot run a meaningful antipode test. Pick the\n        # centroid-nearest (or the lone candidate) and proceed to gate.\n        if cloud.shape[0] >= 1:\n            centroid = cloud.mean(axis=0)\n            distances = np.linalg.norm(cloud - centroid, axis=1)\n            nearest_idx = int(distances.argmin())\n        converged = False\n        alt_indices = []\n\n    centroid_nearest_text = passing_triples[nearest_idx][0]\n\n    # -----------------------------------------------------------------------\n    # Phase 7 \u2014 Gate\n    # -----------------------------------------------------------------------\n    # M18: resolve repo_root for gate-SFT emission.\n    import os as _os\n    _sft_root = repo_root if repo_root is not None else _os.getcwd()\n\n    gate_verdict, gate_reason = await _gate_call(\n        input_text, centroid_nearest_text, gate_model,\n        gate_prompt=effective_gate_prompt,\n    )\n\n    # M18: emit gate-SFT record for primary gate call.\n    try:\n        from pidgin.store import sft as _sft\n        _sft.append(_sft_root, [_sft.SFTRecord(\n            type=\"gate\",\n            prompt=centroid_nearest_text,\n            label=\"valid\" if gate_verdict == \"valid\" else \"invalid\",\n            reason=gate_reason,\n            function_id=function_id,\n        )])\n    except Exception:\n        pass  # gate-SFT emission is non-fatal\n\n    description = centroid_nearest_text\n    selection_rationale = gate_reason\n\n    if gate_verdict == \"valid\":\n        # Accept centroid-nearest. Skip selection.\n        pass\n    elif gate_verdict == \"invalid\":\n        # Try one alternative \u2014 shortest-above-target_n/2 in a different tier.\n        nearest_tier = passing_triples[nearest_idx][1]\n        half = max(1, target_n // 2)\n        retry_pool = [\n            i for i in alt_indices\n            if (\n                passing_triples[i][1] != nearest_tier\n                and len(passing_triples[i][0].split()) > half\n            )\n        ]\n        if retry_pool:\n            # Shortest among that pool (above target_n/2, by word count).\n            retry_idx = min(\n                retry_pool,\n                key=lambda i: len(passing_triples[i][0].split()),\n            )\n            retry_text = passing_triples[retry_idx][0]\n            retry_verdict, retry_reason = await _gate_call(\n                input_text, retry_text, gate_model,\n                gate_prompt=effective_gate_prompt,\n            )\n            # M18: emit gate-SFT record for retry gate call.\n            try:\n                _sft.append(_sft_root, [_sft.SFTRecord(\n                    type=\"gate\",\n                    prompt=retry_text,\n                    label=\"valid\" if retry_verdict == \"valid\" else \"invalid\",\n                    reason=retry_reason,\n                    function_id=function_id,\n                )])\n            except Exception:\n                pass  # gate-SFT emission is non-fatal\n            if retry_verdict == \"valid\":\n                description = retry_text\n                gate_verdict = \"valid\"\n                gate_reason = retry_reason\n                selection_rationale = retry_reason\n            else:\n                # Retry did not pass \u2014 gate verdict is ambiguous overall.\n                gate_verdict = \"ambiguous\"\n                gate_reason = (\n                    f\"primary invalid: {gate_reason}; \"\n                    f\"retry {retry_verdict}: {retry_reason}\"\n                )\n                selection_rationale = gate_reason\n        else:\n            # No suitable alternative \u2014 escalate to ambiguous so Opus picks.\n            gate_verdict = \"ambiguous\"\n            selection_rationale = gate_reason\n    # gate_verdict == \"ambiguous\" \u2014 fall through to selection.\n\n    # -----------------------------------------------------------------------\n    # Phase 8 \u2014 Selection (Opus) when gate is ambiguous\n    # -----------------------------------------------------------------------\n    if gate_verdict == \"ambiguous\":\n        centroid = cloud.mean(axis=0)\n        distances = np.linalg.norm(cloud - centroid, axis=1)\n        candidate_indices = [nearest_idx] + [\n            i for i in alt_indices if i != nearest_idx\n        ]\n        # Cap to nearest + 4 alternatives.\n        candidate_indices = candidate_indices[:5]\n\n        candidates_payload = []\n        for slot, i in enumerate(candidate_indices):\n            text, tier, _ = passing_triples[i]\n            candidates_payload.append({\n                \"index\": slot,\n                \"text\": text,\n                \"tier\": tier,\n                \"dist_to_centroid\": float(distances[i]),\n                \"length\": len(text.split()),\n            })\n\n        selection_content = _json.dumps({\n            \"function_or_text\": input_text,\n            \"candidates\": candidates_payload,\n        })\n\n        try:\n            sel_result = await api.generate(\n                prompt=_SELECT_PROMPT,\n                content=selection_content,\n                model=selection_model,\n                temperature=0.0,\n                max_tokens=512,\n                schema=_SELECT_SCHEMA,\n                n=1,\n            )\n            if isinstance(sel_result, list):\n                sel_result = sel_result[0] if sel_result else None\n            structured = getattr(sel_result, \"structured\", None)\n            if isinstance(structured, dict):\n                selected_index = structured.get(\"selected_index\", 0)\n                try:\n                    selected_index = int(selected_index)\n                except Exception:\n                    selected_index = 0\n                if selected_index < 0 or selected_index >= len(candidate_indices):\n                    selected_index = 0\n                description = passing_triples[candidate_indices[selected_index]][0]\n                selection_rationale = str(structured.get(\"rationale\", \"\")) or selection_rationale\n            else:\n                # Selection produced no structured output; keep centroid-nearest.\n                selection_rationale = (\n                    f\"{gate_reason}; selection returned no structured output\"\n                )\n        except Exception as exc:\n            selection_rationale = (\n                f\"{gate_reason}; selection call failed: {exc!r}\"\n            )\n\n    # -----------------------------------------------------------------------\n    # Phase 9 \u2014 Confidence\n    # -----------------------------------------------------------------------\n    confidence = 1.0 if converged else 0.5\n    if degenerate_pre_check:\n        confidence -= 0.1\n    if deduped:\n        confidence -= 0.1\n    if gate_verdict == \"ambiguous\":\n        confidence -= 0.2\n    confidence = max(0.0, min(1.0, confidence))\n\n    cost_snapshot = api.cost_totals()\n\n    return MLDResult(\n        description=description,\n        target_n=target_n,\n        natural_length=len(description.split()),\n        confidence=confidence,\n        converged=converged,\n        candidates_generated=candidates_generated,\n        candidates_filtered=candidates_filtered,\n        gate_verdict=gate_verdict,\n        gate_reason=gate_reason,\n        selection_rationale=selection_rationale,\n        targets_tried=[target_n],\n        degenerate_pre_check=degenerate_pre_check,\n        deduped=deduped,\n        cost=cost_snapshot,\n    )\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Generate minimum lossless description of mld function.", "label": "positive", "reason": "", "function_id": "39593a9d7f9d8bea", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:44.340017+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"find_project_root\" line_start=\"17\">\n[CDATA-OPEN]\n            \"\"\"Walk up from start_dir to find the nearest directory containing .git.\n\n    Returns the directory path as a string, or None if no .git found.\n    \"\"\"\n    current = Path(start_dir).resolve()\n    for directory in [current] + list(current.parents):\n        if (directory / \".git\").exists():\n            return str(directory)\n    return None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Find the nearest directory with a .git folder by walking up from start_dir.", "label": "positive", "reason": "", "function_id": "57d08cd10f741bf6", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:44.340253+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"find_project_root\" line_start=\"17\">\n[CDATA-OPEN]\n            \"\"\"Walk up from start_dir to find the nearest directory containing .git.\n\n    Returns the directory path as a string, or None if no .git found.\n    \"\"\"\n    current = Path(start_dir).resolve()\n    for directory in [current] + list(current.parents):\n        if (directory / \".git\").exists():\n            return str(directory)\n    return None\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Locate the root directory containing '.git' starting from a given directory.", "label": "negative", "reason": "", "function_id": "57d08cd10f741bf6", "centroid_distance": 0.11048871260833604}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:50.537373+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"is_git_repo\" line_start=\"29\">\n[CDATA-OPEN]\n            \"\"\"Return True if project_root is inside a git repository.\n\n    Runs `git rev-parse --git-dir` in project_root; returns True on exit 0.\n    \"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", project_root, \"rev-parse\", \"--git-dir\"],\n        capture_output=True,\n        text=True,\n    )\n    return result.returncode == 0\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Check if the directory is a git repository by running 'git rev-parse --git-dir' inside project_root and return true if the command succeeds.", "label": "positive", "reason": "", "function_id": "869494a3e337f64c", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:50.537607+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"is_git_repo\" line_start=\"29\">\n[CDATA-OPEN]\n            \"\"\"Return True if project_root is inside a git repository.\n\n    Runs `git rev-parse --git-dir` in project_root; returns True on exit 0.\n    \"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", project_root, \"rev-parse\", \"--git-dir\"],\n        capture_output=True,\n        text=True,\n    )\n    return result.returncode == 0\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Check if the directory is inside a Git repository by running `git rev-parse --git-dir` and returning true if it succeeds.", "label": "negative", "reason": "", "function_id": "869494a3e337f64c", "centroid_distance": 0.13991544934403477}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:58.729968+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"git_commit_files\" line_start=\"42\">\n[CDATA-OPEN]\n            project_root: str,\n    paths: list[str],\n    message: str,\n) -> str:\n    \"\"\"Stage `paths` and create one commit in `project_root`.\n\n    All paths must be under project_root. Raises ValueError if any path\n    escapes the root. Raises RuntimeError on git add/commit failure.\n\n    Returns the full commit SHA of the resulting commit.\n    \"\"\"\n    root = Path(project_root).resolve()\n\n    # Security: reject paths that escape project_root.\n    for p in paths:\n        resolved = Path(p).resolve()\n        try:\n            resolved.relative_to(root)\n        except ValueError:\n            raise ValueError(\n                f\"Path {p!r} is not under project_root {project_root!r}\"\n            )\n\n    # Stage all files in one call.\n    add_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"add\", \"--\"] + paths,\n        capture_output=True,\n        text=True,\n    )\n    if add_result.returncode != 0:\n        raise RuntimeError(\n            f\"git add failed: {add_result.stderr.strip()}\"\n        )\n\n    # Commit.\n    commit_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"commit\", \"-m\", message],\n        capture_output=True,\n        text=True,\n    )\n    if commit_result.returncode != 0:\n        raise RuntimeError(\n            f\"git commit failed: {commit_result.stderr.strip()}\"\n        )\n\n    # Return full SHA of the new commit.\n    sha_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=True,\n    )\n    return sha_result.stdout.strip()\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Stage files with git add, commit with a message, and return the commit SHA.", "label": "positive", "reason": "", "function_id": "7b93c7c07f712033", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:01:58.730208+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/git.py\" name=\"git_commit_files\" line_start=\"42\">\n[CDATA-OPEN]\n            project_root: str,\n    paths: list[str],\n    message: str,\n) -> str:\n    \"\"\"Stage `paths` and create one commit in `project_root`.\n\n    All paths must be under project_root. Raises ValueError if any path\n    escapes the root. Raises RuntimeError on git add/commit failure.\n\n    Returns the full commit SHA of the resulting commit.\n    \"\"\"\n    root = Path(project_root).resolve()\n\n    # Security: reject paths that escape project_root.\n    for p in paths:\n        resolved = Path(p).resolve()\n        try:\n            resolved.relative_to(root)\n        except ValueError:\n            raise ValueError(\n                f\"Path {p!r} is not under project_root {project_root!r}\"\n            )\n\n    # Stage all files in one call.\n    add_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"add\", \"--\"] + paths,\n        capture_output=True,\n        text=True,\n    )\n    if add_result.returncode != 0:\n        raise RuntimeError(\n            f\"git add failed: {add_result.stderr.strip()}\"\n        )\n\n    # Commit.\n    commit_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"commit\", \"-m\", message],\n        capture_output=True,\n        text=True,\n    )\n    if commit_result.returncode != 0:\n        raise RuntimeError(\n            f\"git commit failed: {commit_result.stderr.strip()}\"\n        )\n\n    # Return full SHA of the new commit.\n    sha_result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=True,\n    )\n    return sha_result.stdout.strip()\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Stage the given file paths in the specified project root, create a commit with the message, and return the SHA.", "label": "negative", "reason": "", "function_id": "7b93c7c07f712033", "centroid_distance": 0.17178104845260556}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:08.616527+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"walk_source_files\" line_start=\"41\">\n[CDATA-OPEN]\n            root: str,\n    *,\n    exclude_globs: \"list[str] | None\" = None,\n) -> \"Iterator[Path]\":\n    \"\"\"Yield Path objects for every file under root that:\n      - has an extension in SUPPORTED_EXTENSIONS,\n      - is not inside a directory whose basename is in SKIP_DIRS,\n      - is not under a child .git boundary (any directory containing\n        .git that is NOT root itself),\n      - does not match any of the exclude_globs (glob matched against\n        the path relative to root).\n\n    CF4: If root is a single file (not a directory), os.walk yields nothing.\n    Guard added: when root is a file, yield it directly if it passes the\n    extension and exclude_globs filters.\n    \"\"\"\n    root_p = Path(root).resolve()\n    exclude_globs = exclude_globs or []\n\n    # CF4: single-file guard \u2014 os.walk on a file yields nothing.\n    if root_p.is_file():\n        if root_p.suffix in SUPPORTED_EXTENSIONS:\n            rel = root_p.name\n            if not any(fnmatch.fnmatch(rel, g) for g in exclude_globs):\n                yield root_p\n        return\n\n    for dirpath, dirnames, filenames in os.walk(root_p):\n        dp = Path(dirpath).resolve()\n        # Prune skip-dir children.\n        dirnames[:] = [\n            d for d in dirnames\n            if d not in SKIP_DIRS\n            and not (\n                dp != root_p and (Path(dirpath, d) / \".git\").exists()\n            )\n        ]\n        # Detect child-git at the dirpath itself.\n        if dp != root_p and (dp / \".git\").exists():\n            dirnames[:] = []\n            continue\n        for fn in filenames:\n            p = Path(dirpath, fn)\n            if p.suffix not in SUPPORTED_EXTENSIONS:\n                continue\n            rel = str(p.relative_to(root_p))\n            if any(fnmatch.fnmatch(rel, g) for g in exclude_globs):\n                continue\n            yield p\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Find files under root with supported extensions, not in skip dirs or inside a .git, not matching exclude globs, yield paths.", "label": "positive", "reason": "", "function_id": "388a43b2fbcffdd1", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:08.616777+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"walk_source_files\" line_start=\"41\">\n[CDATA-OPEN]\n            root: str,\n    *,\n    exclude_globs: \"list[str] | None\" = None,\n) -> \"Iterator[Path]\":\n    \"\"\"Yield Path objects for every file under root that:\n      - has an extension in SUPPORTED_EXTENSIONS,\n      - is not inside a directory whose basename is in SKIP_DIRS,\n      - is not under a child .git boundary (any directory containing\n        .git that is NOT root itself),\n      - does not match any of the exclude_globs (glob matched against\n        the path relative to root).\n\n    CF4: If root is a single file (not a directory), os.walk yields nothing.\n    Guard added: when root is a file, yield it directly if it passes the\n    extension and exclude_globs filters.\n    \"\"\"\n    root_p = Path(root).resolve()\n    exclude_globs = exclude_globs or []\n\n    # CF4: single-file guard \u2014 os.walk on a file yields nothing.\n    if root_p.is_file():\n        if root_p.suffix in SUPPORTED_EXTENSIONS:\n            rel = root_p.name\n            if not any(fnmatch.fnmatch(rel, g) for g in exclude_globs):\n                yield root_p\n        return\n\n    for dirpath, dirnames, filenames in os.walk(root_p):\n        dp = Path(dirpath).resolve()\n        # Prune skip-dir children.\n        dirnames[:] = [\n            d for d in dirnames\n            if d not in SKIP_DIRS\n            and not (\n                dp != root_p and (Path(dirpath, d) / \".git\").exists()\n            )\n        ]\n        # Detect child-git at the dirpath itself.\n        if dp != root_p and (dp / \".git\").exists():\n            dirnames[:] = []\n            continue\n        for fn in filenames:\n            p = Path(dirpath, fn)\n            if p.suffix not in SUPPORTED_EXTENSIONS:\n                continue\n            rel = str(p.relative_to(root_p))\n            if any(fnmatch.fnmatch(rel, g) for g in exclude_globs):\n                continue\n            yield p\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Traverse directory or handle a single file, filter by extension and exclude patterns, then yield matching Path objects.", "label": "negative", "reason": "", "function_id": "388a43b2fbcffdd1", "centroid_distance": 0.22525732830477008}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:20.002067+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"file_type_breakdown\" line_start=\"93\">\n[CDATA-OPEN]\n            root: str,\n    *,\n    exclude_globs: \"list[str] | None\" = None,\n) -> \"dict[str, int]\":\n    \"\"\"Return {extension: count} for files that pass the filter, plus\n    {'__skipped_dirs': N} as a sentinel summary.\"\"\"\n    counts: dict = {}\n    skipped_dirs = 0\n\n    root_p = Path(root).resolve()\n    exclude_globs_list = exclude_globs or []\n\n    for dirpath, dirnames, filenames in os.walk(root_p):\n        dp = Path(dirpath).resolve()\n        # Count and prune skip-dir children.\n        pruned: list[str] = []\n        for d in dirnames:\n            child_dp = Path(dirpath, d)\n            if d in SKIP_DIRS or (dp != root_p and (child_dp / \".git\").exists()):\n                skipped_dirs += 1\n            else:\n                pruned.append(d)\n        dirnames[:] = pruned\n\n        # Detect child-git at the dirpath itself.\n        if dp != root_p and (dp / \".git\").exists():\n            dirnames[:] = []\n            continue\n\n        for fn in filenames:\n            p = Path(dirpath, fn)\n            if p.suffix not in SUPPORTED_EXTENSIONS:\n                continue\n            rel = str(p.relative_to(root_p))\n            if any(fnmatch.fnmatch(rel, g) for g in exclude_globs_list):\n                continue\n            counts[p.suffix] = counts.get(p.suffix, 0) + 1\n\n    counts[\"__skipped_dirs\"] = skipped_dirs\n    return counts\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Count supported file extensions in directory tree, excluding skipped dirs and globs.", "label": "positive", "reason": "", "function_id": "0935b2b94a04cb93", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:20.002176+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"file_type_breakdown\" line_start=\"93\">\n[CDATA-OPEN]\n            root: str,\n    *,\n    exclude_globs: \"list[str] | None\" = None,\n) -> \"dict[str, int]\":\n    \"\"\"Return {extension: count} for files that pass the filter, plus\n    {'__skipped_dirs': N} as a sentinel summary.\"\"\"\n    counts: dict = {}\n    skipped_dirs = 0\n\n    root_p = Path(root).resolve()\n    exclude_globs_list = exclude_globs or []\n\n    for dirpath, dirnames, filenames in os.walk(root_p):\n        dp = Path(dirpath).resolve()\n        # Count and prune skip-dir children.\n        pruned: list[str] = []\n        for d in dirnames:\n            child_dp = Path(dirpath, d)\n            if d in SKIP_DIRS or (dp != root_p and (child_dp / \".git\").exists()):\n                skipped_dirs += 1\n            else:\n                pruned.append(d)\n        dirnames[:] = pruned\n\n        # Detect child-git at the dirpath itself.\n        if dp != root_p and (dp / \".git\").exists():\n            dirnames[:] = []\n            continue\n\n        for fn in filenames:\n            p = Path(dirpath, fn)\n            if p.suffix not in SUPPORTED_EXTENSIONS:\n                continue\n            rel = str(p.relative_to(root_p))\n            if any(fnmatch.fnmatch(rel, g) for g in exclude_globs_list):\n                continue\n            counts[p.suffix] = counts.get(p.suffix, 0) + 1\n\n    counts[\"__skipped_dirs\"] = skipped_dirs\n    return counts\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Count supported files, track skipped dirs, handle excludes.", "label": "negative", "reason": "", "function_id": "0935b2b94a04cb93", "centroid_distance": 0.18740800843271666}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:25.999928+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"FunctionRecord\" line_start=\"138\">\n[CDATA-OPEN]\n            \"\"\"Hold extracted function name, signature, body, line range, module path, visibility, and language.\"\"\"\n    name: str\n    signature: str\n    body: str\n    line_range: tuple\n    module: str\n    visibility: str\n    language: str\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Describe a data structure that stores function metadata, including name, signature, body, line range, module, visibility, and language.", "label": "positive", "reason": "", "function_id": "fbbb252fa74d5563", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:26.000040+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"FunctionRecord\" line_start=\"138\">\n[CDATA-OPEN]\n            \"\"\"Hold extracted function name, signature, body, line range, module path, visibility, and language.\"\"\"\n    name: str\n    signature: str\n    body: str\n    line_range: tuple\n    module: str\n    visibility: str\n    language: str\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Store function info: name, signature, body, lines, module, visibility, language.", "label": "negative", "reason": "", "function_id": "fbbb252fa74d5563", "centroid_distance": 0.158863502495305}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:31.948321+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"detect_language\" line_start=\"163\">\n[CDATA-OPEN]\n            \"\"\"Return language name from file extension or raise ValueError on unknown extension.\"\"\"\n    p = Path(path)\n    ext = p.suffix.lower()\n    if ext not in _EXT_MAP:\n        raise ValueError(f\"Unknown language for extension {ext!r} (path: {path})\")\n    return _EXT_MAP[ext]\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Check the file extension of the given path, convert it to lowercase, and return the corresponding language name from the map or raise ValueError if unknown.", "label": "positive", "reason": "", "function_id": "3f530c76475e42a8", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:31.948434+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"detect_language\" line_start=\"163\">\n[CDATA-OPEN]\n            \"\"\"Return language name from file extension or raise ValueError on unknown extension.\"\"\"\n    p = Path(path)\n    ext = p.suffix.lower()\n    if ext not in _EXT_MAP:\n        raise ValueError(f\"Unknown language for extension {ext!r} (path: {path})\")\n    return _EXT_MAP[ext]\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extract the suffix from the path, normalize case, look up the language in the extension map, or raise an error if not found.", "label": "negative", "reason": "", "function_id": "3f530c76475e42a8", "centroid_distance": 0.2160094087326626}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:40.656460+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_python\" line_start=\"173\">\n[CDATA-OPEN]\n            \"\"\"Extract top-level function and class records from Python source via AST.\n\n    Two-pass extraction: module-level functions/classes first, then public\n    methods within each class body.  Class methods are qualified as\n    ClassName.method_name so identities are unique across classes.\n    \"\"\"\n    try:\n        tree = ast.parse(source)\n    except SyntaxError:\n        return []\n\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n\n    for node in ast.iter_child_nodes(tree):\n        # Module-level functions\n        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n            rec = _python_record(node, lines, module_path, class_name=None)\n            if rec is not None:\n                records.append(rec)\n        # Classes: record the class itself, then extract public methods\n        elif isinstance(node, ast.ClassDef):\n            class_rec = _python_record(node, lines, module_path, class_name=None)\n            if class_rec is not None:\n                records.append(class_rec)\n            for child in ast.iter_child_nodes(node):\n                if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):\n                    method_rec = _python_record(\n                        child, lines, module_path,\n                        class_name=node.name,\n                    )\n                    if method_rec is not None:\n                        records.append(method_rec)\n\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse Python source to extract top-level functions and classes, then find public methods within classes.", "label": "positive", "reason": "", "function_id": "114b5f882bfa302e", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:40.656688+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_python\" line_start=\"173\">\n[CDATA-OPEN]\n            \"\"\"Extract top-level function and class records from Python source via AST.\n\n    Two-pass extraction: module-level functions/classes first, then public\n    methods within each class body.  Class methods are qualified as\n    ClassName.method_name so identities are unique across classes.\n    \"\"\"\n    try:\n        tree = ast.parse(source)\n    except SyntaxError:\n        return []\n\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n\n    for node in ast.iter_child_nodes(tree):\n        # Module-level functions\n        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n            rec = _python_record(node, lines, module_path, class_name=None)\n            if rec is not None:\n                records.append(rec)\n        # Classes: record the class itself, then extract public methods\n        elif isinstance(node, ast.ClassDef):\n            class_rec = _python_record(node, lines, module_path, class_name=None)\n            if class_rec is not None:\n                records.append(class_rec)\n            for child in ast.iter_child_nodes(node):\n                if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):\n                    method_rec = _python_record(\n                        child, lines, module_path,\n                        class_name=node.name,\n                    )\n                    if method_rec is not None:\n                        records.append(method_rec)\n\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Uses AST to parse source, captures module functions, classes, and public methods, creating unique records with qualified identities.", "label": "negative", "reason": "", "function_id": "114b5f882bfa302e", "centroid_distance": 0.24422253840916985}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:45.233116+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_rust\" line_start=\"323\">\n[CDATA-OPEN]\n            \"\"\"Extract public function records from Rust source via regex.\"\"\"\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n    i = 0\n    while i < len(lines):\n        line = lines[i]\n        m = _RUST_ITEM.match(line)\n        if m:\n            name = m.group(\"name\")\n            visibility = \"public\" if (m.group(\"vis\") or \"\").strip().startswith(\"pub\") else \"private\"\n            body, end_idx = _collect_braced_body(lines, i)\n            records.append(FunctionRecord(\n                name=name,\n                signature=line,\n                body=body,\n                line_range=(i + 1, end_idx + 1),\n                module=module_path,\n                visibility=visibility,\n                language=\"rust\",\n            ))\n            i = end_idx + 1\n            continue\n        i += 1\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extracts public Rust functions using regex and collects their details.", "label": "positive", "reason": "", "function_id": "87ac39abac6ddebc", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:45.233355+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_rust\" line_start=\"323\">\n[CDATA-OPEN]\n            \"\"\"Extract public function records from Rust source via regex.\"\"\"\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n    i = 0\n    while i < len(lines):\n        line = lines[i]\n        m = _RUST_ITEM.match(line)\n        if m:\n            name = m.group(\"name\")\n            visibility = \"public\" if (m.group(\"vis\") or \"\").strip().startswith(\"pub\") else \"private\"\n            body, end_idx = _collect_braced_body(lines, i)\n            records.append(FunctionRecord(\n                name=name,\n                signature=line,\n                body=body,\n                line_range=(i + 1, end_idx + 1),\n                module=module_path,\n                visibility=visibility,\n                language=\"rust\",\n            ))\n            i = end_idx + 1\n            continue\n        i += 1\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Uses regex to find function signatures, determines visibility, and records function info.", "label": "negative", "reason": "", "function_id": "87ac39abac6ddebc", "centroid_distance": 0.1813402176224087}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:49.184474+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_typescript\" line_start=\"402\">\n[CDATA-OPEN]\n            \"\"\"Extract function and class records from TypeScript source via regex.\"\"\"\n    return _parse_ts_like(source, module_path, \"typescript\", skip_kinds=set())\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse TypeScript source and extract function and class records using regex.", "label": "positive", "reason": "", "function_id": "3fb162e7d7ede353", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:49.184697+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_typescript\" line_start=\"402\">\n[CDATA-OPEN]\n            \"\"\"Extract function and class records from TypeScript source via regex.\"\"\"\n    return _parse_ts_like(source, module_path, \"typescript\", skip_kinds=set())\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Parse TypeScript source code with regex to find functions and classes.", "label": "negative", "reason": "", "function_id": "3fb162e7d7ede353", "centroid_distance": 0.05545722775388506}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:55.375800+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_javascript\" line_start=\"407\">\n[CDATA-OPEN]\n            \"\"\"Extract function and class records from JavaScript source via regex.\"\"\"\n    return _parse_ts_like(source, module_path, \"javascript\",\n                          skip_kinds={\"interface\", \"type\"})\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extract JavaScript functions and classes from source code using regex patterns.", "label": "positive", "reason": "", "function_id": "bc888409a3b83cf6", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:02:55.375899+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_javascript\" line_start=\"407\">\n[CDATA-OPEN]\n            \"\"\"Extract function and class records from JavaScript source via regex.\"\"\"\n    return _parse_ts_like(source, module_path, \"javascript\",\n                          skip_kinds={\"interface\", \"type\"})\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Match functions and classes in JavaScript source with regex and extract records.", "label": "negative", "reason": "", "function_id": "bc888409a3b83cf6", "centroid_distance": 0.18005646873201775}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:02.141572+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_go\" line_start=\"422\">\n[CDATA-OPEN]\n            \"\"\"Extract exported function records from Go source via regex.\"\"\"\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n    i = 0\n    while i < len(lines):\n        line = lines[i]\n        m = _GO_FUNC.match(line) or _GO_TYPE.match(line)\n        if m:\n            name = m.group(\"name\")\n            visibility = \"public\"\n            # Collect braced body if type has struct definition.\n            if \"{\" in line or (i + 1 < len(lines) and \"{\" in lines[i + 1]):\n                body, end_idx = _collect_braced_body(lines, i)\n            else:\n                body = line\n                end_idx = i\n            records.append(FunctionRecord(\n                name=name,\n                signature=line,\n                body=body,\n                line_range=(i + 1, end_idx + 1),\n                module=module_path,\n                visibility=visibility,\n                language=\"go\",\n            ))\n            i = end_idx + 1\n            continue\n        i += 1\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extract function records from Go source code using regex and brace matching.", "label": "positive", "reason": "", "function_id": "192878ea3c3091f4", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:02.141689+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"parse_go\" line_start=\"422\">\n[CDATA-OPEN]\n            \"\"\"Extract exported function records from Go source via regex.\"\"\"\n    lines = source.splitlines()\n    records: list[FunctionRecord] = []\n    i = 0\n    while i < len(lines):\n        line = lines[i]\n        m = _GO_FUNC.match(line) or _GO_TYPE.match(line)\n        if m:\n            name = m.group(\"name\")\n            visibility = \"public\"\n            # Collect braced body if type has struct definition.\n            if \"{\" in line or (i + 1 < len(lines) and \"{\" in lines[i + 1]):\n                body, end_idx = _collect_braced_body(lines, i)\n            else:\n                body = line\n                end_idx = i\n            records.append(FunctionRecord(\n                name=name,\n                signature=line,\n                body=body,\n                line_range=(i + 1, end_idx + 1),\n                module=module_path,\n                visibility=visibility,\n                language=\"go\",\n            ))\n            i = end_idx + 1\n            continue\n        i += 1\n    return records\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extracts Go function and type definitions from source code.", "label": "negative", "reason": "", "function_id": "192878ea3c3091f4", "centroid_distance": 0.240173145799931}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:09.419645+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"extract_imports\" line_start=\"462\">\n[CDATA-OPEN]\n            \"\"\"Return import paths from source text for the given language.\"\"\"\n    if language == \"python\":\n        try:\n            tree = ast.parse(source)\n        except SyntaxError:\n            return []\n        imports: list[str] = []\n        for node in ast.walk(tree):\n            if isinstance(node, ast.Import):\n                for alias in node.names:\n                    imports.append(alias.name)\n            elif isinstance(node, ast.ImportFrom):\n                if node.module:\n                    imports.append(node.module)\n        return imports\n\n    if language in (\"typescript\", \"javascript\"):\n        return _TS_IMPORT.findall(source)\n\n    if language == \"rust\":\n        return [m.group(1) for m in _RUST_USE.finditer(source)]\n\n    if language == \"go\":\n        imports = list(_GO_IMPORT_SINGLE.findall(source))\n        for block in _GO_IMPORT_BLOCK.findall(source):\n            imports.extend(_GO_IMPORT_BLOCK_ITEM.findall(block))\n        return imports\n\n    return []\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extract import paths from source code based on language.", "label": "positive", "reason": "", "function_id": "8383c550712f0940", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:09.419890+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"extract_imports\" line_start=\"462\">\n[CDATA-OPEN]\n            \"\"\"Return import paths from source text for the given language.\"\"\"\n    if language == \"python\":\n        try:\n            tree = ast.parse(source)\n        except SyntaxError:\n            return []\n        imports: list[str] = []\n        for node in ast.walk(tree):\n            if isinstance(node, ast.Import):\n                for alias in node.names:\n                    imports.append(alias.name)\n            elif isinstance(node, ast.ImportFrom):\n                if node.module:\n                    imports.append(node.module)\n        return imports\n\n    if language in (\"typescript\", \"javascript\"):\n        return _TS_IMPORT.findall(source)\n\n    if language == \"rust\":\n        return [m.group(1) for m in _RUST_USE.finditer(source)]\n\n    if language == \"go\":\n        imports = list(_GO_IMPORT_SINGLE.findall(source))\n        for block in _GO_IMPORT_BLOCK.findall(source):\n            imports.extend(_GO_IMPORT_BLOCK_ITEM.findall(block))\n        return imports\n\n    return []\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Identifies language-specific import paths from source code for Python, TypeScript, JavaScript, Rust, and Go.", "label": "negative", "reason": "", "function_id": "8383c550712f0940", "centroid_distance": 0.1299758636217715}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:17.456418+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"build_import_graph\" line_start=\"495\">\n[CDATA-OPEN]\n            \"\"\"Build a dependency map from source file paths to their resolved imports.\"\"\"\n    project_root = Path(project_root).resolve()\n\n    # Index files by absolute path and lookup keys.\n    file_paths: list[Path] = [Path(f).resolve() for f in source_files]\n    path_set = set(file_paths)\n\n    # Build lookup tables by stem and dotted module path.\n    by_stem: dict[str, list[Path]] = {}\n    by_dotted: dict[str, Path] = {}\n    for p in file_paths:\n        by_stem.setdefault(p.stem, []).append(p)\n        try:\n            rel = p.relative_to(project_root).with_suffix(\"\")\n            dotted = \".\".join(rel.parts)\n            by_dotted[dotted] = p\n        except ValueError:\n            continue\n\n    graph: dict = {}\n\n    for p in file_paths:\n        try:\n            rel_key = str(p.relative_to(project_root))\n        except ValueError:\n            rel_key = str(p)\n\n        try:\n            src = p.read_text(encoding=\"utf-8\")\n        except Exception:\n            graph[rel_key] = set()\n            continue\n\n        try:\n            language = detect_language(p)\n        except ValueError:\n            graph[rel_key] = set()\n            continue\n\n        imports = extract_imports(src, language)\n        edges: set = set()\n\n        for imp in imports:\n            target = _resolve_import(imp, p, project_root, by_stem, by_dotted, path_set, language)\n            if target is not None:\n                try:\n                    edges.add(str(target.relative_to(project_root)))\n                except ValueError:\n                    edges.add(str(target))\n\n        graph[rel_key] = edges\n\n    return graph\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Builds a dependency graph from source files to their imports.", "label": "positive", "reason": "", "function_id": "77ad2443c709aa52", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:17.456661+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"build_import_graph\" line_start=\"495\">\n[CDATA-OPEN]\n            \"\"\"Build a dependency map from source file paths to their resolved imports.\"\"\"\n    project_root = Path(project_root).resolve()\n\n    # Index files by absolute path and lookup keys.\n    file_paths: list[Path] = [Path(f).resolve() for f in source_files]\n    path_set = set(file_paths)\n\n    # Build lookup tables by stem and dotted module path.\n    by_stem: dict[str, list[Path]] = {}\n    by_dotted: dict[str, Path] = {}\n    for p in file_paths:\n        by_stem.setdefault(p.stem, []).append(p)\n        try:\n            rel = p.relative_to(project_root).with_suffix(\"\")\n            dotted = \".\".join(rel.parts)\n            by_dotted[dotted] = p\n        except ValueError:\n            continue\n\n    graph: dict = {}\n\n    for p in file_paths:\n        try:\n            rel_key = str(p.relative_to(project_root))\n        except ValueError:\n            rel_key = str(p)\n\n        try:\n            src = p.read_text(encoding=\"utf-8\")\n        except Exception:\n            graph[rel_key] = set()\n            continue\n\n        try:\n            language = detect_language(p)\n        except ValueError:\n            graph[rel_key] = set()\n            continue\n\n        imports = extract_imports(src, language)\n        edges: set = set()\n\n        for imp in imports:\n            target = _resolve_import(imp, p, project_root, by_stem, by_dotted, path_set, language)\n            if target is not None:\n                try:\n                    edges.add(str(target.relative_to(project_root)))\n                except ValueError:\n                    edges.add(str(target))\n\n        graph[rel_key] = edges\n\n    return graph\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Processes source files to create a map from relative paths to imported modules' relative paths.", "label": "negative", "reason": "", "function_id": "77ad2443c709aa52", "centroid_distance": 0.18980476679589964}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:31.229707+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"extract_public_functions\" line_start=\"618\">\n[CDATA-OPEN]\n            \"\"\"Walk a path (file or directory) and return public function dicts.\n\n    Each dict has keys: name, file, body, line_start, line_end, module,\n    visibility, language, signature.\n\n    Only files with recognised extensions are parsed. Unknown extensions are\n    silently skipped. Private functions (visibility != 'public') are excluded.\n    The returned dicts are compatible with run_embedding_mld_pipeline_for_functions.\n    \"\"\"\n    root = Path(path).resolve()\n\n    # Collect candidate files.\n    if root.is_file():\n        candidates = [root]\n    else:\n        candidates = [\n            p for p in root.rglob(\"*\")\n            if p.is_file() and p.suffix.lower() in _EXT_MAP\n        ]\n\n    # Compute repo root once for stable relative module_path values.\n    # find_project_root walks up from root (or its parent for single files)\n    # to find the nearest .git boundary.  Falls back gracefully when no git\n    # repo exists.\n    _start_dir = str(root.parent if root.is_file() else root)\n    repo_root = find_project_root(_start_dir)\n    if repo_root:\n        repo_root_p = Path(repo_root)\n    else:\n        repo_root_p = root.parent if root.is_file() else root\n\n    results: list[dict] = []\n    for file_path in sorted(candidates):\n        try:\n            lang = detect_language(file_path)\n        except ValueError:\n            continue\n\n        parser_fn = _PARSERS.get(lang)\n        if parser_fn is None:\n            continue\n\n        try:\n            source = file_path.read_text(encoding=\"utf-8\")\n        except OSError:\n            continue\n\n        try:\n            module_path = str(file_path.relative_to(repo_root_p))\n        except ValueError:\n            module_path = str(file_path)\n\n        records: list[FunctionRecord] = parser_fn(source, module_path)\n        for rec in records:\n            if rec.visibility != \"public\":\n                continue\n            line_start, line_end = rec.line_range\n            results.append({\n                \"name\": rec.name,\n                \"file\": str(file_path),\n                \"body\": rec.body,\n                \"line_start\": line_start,\n                \"line_end\": line_end,\n                \"module\": rec.module,\n                \"visibility\": rec.visibility,\n                \"language\": rec.language,\n                \"signature\": rec.signature,\n            })\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Find all public functions in given files, extract their details, and list them.", "label": "positive", "reason": "", "function_id": "c506890464809b1d", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:31.229954+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"extract_public_functions\" line_start=\"618\">\n[CDATA-OPEN]\n            \"\"\"Walk a path (file or directory) and return public function dicts.\n\n    Each dict has keys: name, file, body, line_start, line_end, module,\n    visibility, language, signature.\n\n    Only files with recognised extensions are parsed. Unknown extensions are\n    silently skipped. Private functions (visibility != 'public') are excluded.\n    The returned dicts are compatible with run_embedding_mld_pipeline_for_functions.\n    \"\"\"\n    root = Path(path).resolve()\n\n    # Collect candidate files.\n    if root.is_file():\n        candidates = [root]\n    else:\n        candidates = [\n            p for p in root.rglob(\"*\")\n            if p.is_file() and p.suffix.lower() in _EXT_MAP\n        ]\n\n    # Compute repo root once for stable relative module_path values.\n    # find_project_root walks up from root (or its parent for single files)\n    # to find the nearest .git boundary.  Falls back gracefully when no git\n    # repo exists.\n    _start_dir = str(root.parent if root.is_file() else root)\n    repo_root = find_project_root(_start_dir)\n    if repo_root:\n        repo_root_p = Path(repo_root)\n    else:\n        repo_root_p = root.parent if root.is_file() else root\n\n    results: list[dict] = []\n    for file_path in sorted(candidates):\n        try:\n            lang = detect_language(file_path)\n        except ValueError:\n            continue\n\n        parser_fn = _PARSERS.get(lang)\n        if parser_fn is None:\n            continue\n\n        try:\n            source = file_path.read_text(encoding=\"utf-8\")\n        except OSError:\n            continue\n\n        try:\n            module_path = str(file_path.relative_to(repo_root_p))\n        except ValueError:\n            module_path = str(file_path)\n\n        records: list[FunctionRecord] = parser_fn(source, module_path)\n        for rec in records:\n            if rec.visibility != \"public\":\n                continue\n            line_start, line_end = rec.line_range\n            results.append({\n                \"name\": rec.name,\n                \"file\": str(file_path),\n                \"body\": rec.body,\n                \"line_start\": line_start,\n                \"line_end\": line_end,\n                \"module\": rec.module,\n                \"visibility\": rec.visibility,\n                \"language\": rec.language,\n                \"signature\": rec.signature,\n            })\n\n    return results\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Finds all public functions in files or directories, returning their metadata.", "label": "negative", "reason": "", "function_id": "c506890464809b1d", "centroid_distance": 0.23136331588660264}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:38.732457+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"ParameterMeta\" line_start=\"696\">\n[CDATA-OPEN]\n            \"\"\"Metadata for a single function parameter.\"\"\"\n    name: str\n    annotation: \"str | None\"\n    default: \"str | None\"        # repr of default literal, or None\n    kind: str                    # \"positional\" | \"keyword\" | \"vararg\" | \"kwarg\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define a data class ParameterMeta with attributes: name, annotation, default, and kind.", "label": "positive", "reason": "", "function_id": "8a15ef9c9d564fd8", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:38.732688+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"ParameterMeta\" line_start=\"696\">\n[CDATA-OPEN]\n            \"\"\"Metadata for a single function parameter.\"\"\"\n    name: str\n    annotation: \"str | None\"\n    default: \"str | None\"        # repr of default literal, or None\n    kind: str                    # \"positional\" | \"keyword\" | \"vararg\" | \"kwarg\"\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "ParameterMeta stores parameter name, annotation, default, and kind.", "label": "negative", "reason": "", "function_id": "8a15ef9c9d564fd8", "centroid_distance": 0.1441585189172857}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:44.687408+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"FunctionSignature\" line_start=\"705\">\n[CDATA-OPEN]\n            \"\"\"Full signature metadata extracted from a function or async function node.\"\"\"\n    name: str\n    parameters: \"list[ParameterMeta]\"\n    return_annotation: \"str | None\"\n    decorators: \"list[str]\"\n    line: int\n    is_async: bool\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Extracts full signature metadata from a function or async node, including name, parameters, return annotation, decorators, line number, and async flag.", "label": "positive", "reason": "", "function_id": "bd10589237107901", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:44.687528+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"FunctionSignature\" line_start=\"705\">\n[CDATA-OPEN]\n            \"\"\"Full signature metadata extracted from a function or async function node.\"\"\"\n    name: str\n    parameters: \"list[ParameterMeta]\"\n    return_annotation: \"str | None\"\n    decorators: \"list[str]\"\n    line: int\n    is_async: bool\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Gather function signature details such as name, parameters, return annotation, decorators, line, and async flag.", "label": "negative", "reason": "", "function_id": "bd10589237107901", "centroid_distance": 0.2267659198493016}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:49.381061+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"ImportMeta\" line_start=\"716\">\n[CDATA-OPEN]\n            \"\"\"Metadata for a single import statement.\"\"\"\n    kind: str                    # \"import\" | \"from\"\n    module: str\n    names: \"list[str]\"\n    line: int\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Define a class ImportMeta with attributes kind, module, names, and line for import statement metadata.", "label": "positive", "reason": "", "function_id": "d309d633df5e6afe", "centroid_distance": null}
{"version": 1, "type": "sft", "timestamp": "2026-04-28T23:03:49.381285+00:00", "prompt": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<pidgin_prompt verb=\"converge\" doc_type=\"function_description\" version=\"1\">\n  <role>\n    You are the MLD convergence generator. Produce three drafts at\n    increasing refinement that describe the function in the payload\n    using the minimum tokens to remain lossless.\n\n    You are a precision describer. You produce minimum-lossless descriptions\n    of code and prose. Your job: given a code snippet, return THREE drafts \u2014\n    each one sentence that describes what it DOES. No prose. No hedging.\n    No commentary. The three drafts are the whole output.\n  </role>\n\n  <principles></principles>\n\n  <rules>\n    <rule id=\"1\">Begin with an imperative verb first. \"Parse\", \"Load\", \"Return\", \"Compute\", \"Dispatch\", \"Embed\", \"Validate\". NOT \"It\", \"This\", \"The\", \"A\", \"Parses\", \"Loading\". The first token must be a base-form verb.</rule>\n    <rule id=\"2\">Concision target. The caller passes a target token count n. Aim for n tokens, accept up to n + 4 if needed for a complete sentence. Never truncate to fit the target. Never pad to reach it.</rule>\n    <rule id=\"3\">No hedging words. Reject: may, might, could, possibly, generally, typically, arguably, perhaps. If you do not know, do not write the description.</rule>\n    <rule id=\"4\">No filler words. Reject: basically, essentially, effectively, simply, just, really. These are tells of uncertainty. Origami does not hedge.</rule>\n    <rule id=\"5\">No passive voice. Reject forms like \"is parsed by\", \"was computed by\", \"been processed\". The function DOES; it is not done TO.</rule>\n    <rule id=\"6\">No copula leads. Reject: \"is\", \"was\", \"are\", \"were\", \"be\", \"been\", \"being\", \"has\", \"have\", \"had\" as the first word.</rule>\n    <rule id=\"7\">No truncation. If the sentence ends with a trailing comma, colon, semicolon, or conjunction (and, or, but, with, for, to) at end-of-string, the sentence is malformed. If the last word is a fragment or partial token (e.g., \"trunc\", \"dimens\", \"config,\"), the sentence is malformed. Write a sentence that FINISHES at the budget boundary, not one that runs off the end. Descriptions must be complete sentences.</rule>\n    <rule id=\"8\">Structured output. The caller enforces a JSON schema via API-level response_format or strict tool use. You do NOT format JSON yourself. Return the three drafts as the schema specifies.</rule>\n  </rules>\n\n  <concision_tiers>\n    <tier n=\"8\">The most compressed legal description. A single verb + 2-3 complements. No joining words. Example: \"Parse YAML config and return a Config dataclass.\" (8 tokens, at target). The sentence strategy is: ONE action, ONE object, ONE destination or return value. No subordinate clauses. No conjunctions.</tier>\n    <tier n=\"16\">Room for one subordinate clause or one compound object. Example: \"Parse CLI arguments, resolve text input, return an embedding vector.\" (11 tokens). Example: \"Embed input text via the OpenAI API and return the float vector as JSON.\" (15 tokens).</tier>\n    <tier n=\"24\">Primary verb + elaborated complement + one adjoining phrase. Room for compound actions: primary clause (8-12 tokens) + secondary clause (8-12 tokens). Do not pad with irrelevant elaboration. Example: \"Parse CLI arguments for text, file, dimension, and model, resolve the input source, then embed and write the vector to stdout.\" (22 tokens).</tier>\n    <tier n=\"32\">Full compound sentence with secondary information. Room for three clauses, an exception case or error path, or cross-service interactions named explicitly. Rare \u2014 use only for genuinely complex functions.</tier>\n  </concision_tiers>\n\n  <vocabulary>\n    <!-- Origami vocabulary: use domain-correct terms when the code references RF-Edge services. -->\n    <term name=\"Convoy\">Unified SDK and platform for edge CV deployment. The formation, not any single component.</term>\n    <term name=\"Forklift\">Heavy tiling operator. Zero-copy shared memory tile dispatch for high-throughput pipelines.</term>\n    <term name=\"HandTruck\">Light tiling operator. Same verb surface as Forklift for smaller payloads.</term>\n    <term name=\"Warehouse\">S3-compatible object storage with TTL lifecycle. Four bucket tiers.</term>\n    <term name=\"Manifest\">Hot config reload. Polls ETag at 1 Hz, applies changes, controlled flush on geometry changes.</term>\n    <term name=\"Catwalk\">Metrics SDK. Digest, Counter, Detection. Protobuf to Redis Stream to TimescaleDB.</term>\n    <term name=\"Dispatch\">Control plane interface. MCP endpoint for service management and agentic tool calls.</term>\n    <term name=\"Switchboard\">Control plane service. REST API and dashboard UI.</term>\n    <term name=\"Watchtower\">Metrics collector. Nine hardware collectors at 100 Hz. Rust binary. TimescaleDB persistence.</term>\n    <term name=\"Marionette\">Agentic SDK. MCP server for file, shell, git, mission dispatch. Sprint execution system.</term>\n    <term name=\"Chrony\">NTP time sync. Sub-millisecond clock tracking.</term>\n    <term name=\"Watch Glass\">GADF-based anomaly detection on Watchtower metrics. ResNet18 classifier. Phase 1 of Watch Stack.</term>\n    <term name=\"Prism\">Geometric monitoring. Convex hull health mesh, Z-buffer anomaly detection. Phase 2 of Watch Stack.</term>\n    <term name=\"Origami\">The design philosophy. Closed vocabularies, bounded action spaces, coordinator/executor split. The way.</term>\n    <term name=\"Technologic\">The judge. Enforces Daft Punk compliance: index currency, description quality, surface area, staleness.</term>\n  </vocabulary>\n\n  <examples>\n    <!-- Target n=8 positive examples -->\n    <example type=\"positive\" id=\"ex1\" target_n=\"8\" language=\"Python\">\n      <input>\ndef add(a, b):\n    return a + b\n      </input>\n      <output>Add two integers and return their numeric sum.</output>\n      <why>Imperative verb, no filler, complete sentence, exactly at target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex4\" target_n=\"8\" language=\"Python\">\n      <input>\ndef parse_config(path):\n    with open(path) as f:\n        return Config(**yaml.safe_load(f))\n      </input>\n      <output>Parse YAML config and return a Config dataclass.</output>\n      <why>Concrete noun (Config dataclass), imperative verb, no filler, exactly at n=8 target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex5\" target_n=\"8\" language=\"Rust\">\n      <input>\nfn clamp(v: f32, lo: f32, hi: f32) -> f32 {\n    v.max(lo).min(hi)\n}\n      </input>\n      <output>Clamp a float value between lo and hi.</output>\n      <why>Imperative verb, names the operation precisely, concrete noun (\"float value\"). Exactly at target n=8.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_simple\" target_n=\"8\" language=\"Python\">\n      <input>\ndef is_empty(lst):\n    return len(lst) == 0\n      </input>\n      <output>Return True if the list contains no elements.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_simple\" target_n=\"8\" language=\"Go\">\n      <input>\nfunc abs(x int) int {\n    if x &lt; 0 { return -x }\n    return x\n}\n      </input>\n      <output>Return the absolute value of a given integer.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_simple\" target_n=\"8\" language=\"TypeScript\">\n      <input>\nfunction toSnakeCase(s: string): string {\n    return s.replace(/([A-Z])/g, '_$1').toLowerCase().replace(/^_/, '');\n}\n      </input>\n      <output>Convert a camelCase identifier string into snake_case format.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_simple\" target_n=\"8\" language=\"JavaScript\">\n      <input>\nfunction sleep(ms) {\n    return new Promise(resolve =&gt; setTimeout(resolve, ms));\n}\n      </input>\n      <output>Return a Promise that resolves after the given millisecond delay.</output>\n    </example>\n\n    <!-- Target n=16 positive examples -->\n    <example type=\"positive\" id=\"ex6\" target_n=\"16\" language=\"Python\">\n      <input>\ndef embed(text, key, model, dim):\n    client = OpenAI(api_key=key)\n    return client.embeddings.create(model=model, input=text, dimensions=dim).data[0].embedding\n      </input>\n      <output>Call the OpenAI embeddings API with the given model and return the vector.</output>\n      <why>Imperative verb, no hedging, describes inputs and output, under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex9\" target_n=\"16\" language=\"TypeScript\">\n      <input>\nasync function insertObservation(batchId: string, rows: ObsRow[]): Promise&lt;void&gt; {\n    await db.query(INSERT_BATCH_SQL, [batchId, rows]);\n}\n      </input>\n      <output>Insert observation rows into TimescaleDB batch_id column with retry.</output>\n      <why>Names the target (TimescaleDB), the key column (batch_id), and the retry behavior. Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex11\" target_n=\"16\" language=\"Go\">\n      <input>\nfunc (f *Forklift) DispatchTile(tile []byte, shmName string) error {\n    if err := f.shm.Write(tile); err != nil {\n        return err\n    }\n    return f.queue.Publish(shmName)\n}\n      </input>\n      <output>Dispatch a tile via Forklift shared memory and publish its handle to the queue.</output>\n      <why>Uses Origami vocabulary correctly (Forklift = heavy tiling operator). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex12\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nasync function archiveToWarehouse(obj, key) {\n    await s3.putObject({ Bucket: 'warehouse-cold', Key: key, Body: obj });\n}\n      </input>\n      <output>Store an object into the Warehouse cold bucket under the provided key.</output>\n      <why>Uses Origami vocabulary correctly (Warehouse = S3-compatible object storage). Names the bucket tier.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_chrony\" target_n=\"16\" language=\"Python\">\n      <input>\ndef wait_for_clock_sync(tolerance_ms=1.0, timeout_s=30):\n    deadline = time.time() + timeout_s\n    while time.time() &lt; deadline:\n        offset = chrony_client.get_offset()\n        if abs(offset) &lt; tolerance_ms / 1000.0:\n            return True\n        time.sleep(0.1)\n    return False\n      </input>\n      <output>Poll Chrony clock offset until within tolerance or timeout, returning sync status.</output>\n      <why>Uses Origami vocabulary (Chrony). Names the condition (tolerance) and return value. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_grammar\" target_n=\"16\" language=\"JavaScript\">\n      <input>\nfunction passesGrammar(description) {\n    const tokens = description.trim().split(/\\s+/);\n    if (tokens.length &lt; 8 || tokens.length &gt; 32) return false;\n    const hedges = ['may', 'might', 'could', 'possibly', 'generally', 'typically'];\n    if (hedges.some(h =&gt; description.toLowerCase().includes(h))) return false;\n    const last = tokens[tokens.length - 1];\n    if (last.endsWith(',') || last.endsWith(';') || last.endsWith(':')) return false;\n    return true;\n}\n      </input>\n      <output>Validate a description against the MLD grammar rules for token count, hedging words, and trailing punctuation.</output>\n      <why>Names all three checked conditions. No filler. Under target.</why>\n    </example>\n\n    <!-- Target n=24 positive examples -->\n    <example type=\"positive\" id=\"ex13\" target_n=\"24\" language=\"Python\">\n      <input>\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('text')\n    args = parser.parse_args()\n    print(args.text)\n      </input>\n      <output>Parse the command-line text argument, collect the parsed args, and print the text value to standard output.</output>\n      <why>Imperative verb, complete sentence, compound action. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex16\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn emit(&amp;self, digest: &amp;Digest) {\n    let proto = digest.serialize_to_proto();\n    self.redis.xadd(\"catwalk:digest\", &amp;[(\"proto\", &amp;proto)]);\n}\n      </input>\n      <output>Serialize the digest to protobuf and publish it to the Catwalk digest stream in Redis.</output>\n      <why>Uses Origami vocabulary correctly (Catwalk = metrics SDK). Names the wire format (protobuf) and transport (Redis stream).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex17\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_cpu(&amp;self) -> Vec&lt;Metric&gt; {\n    let pcts = read_cpu_pcts();\n    pcts.iter().map(|p| Metric::new(\"cpu\", *p)).collect()\n}\n      </input>\n      <output>Read per-core CPU percentages from /proc and emit them as Watchtower CPU metrics.</output>\n      <why>Uses Origami vocabulary (Watchtower = metrics collector). Names the data source (/proc).</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex18\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function pollManifest(url: string, etag: string): Promise&lt;{config: Config, etag: string} | null&gt; {\n    const res = await fetch(url, { headers: { 'If-None-Match': etag } });\n    if (res.status === 304) return null;\n    return { config: await res.json(), etag: res.headers.get('ETag') ?? etag };\n}\n      </input>\n      <output>Poll the Manifest endpoint with an ETag, return new config and updated ETag when changed, or null on 304.</output>\n      <why>Uses Origami vocabulary (Manifest = hot config reload). Names the HTTP conditional-get mechanism (ETag, 304). Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_memory\" target_n=\"24\" language=\"Rust\">\n      <input>\nfn collect_memory(&amp;self) -> Vec&lt;Metric&gt; {\n    let info = procfs::Meminfo::new().unwrap();\n    let used = info.mem_total - info.mem_free - info.buffers - info.cached;\n    vec![\n        Metric::new(\"mem_used_bytes\", used as f64),\n        Metric::new(\"mem_total_bytes\", info.mem_total as f64),\n    ]\n}\n      </input>\n      <output>Read memory info from /proc/meminfo, compute used bytes by subtracting free and cache, and return two Watchtower memory metrics.</output>\n      <why>Names the source (/proc/meminfo), the computation (subtracting free and cache), and the output type (Watchtower memory metrics). Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_rust_timescale\" target_n=\"24\" language=\"Rust\">\n      <input>\nasync fn insert_batch(pool: &amp;PgPool, batch_id: Uuid, rows: &amp;[MetricRow]) -> Result&lt;u64&gt; {\n    let query = build_insert_query(rows.len());\n    let result = sqlx::query(&amp;query).bind(batch_id).execute(pool).await?;\n    Ok(result.rows_affected())\n}\n      </input>\n      <output>Build and execute a parameterized batch INSERT into TimescaleDB, binding the batch_id, and return the rows-affected count.</output>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_rest\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function getDeviceStatus(req: Request, res: Response): Promise&lt;void&gt; {\n    const device = await registry.lookup(req.params.id);\n    if (!device) { res.status(404).json({ error: 'not found' }); return; }\n    const status = await dispatch.query({ action: 'status', target: device.id });\n    res.json({ device: device.id, ...status });\n}\n      </input>\n      <output>Look up the device in the registry, query its status through Dispatch, and respond with the merged device and status JSON.</output>\n      <why>Uses Origami vocabulary (Dispatch). Names all three steps. Complete sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_js_forklift\" target_n=\"24\" language=\"JavaScript\">\n      <input>\nasync function dispatchTile(tile, shmRegion) {\n    await shmRegion.write(tile.data);\n    await queue.publish(JSON.stringify({ handle: shmRegion.name, shape: tile.shape }));\n}\n      </input>\n      <output>Write a tile's pixel data into the Forklift shared memory region and publish its handle and shape to the dispatch queue.</output>\n      <why>Uses Origami vocabulary (Forklift). Names both operations. Complete sentence.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_catwalk\" target_n=\"24\" language=\"Python\">\n      <input>\ndef increment(self, name: str, value: float = 1.0):\n    proto = Counter(name=name, value=value, timestamp=time.time()).SerializeToString()\n    self.redis.xadd('catwalk:counter', {'proto': proto})\n      </input>\n      <output>Serialize a Catwalk Counter protobuf and publish it to the catwalk:counter Redis stream.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf) and the exact stream key.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_ts_switchboard\" target_n=\"24\" language=\"TypeScript\">\n      <input>\nasync function restartService(req: Request, res: Response): Promise&lt;void&gt; {\n    const { service } = req.params;\n    await dispatch.send({ action: 'restart', target: service });\n    res.json({ status: 'ok', service });\n}\n      </input>\n      <output>Receive a restart request, forward the action to Dispatch for the target service, and respond with a success acknowledgment.</output>\n      <why>Uses Origami vocabulary (Dispatch = control plane interface). Names the action and the acknowledgment. Under target.</why>\n    </example>\n\n    <!-- Target n=32 positive examples -->\n    <example type=\"positive\" id=\"ex19\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_sprint(plan):\n    missions = parse_plan(plan)\n    for m in missions:\n        report = marionette.execute_mission(m)\n        log.append(report)\n        if report.status == 'RED':\n            break\n      </input>\n      <output>Parse the sprint plan into missions, dispatch each to Marionette in order, append every report to the log, and stop on the first RED verdict.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes the stop condition. Complete compound sentence. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex20\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_pipeline(func_text, budgets=[8,16,24,32], n=4):\n    candidates_by_budget = {b: [generate(func_text, b) for _ in range(n)] for b in budgets}\n    filtered = {b: [c for c in cs if grammar_filter(c)] for b, cs in candidates_by_budget.items()}\n    centroid = {b: embedding_centroid(filtered[b]) for b in budgets}\n    return convergence_verdict(centroid)\n      </input>\n      <output>Spawn parallel MLD candidate generation at each budget tier, grammar-filter rejects, embed survivors, compute convergence verdict from per-draft spherical variance.</output>\n      <why>All key pipeline stages named. Origami vocabulary (MLD, grammar-filter, convergence_verdict). Imperative verb. Under target.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex21\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Evaluate(metrics []float64) (AnomalyVerdict, error) {\n    gadf := ToGADF(metrics)\n    tensor := preprocess(gadf)\n    prob, err := wg.model.Infer(tensor)\n    if err != nil { return AnomalyVerdict{}, err }\n    return AnomalyVerdict{Score: prob, Anomalous: prob &gt; wg.threshold}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image, run it through the Watch Glass ResNet18 classifier, and return the anomaly verdict with score.</output>\n      <why>Uses two Origami vocabulary terms correctly (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Complete sentence. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex22\" target_n=\"32\" language=\"Rust\">\n      <input>\npub fn update_hull(&amp;mut self, metrics: &amp;[f64]) -> HullVerdict {\n    let hull = ConvexHull::from_points(metrics);\n    let z = self.z_buffer.score(&amp;hull);\n    self.history.push(hull.clone());\n    HullVerdict { hull, z_score: z, healthy: z &lt; self.threshold }\n}\n      </input>\n      <output>Compute the convex hull from metric samples, score it against the Prism Z-buffer baseline, append to hull history, and return a health verdict.</output>\n      <why>Uses Origami vocabulary (Prism = geometric monitoring). Names the algorithm (convex hull, Z-buffer). Names the output type (health verdict). Complete sentence. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_py_orchestrator\" target_n=\"32\" language=\"Python\">\n      <input>\ndef run_mld_for_module(module_path, budgets=[8,16,24,32]):\n    source = parse_module(module_path)\n    for fn in source.functions:\n        candidates = {b: [generate_candidate(fn.body, b) for _ in range(4)] for b in budgets}\n        valid = {b: [c for c in cs if passes_grammar(c)] for b, cs in candidates.items()}\n        best = {b: embed_centroid(valid[b]) for b in budgets if valid[b]}\n        fn.mld = antipode_select(best)\n    return source\n      </input>\n      <output>Parse module source, generate grammar-filtered MLD candidates at each budget tier for every function, embed and antipode-select the best, and annotate the source tree.</output>\n      <why>Full pipeline named. Three n=32-tier descriptors: algorithm (antipode-select), operation (grammar-filtered), and artifact (source tree). Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_watchglass\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (wg *WatchGlass) Classify(window []float64) (Classification, error) {\n    gadf := ComputeGADF(window)\n    img := TensorFromGADF(gadf)\n    logits, err := wg.model.Forward(img)\n    if err != nil { return Classification{}, err }\n    label, score := Argmax(logits)\n    return Classification{Label: label, Score: score, Anomalous: label != \"nominal\"}, nil\n}\n      </input>\n      <output>Convert a Watchtower metric window to a GADF image tensor, run it through the Watch Glass ResNet18 forward pass, and return the anomaly classification with confidence score.</output>\n      <why>Uses two Origami terms (Watchtower, Watch Glass). Names the algorithm (GADF, ResNet18). Names the output type and its fields. Three informative clauses. Under 32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(tier, block string) (*Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    result, err := m.anthropic.Messages(agent, grammar, block, m.tools)\n    if err != nil { return nil, err }\n    return parseReport(result), nil\n}\n      </input>\n      <output>Load the tier agent definition and mission grammar, dispatch the block to the Anthropic Messages API with the tool set, parse the response, and return the mission report.</output>\n      <why>Uses Origami vocabulary (Marionette). Names all four stages. Names the API (Anthropic Messages). Complete compound sentence. At 28 tokens, fits target n=32.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_catwalk\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (c *Catwalk) EmitDetection(d Detection) error {\n    proto, err := proto.Marshal(&amp;d)\n    if err != nil { return err }\n    return c.redis.XAdd(ctx, &amp;redis.XAddArgs{\n        Stream: \"catwalk:detection\",\n        Values: map[string]interface{}{\"proto\": proto},\n    })\n}\n      </input>\n      <output>Marshal a Detection to protobuf and publish it to the Catwalk detection stream in Redis.</output>\n      <why>Uses Origami vocabulary (Catwalk). Names the wire format (protobuf). Names the exact stream. Under target n=24.</why>\n    </example>\n\n    <example type=\"positive\" id=\"ex_go_marionette2\" target_n=\"32\" language=\"Go\">\n      <input>\nfunc (m *Marionette) ExecuteMission(block string, tier string) (Report, error) {\n    agent := m.loadAgent(tier)\n    grammar := m.loadGrammar()\n    return m.claude.RunMission(agent, grammar, block)\n}\n      </input>\n      <output>Load the tier's agent definition and mission grammar, then dispatch the mission block to Claude and return the execution report.</output>\n      <why>Uses Origami vocabulary (Marionette = executor). Describes all three steps. Names the output (execution report). Complete sentence. Under target.</why>\n    </example>\n\n    <!-- Anti-examples from Live Validation Failures -->\n    <anti_example source=\"v1_validation\" id=\"ae1\">\n      <output>Call the OpenAI embeddings API with the given model and dimensions, trunc</output>\n      <reason>Rule 7 \u2014 truncation. The last word \"trunc\" is a fragment of \"truncating\". The budget cap cut the sentence mid-word. Write sentences that complete before the budget, not sentences that overflow. Fix: \"Call the OpenAI embeddings API with model, input, and dimensions, and return the vector.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae2\">\n      <output>Parse CLI arguments for text, file,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. The sentence was mid-enumeration when it hit the budget. Fix: \"Parse CLI arguments for text and file, then resolve the input source.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae3\">\n      <output>...truncating text to thirty thousand characters, and</output>\n      <reason>Rule 7 \u2014 truncation. Trailing conjunction \"and\" at end-of-string signals an incomplete compound clause. Fix: \"Truncate input text to thirty thousand characters before embedding.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae4\">\n      <output>Merge delta into a copy of state,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma makes it look like the start of a list that never arrives. Even if the behavior is technically described, the trailing comma flags a malformed sentence. Fix: \"Merge delta into a copy of state, deleting keys where the delta value is None.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae5\">\n      <output>The function handles parsing of YAML configuration files.</output>\n      <reason>Rule 1 \u2014 starts with \"The\" (determiner). Not an imperative verb. Fix: \"Parse YAML configuration files and return the populated config object.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae6\">\n      <output>Is responsible for embedding documents into vector space.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula). Also passive in spirit. Fix: \"Embed documents into vector space and return their float arrays.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae7\">\n      <output>May possibly typically return a vector depending on the input.</output>\n      <reason>Rule 3 \u2014 \"May\", \"possibly\", \"typically\" are all hedging words. Three violations in one sentence. Fix: \"Return the embedding vector, or raise ValueError on empty input.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae8\">\n      <output>Basically just wraps the Redis client to publish a protobuf message.</output>\n      <reason>Rule 4 \u2014 \"Basically\" and \"just\" are filler. Fix: \"Wrap the Redis client and publish a serialized protobuf message to the target stream.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae9\">\n      <output>Process the data and return the result.</output>\n      <reason>Semantics failure: \"Process\" is semantically empty. Fails the lossless test \u2014 a reader cannot reconstruct the function's behavior. Fix (requires knowing the code): \"Normalize sensor readings to zero-mean unit variance and return the float array.\"</reason>\n    </anti_example>\n\n    <anti_example source=\"v1_validation\" id=\"ae10\">\n      <output>Perform an embedding operation on the input.</output>\n      <reason>Semantics failure: name restatement. If the function is named embed, this adds no information. The description must say WHAT IT DOES. Fix: \"Call the OpenAI embeddings API and return a 1536-dimensional float vector.\"</reason>\n    </anti_example>\n\n    <!-- Inline bad examples from worked examples section -->\n    <anti_example source=\"worked_examples\" id=\"ae_bad2\">\n      <output>This function adds two numbers together.</output>\n      <reason>Rule 1 \u2014 starts with \"This\" (determiner lead). Not an imperative verb.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad3\">\n      <output>Is a function that adds two numbers.</output>\n      <reason>Rule 6 \u2014 starts with \"Is\" (copula lead).</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad7\">\n      <output>May call the embeddings API and possibly return a vector.</output>\n      <reason>Rule 3 \u2014 \"May\" and \"possibly\" are hedging words.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad8\">\n      <output>A vector is returned by calling the OpenAI embeddings API.</output>\n      <reason>Rule 5 \u2014 \"is returned by\" is passive voice.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad10\">\n      <output>Basically inserts things into the database, might retry sometimes.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and Rule 3 \u2014 \"might\" (hedging). Both rejected.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad14\">\n      <output>Parse command-line arguments for text, file, dimension, model, API key, and output,</output>\n      <reason>Rule 7 \u2014 truncation. Trailing comma signals an incomplete list. This is a live failure observed in validation.</reason>\n    </anti_example>\n\n    <anti_example source=\"worked_examples\" id=\"ae_bad15\">\n      <output>Basically parse CLI args and just print the text that was provided.</output>\n      <reason>Rule 4 \u2014 \"Basically\" (filler) and \"just\" (filler). Both rejected.</reason>\n    </anti_example>\n  </examples>\n\n  <payload>\n    <function file=\"/Users/elliotwillis/Desktop/pidgin-standalone/utils/parser.py\" name=\"ImportMeta\" line_start=\"716\">\n[CDATA-OPEN]\n            \"\"\"Metadata for a single import statement.\"\"\"\n    kind: str                    # \"import\" | \"from\"\n    module: str\n    names: \"list[str]\"\n    line: int\n[CDATA-CLOSE]\n    </function>\n  </payload>\n\n  <output_schema>\n    <field name=\"draft_1\" type=\"string\" min_tokens=\"1\" max_tokens=\"120\"/>\n    <field name=\"draft_2\" type=\"string\" min_tokens=\"1\" max_tokens=\"80\"/>\n    <field name=\"draft_3\" type=\"string\" min_tokens=\"1\" max_tokens=\"60\"/>\n  </output_schema>\n</pidgin_prompt>\n", "completion": "Implement ImportMeta class with attributes for import statement metadata.", "label": "negative", "reason": "", "function_id": "d309d633df5e6afe", "centroid_distance": 0.15053874600811423}
